# Atlas — Project Board

> **The single source of truth for "where we are."** Update this whenever a task changes status or work happens.
> Use the **`/board`** skill to update consistently. Last updated: **2026-06-30**.

**Status legend:** 📋 Todo · 🔵 In Progress · 🔍 Review · ✅ Done · ⛔ Blocked · ⏸️ Deferred (Phase-1+)

---

## 📍 YOU ARE HERE

> **Blueprint phase complete.** All 19 design docs are written (v1.0). Continuity scaffolding (this board, README, CLAUDE.md, memory, skills) is being set up.
> **Next milestone:** begin the build at **Sprint F1 — Foundation** (auth + tenancy + CI gates). Nothing is coded yet.
> **Suggested next action:** decide whether to `git init` the repo, then scaffold the monorepo per `docs/16` §2 and start F1.

**Progress at a glance**
- 📘 Blueprint (docs): **19 / 19 ✅ (100%)**
- 🧰 Project setup: **✅ done** (CLAUDE.md, README, board, skills, memory, git+GitHub)
- 🏗️ Build: **F1 ✅ + F2 ✅ COMPLETE** (Phase F done). **Now in Phase I (Ingest):** I1 (AWS crawler) ∥ I2 (GitHub crawler) implement the frozen SDK (`docs/06`/`07`). **I1 (AWS crawler) CODE-COMPLETE** (I1.1–I1.4; live-account verify pending creds). **Phase I CODE-COMPLETE** (I1 AWS + I2 GitHub, live verify pending creds). **Phase G: G1 (inference) ✅ COMPLETE** (all 8 rules, atlas.service, determinism + precision, infer stage wired). **G2 (read API) ✅ COMPLETE.** **Sprint G3 (AI engine) ✅ COMPLETE** — `@atlas/ai`: LLMProvider (Mock+Claude) + planner + retrieval/context + grounding gate + answer pipeline (cited, confidence-tiered, honest-absence, uncited-claim detection) + AI eval set (escaped-hallucination 0). Wired into the API: `/ai` SSE endpoints + conversation persistence (Claude in prod, mock in dev/CI). **Sprint G4 (Explore UI) ✅ COMPLETE** (G4.1 foundation + G4.2 Explore list/detail/provenance + G4.3 impact analysis + G4.4 Ask AI SSE chat + ⌘K). **✅ EPIC G COMPLETE** — the graph is queryable, explorable, and answerable (cited + confidence-tiered end to end). **Now in Phase P:** P1.1 (dashboard metrics) ✅ + **P1.2 (onboarding wizard + gated `POST /demo/seed` + reusable UI primitives) ✅**. **Next: P1.3 (state polish) → P2 (hardening) → GA.** Live data awaits connector creds (deferral ledger). *(Env note: `next build` is broken repo-wide by next/react version drift — see P1.3; CI `check` is green.)*

---

## How to use this board (for future-me)
- Each **Epic** = a phase from `docs/15`. Each **Task** has an ID, Status, linked doc(s), and a **Status note** (the latest comment on it).
- When you work: set the task 🔵, leave a Status note, and **append a dated entry to the Activity Log** at the bottom.
- When done: set ✅, note what "done" means (which `docs/15` Definition-of-Done gates passed).
- Keep tasks small enough to reason about; add sub-tasks under a sprint as you start it.

---

## EPIC 0 — 📘 Blueprint / Documentation — ✅ DONE
*All design docs written, internally consistent, v1.0. See `docs/README.md`.*

| ID | Doc | Status | Note |
|---|---|---|---|
| DOC-00 | project-overview | ✅ | Vision, P1–P10, personas, metrics |
| DOC-01 | product-requirements | ✅ | FR/NFR/US + Gherkin |
| DOC-02 | system-architecture | ✅ | Modular monolith + workers, 5 planes |
| DOC-03 | domain-model | ✅ | Entities, BR-x, dual identity |
| DOC-04 | database-schema | ✅ | PG DDL, RLS, migrations; +Phase-1 auth tables |
| DOC-05 | knowledge-graph | ✅ | URN, edges, 8 inference rules, confidence |
| DOC-06 | aws-crawler | ✅ | Connector SDK, service catalog, partial-sync |
| DOC-07 | github-crawler | ✅ | App, webhooks, deploy inference |
| DOC-07b | bitbucket-crawler | ⏸️ | Phase-2 contingency (design only) |
| DOC-08 | api-specification | ✅ | REST, DTOs, SSE, OpenAPI; +Google/domain endpoints |
| DOC-09 | frontend-spec | ✅ | Next.js + shadcn, viz, 4 states, a11y |
| DOC-10 | ai-engine | ✅ | Grounded, cited, 7-layer anti-hallucination |
| DOC-11 | search-engine | ✅ | OpenSearch hybrid, embeddings |
| DOC-12 | authentication | ✅ | Google login, RBAC, hd-domain join (Phase-1) |
| DOC-13 | security | ✅ | Threat model, IAM, Persona-E package |
| DOC-14 | testing-strategy | ✅ | Pyramid + adversarial QA agent, DoD gates |
| DOC-15 | development-roadmap | ✅ | Sprints, DoD, MVP checklist |
| DOC-16 | coding-standards | ✅ | Gold standard, parse-don't-validate, shadcn-MCP |
| DOC-17 | deployment | ✅ | Fargate, CI/CD, DR |
| DOC-18 | business-model | ✅ | Pricing, GTM, moat |

---

## EPIC SETUP — 🧰 Project Continuity Scaffolding — ✅ DONE
*Make any future session instantly resumable (this session's work).*

| ID | Task | Status | Note |
|---|---|---|---|
| SET-1 | Root `CLAUDE.md` (rules + session protocol) | ✅ | Auto-loads every session |
| SET-2 | `docs/README.md` (index + cross-ref map) | ✅ | Navigation for all 19 docs |
| SET-3 | `docs/PROJECT-BOARD.md` (this board) | ✅ | Jira-lite tracking + activity log |
| SET-4 | Skills: `/resume`, `/board`, `/atlas-doc` | ✅ | In `.claude/skills/` |
| SET-5 | Memory files (decisions, state, prefs) | ✅ | 5 files + `MEMORY.md` in project memory dir |
| SET-6 | `git init` + GitHub remote | ✅ | Repo `atlas` on `main`; **private** GitHub remote `anmolbhardwaj17/atlas` pushed. |

---

## EPIC F — 🏗️ Foundation — 🔵 IN PROGRESS  *(walking skeleton + safety rails, `docs/15` Phase F)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| F1.1 | Monorepo scaffold + shared tooling + `config` package | ✅ | 16 §2/§4, 17 §6 | pnpm workspaces + strict TS + eslint(no-any) + prettier + vitest. `@atlas/config` (Zod env, parse-don't-validate). **All gates green: format/lint/typecheck/6 tests.** |
| F1.2 | CI gate harness (lint/typecheck/test) | ✅ | 14 §16, 17 §5 | GitHub Actions `.github/workflows/ci.yml`: pnpm + Node 22, format/lint/typecheck/test on push to main + PRs. **First run green (22s).** Badge on README. Heavier stages (integration/contract/adversarial-QA/E2E/load) added in later sprints. |
| F1.4 | DB foundation: Postgres (plain `pg`) + SQL migrations + core tables + RLS | ✅ | 04, 13 §6, 17 | `@atlas/db`: **plain `pg`, no ORM** (Kysely removed per user). Typed row schema, 5 core tables, own forward-only SQL migration runner (`schema_migrations`), `atlas_app` non-bypass role + RLS policies, `withOrgScope` GUC helper. **Verified end-to-end on live Supabase** — RLS isolation 3/3 (org A↔B + fail-closed), app connects as `atlas_app` / migrations as `postgres`. Fixes from real testing: `NULLIF` empty-string GUC (pooled conns), session-pooler (IPv4), two-role model (Supabase `postgres` has BYPASSRLS). `docker-compose.yml`+`.env.example` added. CI Postgres job = follow-up (F1.7). |
| F1.5 | Supabase Auth (Google) integration + session/JWT verify | ✅ | 12 | **Live Google login verified end-to-end** (real account → `/me` 200). `docs/12` §2–4 rewritten v1.1 (docs-before-code): Supabase-hosted Google OAuth (DD-1a), Atlas verifies the ES256 JWT via JWKS and mints no tokens (DD-2 revised), identity mirrored into `public.users` (id=auth uid) + `auth_identities`, active org resolved live via `app_user_memberships` SECURITY DEFINER fn (org not in token). **Backend:** `CoreModule` (env + atlas_app pool), `SupabaseJwtVerifier` (jose JWKS, iss/aud, HS256 fallback), `AuthGuard`, `UserMirrorService`, `MembershipService`, `GET /me`; claims unit tests. **Frontend:** `@supabase/ssr` browser/server/middleware clients, Sign-in-with-Google page, `/auth/callback`, server home calling `/me`. **Migrations:** 0003 resolver fn (EXECUTE→atlas_app), 0004 identity-table RLS policies `TO atlas_app` (Supabase auto-enables RLS on all public tables — global identity tables needed policies; anon/authenticated deliberately denied). Guard 401s (missing/bogus/malformed) verified. Ports moved to **API :4290 / web :4291** (avoid local clashes). All gates green. |
| F1.6 | Org / RBAC / memberships / invitations (endpoints + guards) | ✅ | 12, 03, 08 | **Live-verified through the app** (real Google session → create org → Owner; invite pending; members RLS-scoped read; last-Owner demote rejected; cross-tenant 404). **Guards:** `TenantScopeGuard` (resolves active org + live role from `X-Atlas-Org`/path via the SECURITY DEFINER resolver; path cross-tenant→404, header→403) + `@Roles`/`RolesGuard` (Owner>Admin>Member). **Endpoints:** POST /orgs (creator→Owner, BR-ORG-1), GET/PATCH /orgs/:id, GET /orgs/:id/members, PATCH/DELETE members (BR-MEM-2/3 invariants), POST/GET/DELETE invitations, POST /invitations/:token/accept (capability token, sha256-hashed, never returned). **API conventions:** `{data}` success envelope + `{error:{code,message,details,requestId}}` error model (docs/08 §4/§11) via global interceptor+filter; zod request validation; CORS for the web origin. **Migration 0005:** organizations org-scope RLS policy + `app_invitation_by_token` resolver. **Docs:** §5.2 RBAC + docs/08 §6 auth-endpoints synced. **Frontend:** create-org form + org-management panel (members/invitations/invite). Unit tests: RolesGuard, slug. All gates green. |
| F1.7 | Tenant-isolation test (US-12 foundation) + CI Postgres job | ✅ | 04 §10, 13 §6, 14 §16 | **CI `integration` job** added (`.github/workflows/ci.yml`): `postgres:16` service → run migrations as owner → grant `atlas_app` LOGIN (throwaway pw via `setup-app-role.ts`) → run env-gated integration tests against real PG with the two-role + RLS setup. **Expanded coverage** (`org-scope.test.ts`): organizations org-scope (+ fail-closed), invitations cross-org isolation, `app_user_memberships` + `app_invitation_by_token` resolvers, and a write-guard negative (can't INSERT another org's row while scoped). **9 integration tests pass** against live PG; skip cleanly in the no-DB `check` job (21 unit tests stay green). Expands to graph tables in the G sprints. |

### Sprint F2 — Connector framework + secrets + queue *(`docs/15` Phase F)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| F2.1 | `connections` + `sync_runs` schema (migration + RLS + types) | ✅ | 04 §5.2, 03 §3.5/3.6 | Migration 0006: both tables with org-scoped RLS (`TO atlas_app`), `updated_at` trigger, **BR-SYNC-1** partial-unique in-flight index. Row types + enums in `@atlas/db`. Integration test: connections cross-org isolation (10 integration tests pass on live PG). |
| F2.2 | `@atlas/connector-sdk` — frozen Connector contract | ✅ | 06 §3 (DD-1) | New pure-types package: `Connector` interface (verify/health/plan/discover/fetchDetail/normalize/extractSignals/observedEdges) + shared types (Connection, SyncRun, WorkPlan, Scope, ResourceRef, RawResource, NodeUpsert, Signal, EdgeUpsert, CrawlContext, SecretAccessor, VerifyResult). Provider union open (extensible). **Contract frozen in F2 (RMR-6).** |
| F2.3 | Graph persist schema (nodes/edges/provenance/raw_snapshots/node_kinds/inference_rules) | ✅ | 04 §5.3–5.4, §6, 05 | Migration 0007: full knowledge schema + indexes + composite FKs (same-org edges, BR-EDGE-1) + provenance-required/inferred-needs-rule constraints; `uq_edge` **NULLS NOT DISTINCT** (observed-edge dedupe, doc synced). Org-scope RLS on nodes/edges/provenance/raw_snapshots; global vocab policy on node_kinds/inference_rules. Row types in `@atlas/db`. **graph-scope.test.ts** (4 tests): node/edge org isolation, cross-tenant edge structurally rejected, observed-edge dedupe. **14 integration tests pass on live PG.** |
| F2.4 | Mock connector + staged sync runner (EXIT proof) | ✅ | 06, 02 §5.2, BR-SYNC-2 | **F2 exit criterion met.** New `@atlas/ingest`: `runStagedSync` (plan→per-scope discover→fetchDetail→normalize/observedEdges→persist→reconcile; each scope one tx; org-scoped via withOrgScope) + `MockConnector` (in-memory fake provider, failure injection, discover-call tracking) + `InMemorySnapshotStore` + dev SecretAccessor/logger. **4 integration tests on live PG:** idempotent persist (upsert by URN/uq_edge, no dupes), reaping (unseen → stale on clean sync), **BR-SYNC-2** (failed scope → partial → reconcile skipped → no false delete), resumability (checkpoint skips completed scopes). CI integration job now runs all integration tests. |
| F2.5 | BullMQ queue + worker runtime | ✅ | 02 §5 (DD-6) | Provider-agnostic `JobQueue` (keeps core decoupled / Temporal-swappable) + `InMemoryQueue` (dev/test, idempotent jobId dedupe, buffering) + `BullMQQueue` (Redis driver, thin) + sync-job worker (`createSyncHandler`/`registerSyncWorker`/`enqueueSync`, deterministic `syncJobId`). Worker handler loads connection → resolves connector → `runStagedSync`. Tests: InMemoryQueue unit (3) + worker→runner→persist on live PG (1). Scheduler + API enqueue-on-verify land with I1 (real connectors). |
| F2.6 | Secrets Broker (interface + dev impl) | ✅ | 13 §7 | `SecretBroker` (extends SDK `SecretAccessor`) + `InMemorySecretBroker` in `@atlas/ingest`. `put`→opaque `secret_ref` (BR-CONN-1, raw secret never on the row); `get`/`delete`. Prod = AWS Secrets Manager (later). |
| F2.7 | Raw-snapshot store (Supabase Storage) | ✅ | 04 §5.4, 13 | `SupabaseStorageSnapshotStore` (impl of `SnapshotStore`): private bucket, content-addressed (`<orgId>/<hash>.json`), idempotent upsert; service-role client (server-only); `storage_ref` = `<bucket>/<path>`. `ensureBucket` provisions programmatically. **Live round-trip verified** against real Supabase Storage (put→get, missing→null). |
| F2.8 | Connection API skeleton | ✅ | 08 §8 | **Live-verified** (real session → create→verify→connected). `/connections` (X-Atlas-Org scoping): POST (Admin), GET list (Member), GET :id (Member), POST :id/verify (Admin — stores creds via broker, calls connector.verify, maps status/health, 422 `connection_verification_failed` on error), DELETE :id (Admin, soft). `ConnectorRegistry` (placeholder mock connectors until I1/I2). zod DTOs; secrets never in responses (`secretConfigured` flag). Guard 401s verified; registry unit tests. |
| F1.3 | App shells: NestJS API + Next.js web + Turborepo | ✅ | 02, 09, 16, 17 | `apps/api` (Nest+Fastify, `/health` ✅ boots) + `apps/web` (Next 15/React 19 shell) + Turborepo build orchestration. `@atlas/config` now builds to CJS, consumed by API at runtime. All gates green. **Note:** full structured-logging/correlation-id observability package deferred to a follow-up (basic Nest logger for now). |
| F2 | Connector SDK interface + queue/worker/scheduler | 📋 | 06 §3, 02 §5 | BullMQ, the fork point |
| F2 | Secrets Broker + Secrets Manager | 📋 | 13 §7 | |
| F2 | Connections + sync_runs lifecycle + S3 snapshots | 📋 | 03, 04, 08 §8 | |

**Exit (M0):** Google login → create org → invite Member; US-12 + RLS pass; CI green; deploys to staging.

---

## EPIC I — 📥 Ingest — ✅ CODE-COMPLETE  *(both crawlers built; live verify pending creds)*

> **Phase I is CODE-COMPLETE — both crawlers built (I1 AWS + I2 GitHub), CI-green.** The only open items are **live verification** (need customer creds): AWS needs a read-only IAM role (ARN + Atlas External ID, docs/13 §4); GitHub needs an App installation (App id + installation id + private key, docs/13 §5). When creds exist: create connection → verify (→ connected/degraded + missing-perms) → confirm populated nodes/edges/provenance + raw snapshots. **Additive follow-ups** (no architecture change): AWS DynamoDB/ElastiCache/APIGW/IAM-policy (docs/06 §9); GitHub webhook API route + worker process (deploy). **Next milestone: G1 (inference engine + edges)** — reads the emitted signals → derives cross-source edges (`docs/05` R1–R8).

### Sprint I1 — AWS crawler *(`docs/06`)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| I1.1 | AWS connector foundation: package + URN grammar + node-kind vocab seed | ✅ | 05 §2/§3, 06 §4 | New `@atlas/connector-aws` (CJS, requireable by API). `awsUrn()` builds deterministic 5-segment URNs matching docs/05 §2.2 exactly (region-scoped + literal `global` for S3/Route53/IAM; natural key case-preserved). `AWS_NODE_KINDS` catalog (18 kinds + URN type/scope). Migration 0008 seeds `node_kinds` (24 = 18 AWS + 5 GitHub + 1 atlas) — global vocab needed before any node insert (FK). 9 unit tests; migration applied to live DB (verified counts). |
| I1.2 | Credential provider (STS AssumeRole + externalId) + verify/health + permission detection → degraded | ✅ | 06 §2/§8, 13 §4 | `StsCredentialProvider` (sts:AssumeRole via Atlas's own identity → ≤1h creds, in-memory only; `sessionName=atlas-…-<id>` for customer CloudTrail traceability; account id parsed from assumed-role ARN). External ID resolved from Secrets Broker (C1), never config. `parseAwsConfig` validates roleArn + regions allow-list. `verify`/`health` AssumeRole then run per-service permission probes: AccessDenied → **`degraded`** with the missing IAM actions (P3, US-1); transient errors ≠ missing perm; AssumeRole/config failure → `error`. Probe framework + `isAccessDenied` classification (real per-service probes land in I1.3). 27 unit tests (aws-sdk-client-mock for STS). |
| I1.3 | Service discoverers + pure normalize/extractSignals/observedEdges (golden fixtures) | ✅ | 06 §4/§5, 05, 14 §10 | Per-service `ServiceModule` layer (pure normalize/observedEdges/extractSignals dispatched by node kind; additive per docs/06 §9). **14 modules**: VPC, Subnet, SecurityGroup, EC2, Lambda, ECS (cluster/service/taskdef), ECR, ELB, Route53, RDS, S3, IAM-role — exercising **every observed edge type** (CONTAINS, PROTECTS, ASSUMES_ROLE, USES_IMAGE, ROUTES_TO, TRIGGERS) + signals for R2/R3/R8 inference. Deterministic URNs via `awsUrn`; `plan()` fans region×service + global-once. Route53→ALB kept as a **signal not observed edge** (alias DNS ≠ deterministic ELB → P3). Golden-fixture tests assert exact nodes/edges/signals (100% precision on fixtures). 44 unit tests. **Additive remaining** (not silently dropped): DynamoDB, ElastiCache, API Gateway, IAM-policy. discover/fetchDetail (live SDK) = I1.4. |
| I1.4 | Resilience (rate-limit/retry/pagination) + incremental hash-diff + wire into runner/registry/API | ✅ | 06 §7, 02 §5 | **Code-complete (live-account verify pending creds).** Discovery layer: `Discoverer` interface + 14 live discoverers (EC2/VPC/Subnet/SG via one client, Lambda, ECS×3, ECR, ELB w/ target-health, Route53, RDS, S3 multi-call, IAM inline-policies) with SDK paginators + adaptive retry; connector `discover`/`fetchDetail` collapse list+detail via a per-run payload cache; one AssumeRole per run. `withRetry` + `classifyAwsError` (throttling/transient backoff+jitter; access-denied/not-found/fatal pass-through, docs/06 §7.3). Permission probes derived from discoverers (verify now does real detection). **Registry wired** — real `AwsConnector` for "aws" (API boots clean). **Enqueue-on-verify** — onboarding `sync_runs` row + `enqueueSync` (BR-SYNC-1 one-in-flight). **Incremental hash-diff** in runner: unchanged content hash → skip re-snapshot (`unchanged` stat), live-DB tested. 54 connector-aws unit tests (incl. aws-sdk-client-mock) + 11 ingest integration. |

### Sprint I2 — GitHub crawler *(`docs/07`)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| I2.1 | GitHub connector foundation: package + URN grammar + external.package vocab | ✅ | 05 §2/§3, 07 | New `@atlas/connector-github` (CJS). URN builders match docs/05 §2.2 (repo `github:o/r`, PR `…:pr:n`, workflow `…:workflow:<path>`, team `github:o:team:slug`, user `github:user:login`, package `external:<eco>:package:<name>`); case-significant keys preserved. `GITHUB_NODE_KINDS` catalog (5 github + external.package). Migration **0009** seeds `external.package` (provider `external`, DEPENDS_ON_PKG target) — applied to live DB. 6 unit tests. |
| I2.2 | GitHub App auth (installation token) + verify/health + permission detection | ✅ | 07 §2, 12, 13 §5 | `GithubAppTokenProvider`: signs a short-lived App JWT (RS256, iat-backdated, exp +9min) via Node `createPrivateKey` (handles GitHub's PKCS#1 keys) → exchanges for an **installation access token** (≤1h, in-memory, read-only — mirrors AWS AssumeRole). App private key resolved from Secrets Broker (C1). Permission detection reads the token's granted `permissions` **directly** (no probing) → `degraded` listing missing `key:read` (metadata/contents/pull_requests/actions/members, docs/07 §2); bad config/key or failed exchange → `error`. 17 unit tests (config, permission logic, real RS256 JWT sign+verify via generated keypair, verify decision matrix with a fake token provider). Crawl stages stubbed (I2.3/I2.4). |
| I2.3 | Pure parsers + normalize/extractSignals/observedEdges (golden fixtures) | ✅ | 07 §6/§7, 05, 14 | Pure parsers: **CODEOWNERS** (rules + team/user classification), **manifests** (npm/pypi/go direct deps, additive ecosystems), **workflow deploys** (AWS actions + ECS `with:` + `run:` flags + ARNs via `yaml`). 6 entity modules dispatched by kind: repository (+ OWNED_BY from CODEOWNERS, DEPENDS_ON_PKG from manifests), pull_request (+ changed-files signal for R6), workflow (+ CONTAINS(repo→workflow) + deploy signal for R1), team/user/package (leaf nodes so edges resolve). **Deploy targets emitted as signals — DEPLOYS_TO is inferred (R1/G1), not observed (P3).** Connector dispatches normalize/observedEdges/extractSignals. 31 unit tests incl. golden fixtures. |
| I2.4 | Live discoverers + rate-limit/retry + webhook HMAC ingress + wiring | ✅ | 07 §5/§10, 13 §5 | **Code-complete (live-install verify pending App creds).** `FetchGithubClient` (bearer install-token, Link pagination, header-aware rate-limit/retry: primary `x-ratelimit-reset` wait, secondary `retry-after`, 5xx backoff — bounded, injectable). `crawlRepo` fetches repo + CODEOWNERS/manifests/workflows + recent PRs and yields repo/workflow/PR nodes **plus** the edge-target team/user/package nodes (so OWNED_BY/DEPENDS_ON_PKG resolve in-scope); `plan()` = one scope per installed repo (bounded/resumable); one installation token+client per run. **Webhook HMAC verify** (timing-safe, GHR-8) + event→descriptor mapping. **Registry wired** — real `GithubConnector` for "github" (API boots clean with both connectors); enqueue-on-verify already generic. 42 connector-github unit tests (webhook, client pagination+rate-limit via fetch mock, crawl+connector via fake client). Webhook API route + live crawl = deploy/live-creds step. |

**Exit (M1):** real AWS+GitHub → nodes + provenance; degraded reports missing perms; no false deletes.

---

## EPIC G — 🧠 Graph & Intelligence — 🔵 IN PROGRESS

### Sprint G1 — Inference engine + edges *(`docs/05` §6)* — ✅ DONE

> **G1 exit criterion MET:** the worked example (`docs/05` §9) reproduces — repo DEPLOYS_TO service, service CONNECTS_TO RDS, atlas.service + IMPLEMENTS/RUNS, OWNED_BY, CHANGED_BY — each cited + tiered; determinism golden file passes; precision 100% (≥95%) on the labeled set; convergence (zero churn on no-change); infer stage runs post-sync. **Next: G2 (read API + traversals + search).**

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G1.1 | Signals persistence + inference engine core + R1 (DEPLOYS_TO) | ✅ | 05 §6, 04 | New `signals` table (migration 0010 + `docs/04` §5.4b) persisted during sync (runner `extractSignals`→upsert). New `@atlas/inference` engine: pure versioned `Rule`s → candidate edges; engine loads nodes+signals+observed-edges, upserts inferred edges with **provenance + evidence + inference_rule_id**, and **retires** (not deletes) edges a rule no longer produces → **convergent (IE-4: re-run writes nothing)**. inference_rules seeded (migration 0011, R1–R8 metadata). **R1 repo→runtime DEPLOYS_TO**: workflow-deploy signals resolved vs AWS runtimes — exact ARN / unique name → `inferred-high`, ambiguous → multiple `inferred-low` (P3). 11 tests (4 pure R1 tiering + 3 live-DB engine: derive+provenance, converge, retire). |
| G1.2 | R4 (atlas.service + IMPLEMENTS/RUNS) + R6 (CHANGED_BY) + R5-propagation | ✅ | 05 §6.4 | Engine extended: rules now emit **derived nodes** (upserted first so their edges resolve) + read earlier rules' inferred edges in one dependency-ordered pass. **R4**: from a high `DEPLOYS_TO`, derive `atlas.service` (`atlas:<slug>:service:<repo>`) + `IMPLEMENTS`(repo→svc) + `RUNS`(runtime→svc); high-only (DD-1); one service per repo, RUNS×N runtimes. **R5-prop**: observed repo `OWNED_BY` + `IMPLEMENTS` → service `OWNED_BY` (high). **R6**: merged-PR `pr.files` signal + repo `IMPLEMENTS` → service `CHANGED_BY` pr (high if 1 service, low if monorepo — P3). `nodes.connection_id` made nullable (migration 0012 + `docs/04` §5.3) for derived nodes. 16 tests (12 pure R4/R5/R6/R1 + 4 live-DB incl. full R1→R4→R5→R6 worked-example chain). |
| G1.3 | R2 (SG correlation) + R3 (env refs) + R8 (IAM access) → CONNECTS_TO/STORES_IN | ✅ | 05 §6.4 | The data-dependency edges. **R3** (high): Lambda/ECS env value referencing a crawled RDS endpoint / S3 bucket / DynamoDB table → CONNECTS_TO / STORES_IN (evidence stores env KEY + resource, not raw value). **R2** (high): compute SG allowed (ingress) into a datastore SG on the engine port + same VPC → CONNECTS_TO (reachability, resolved via PROTECTS edges + sg.rules signals + groupRef→SG mapping). **R8** (low): ASSUMES_ROLE + IAM policy statement granting a specific resource ARN → CONNECTS_TO/STORES_IN; wildcard grants ignored (IAM permission ≠ use, P3). Shared `refs.ts` (engine-port, ARN→node resolution). 6 pure tests (+ engine integration unaffected). |
| G1.4 | Reconciliation/determinism golden files + precision harness ≥95% + wire infer stage | ✅ | 05 §6.5, 14 §10 | Pure `evaluateAll` (mirrors the engine fold, no DB) powers the quality tests. **Determinism golden file**: the docs/05 §9 worked example reproduces an exact 7-edge set (CONNECTS_TO×2, DEPLOYS_TO, IMPLEMENTS, RUNS, OWNED_BY, CHANGED_BY) + 1 derived atlas.service; re-run = identical plan (IE-1). **Precision harness** (≥95%): labeled fixtures incl. negative cases (wrong-port, wildcard-IAM) → **100% precision, zero false positives**. **Infer stage wired**: the sync worker runs `runInference` after each non-failed sync (docs/05 §6.1); inference errors are logged, never fail the sync (P7). Sync→infer proven end-to-end (mock sync w/ deploy signal → DEPLOYS_TO). 25 inference + 12 ingest tests; API boots clean. |

### Sprint G2 — Read API + traversals + search *(`docs/08` §9, `docs/11`)* — ✅ DONE

> **G2 exit:** the graph is queryable — nodes/edges/detail/neighbors, **blast-radius/dependencies** with why-chains + pathConfidence, /timeline, edge evidence, and **/search** (keyword; hybrid vector at deploy). All org-scoped + cited. Unblocks **G3 (AI) ∥ G4 (Explore UI)**. Search's OpenSearch vector driver deferred to deploy (ledger).

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G2.1 | Read API: nodes list/detail/edges/neighbors + cursor pagination | ✅ | 08 §9 | New `GraphModule` (Member+): `GET /nodes` (filter kind/region/status/confidence/`q`, keyset cursor over (last_seen,id) desc — stable under crawl writes, DD-3), `/nodes/:id` (detail + provenance/raw-snapshot click-through, P4), `/nodes/:id/edges` (in/out/both, type/confidence filters), `/nodes/:id/neighbors` (depth-1, node-budgeted). Org-scoped via withOrgScope (RLS); malformed/cross-tenant → 404. `parseBody` fixed for `.default()`/`.coerce` schemas. 6 live-PG service tests incl. tenant isolation. |
| G2.2 | Traversals: blast-radius (inbound) + dependencies (outbound) + why-chains | ✅ | 05 §7.2, 08 §9 | `GET /nodes/:id/blast-radius` (inbound impact, US-4) + `/dependencies` (outbound, US-9). Shared recursive-CTE `traverse`: bounded depth (≤6) + nodeBudget (≤500) + cycle guard (edge-path array), over-limit clamped with `warnings` (A21). Impact edge set CONNECTS_TO/DEPENDS_ON/ROUTES_TO/STORES_IN/DEPLOYS_TO (overridable). Each impacted node carries **`via` (why-chain edges w/ evidence + rule)** + **`pathConfidence` = weakest edge** (docs/05 §8); `minConfidence` prunes low paths (P3 high-only view). 7 live-PG tests incl. weakest-edge + minConfidence + depth-clamp. |
| G2.3 | /timeline + /edges/:id evidence + raw-snapshot link | ✅ | 08 §9/§10.3 | `GET /timeline?since=&kinds=&limit=` (US-5 "what changed"): new/updated nodes (first_seen vs last_seen → created/updated) + new edges since a timestamp, merged newest-first; `kinds` filters node changes. `GET /edges/:id`: full edge detail — from/to summaries, evidence, rule (`key@version`), provenance (source/observedAt) + raw-snapshot ref (P4 click-through). Both org-scoped (404/empty cross-tenant). 9 live-PG tests total (2 new: edge detail + isolation, timeline window + kind filter). |
| G2.4 | Search: SearchProvider + Postgres hybrid + index stage | ✅ | 11 | `GET /search?q=&kind=&type=` (Member+) behind a **`SearchProvider`** interface (SEARCH_PROVIDER token; OpenSearch driver swaps in at deploy — docs/11 impl note). `PostgresSearchProvider`: pg_trgm `similarity` + substring over name/urn/attributes (SE-1 — queries `nodes` directly, no separate index), returns `{node,score,match,highlights}` with `semantic:null` (keyword-only MVP). Org-scoped (SE-4/R8). 4 live-PG tests (identifier match + highlight, kind filter, attribute match, tenant isolation). API boots with both new modules. |

### Sprint G3 — AI engine *(`docs/10`)* — ✅ DONE  *(parallel with G4)*

> **G3 exit:** canonical questions answered with citations + confidence tiers; **honest-absence on zero grounding** (US-11); hallucination defenses L1–L7 in place; AI eval set (canonical + adversarial) green with escaped-hallucination rate 0 (<1% bar). New package `@atlas/ai`; `/ai` SSE endpoints live (Claude in prod, mock in dev/CI). **Next: G4 (Explore UI).**

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G3.1 | `@atlas/ai`: LLMProvider (Mock + Claude) + prompt + RetrievalPort | ✅ | 10 §3/§8 | New `@atlas/ai` pkg (CJS, NestJS-free). `LLMProvider` interface (streamed, tool-capable, AE-5) + `MockLLMProvider` (deterministic responder → LLMEvent stream, for CI eval) + `ClaudeProvider` (Anthropic SDK adapter; text + tool-call deltas). Versioned `SYSTEM_PROMPT` (§8 six invariants: grounding/citation/confidence/honesty/scope/injection-resistance) + `honestAbsence`. `RetrievalPort` interface (search/getNode/blastRadius/dependencies/edges/timeline) — the API adapts it over G2. 5 unit tests. |
| G3.2 | Query planner (intent → retrieval plan) + entity resolution | ✅ | 10 §4.2 | `classifyIntent` (rule-based, deterministic → reliable canonical questions): blast_radius/dependents/deploy_mapping/architecture/timeline/culprit/lookup/out_of_scope. `resolveEntity`: extract identifier-like/content terms (stopword-filtered) → `port.search` → merged, ranked candidates; 0 = unresolved (→ honest-absence), >1 near-top = ambiguous (candidates, P3 — never silently pick). `plan()` → typed `RetrievalPlan` (intent + entity + window). 15 unit tests (8 canonical-question classifications + resolution + ambiguity). |
| G3.3 | Retrieval orchestration + context builder + grounding gate (honest-absence) | ✅ | 10 §4.3–§4.5 | `orchestrate` runs the plan via RetrievalPort (blast_radius/dependents→blastRadius; deploy_mapping→in-DEPLOYS_TO; culprit→in-CHANGED_BY+timeline; timeline→window; lookup/architecture→edges) — retrieval-first, no LLM. `buildContext` (DD-3): compact CONTEXT block where every node/edge carries a stable `N#/E#` marker + `cite:id` + confidence + evidence + FRESHNESS notes; returns the `cites` map for deterministic binding (DD-5). `groundingGate` (DD-4, deterministic): out-of-scope / unresolved-entity → structured honest-absence reason (US-11); resolved entity with empty traversal still grounded ("nothing depends on X" is truthful). 7 unit tests (fake port). |
| G3.4 | Answer pipeline: narrator + citation engine + confidence scorer + AI eval in CI | ✅ | 10 §5–§7, 14 §11 | `answerQuestion` = plan→retrieve→gate→narrate→post-process. Gate fails → honest-absence (never calls the LLM, L1). Narrate via LLMProvider (low temp). **Citation engine** (DD-5/L4): binds `[N#]/[E#]` markers → real ids + provenanceUrl, dedup+numbered; unknown markers dropped (can't fabricate a source). **Uncited-claim detector** (L5): flags factual sentences lacking a marker (hedges excused). **Confidence scorer** (§5): overall = weakest cited tier; caveats from FRESHNESS. **AI eval set** (L7, docs/14 §11): canonical (cited+tiered) + honest-absence (unresolved/out-of-scope) + adversarial (fabrication blocked by gate / caught by L5) — escaped-hallucination rate 0. 10 unit+eval tests (37 pkg total). |
| G3.5 | SSE /ai endpoints + conversation memory + wire into API | ✅ | 08 §10.2, 10 §9 | `answerQuestionStream` (SSE events: retrieval→token→citation→confidence→done). `AiModule`: `GraphRetrievalPort` adapts the engine's port over G2 GraphService+SearchProvider; LLM provider = Claude when `ANTHROPIC_API_KEY` set, else dev mock (retrieval/grounding/citations still work). `ai_conversations`/`ai_messages` tables (migration 0013, org-scoped RLS). `POST /ai/conversations`, `POST /ai/conversations/:id/messages` (manual SSE via raw reply), `GET /ai/conversations/:id`. `askStream` persists user + assistant turns (citations + confidence). 3 live-PG tests (grounded+cited+persisted, honest-absence, cross-org 404); API boots with AiModule (/health 200). |

### Sprint G4 — Explore UI *(`docs/09`)* — ✅ DONE

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G4.1 | Frontend foundation: design system + tokens + AppShell + Dashboard | ✅ | 09 §3, §5.1–5.2 | Tailwind v3 + **certainty visual language** tokens (observed/inferred-high/inferred-low/stale as CSS vars); hand-vendored shadcn-style primitives (Card, Badge) + `cn()`; `ConfidenceBadge`/`FreshnessTag`/`CitationLink`; `lib/api.ts` (`getSession`/`apiGet` — Bearer + X-Atlas-Org); `AppShell` (TopBar + nav Dashboard/Explore/Ask/Settings + sign-out); server-rendered `Dashboard` (connections + recent-nodes preview + empty states). `page.tsx` now: Landing (no session) → create-org card (no membership) → **AppShell + Dashboard** (authed). Login now lands on a real home. `next build` green; `pnpm run check` green. |
| G4.2 | Explore: nodes list (filter/search) + node detail + provenance drawer + edges | ✅ | 09 §5.3 | Server-rendered (secrets stay server-side, NFR-22): `requireShell()` (session+org guard, redirects), `graph-types.ts` (read-API shapes). **`/explore`** — filter bar (GET form: q/kind/status/confidence; URL is source of truth, works no-JS) + nodes table (certainty + freshness per row) + keyset "Next page". **`/explore/[id]`** — node detail: header (confidence+freshness), attributes, **provenance** (source/observed/sync-run/raw-snapshot; "derived, not observed" when none), connections split incoming/outgoing with per-edge confidence + origin. **`/explore/edge/[id]`** — the **"why?"** page (P4): origin, derived-by rule, evidence JSON, raw source. Cross-tenant/absent → graceful not-found (404-as-not-found, R8). `next build` green (7 routes); `pnpm run check` green. |
| G4.3 | Blast-radius panel + neighbor subgraph (list-first, a11y) + graph canvas | ✅ | 09 §5.4 | **`/explore/[id]/impact`** — side-by-side **Blast radius** (downstream: what breaks if this changes) + **Dependencies** (upstream: what this needs), both from the G2 recursive-CTE traversals. **List-first (a11y)**: impacted nodes grouped by hop distance, each with **pathConfidence** (weakest-link, P3) and an expandable **why-chain** (native `<details>` — the edges back to root, each linking to its evidence page, P4). Truncation/warning banner surfaced (never silent, A21); empty = "truthful empty result, not a failure" (honest-absence). "Impact analysis" entry added to node detail. **WebGL graph canvas deferred to ledger** (optional enhancement; the list is the source of truth). `next build` green (8 routes); `pnpm run check` green. |
| G4.4 | Ask AI surface (SSE chat, citations/confidence) + ⌘K search + 4 UI states | ✅ | 09 §5.5, 10 | **`/ask`** — client chat streaming the G3.5 SSE `/ai/conversations/:id/messages` endpoint (`streamAsk` parses the SSE frames off a fetch reader — EventSource is GET-only). Tokens append live; **citations** render as numbered chips linking to `/explore/[id]` or `/explore/edge/[id]` (P4); **ConfidenceBadge** on each answer; caveats surfaced. The **four designed states** (docs/09 §7): empty (example prompts) · streaming (typing dots + caret) · answered (cited + tiered) · **honest-absence** (`confidence: insufficient` → muted "I don't know", never a fabrication — US-11). **⌘K command palette** (`CommandPalette` + `CommandTrigger`, connected by a window event): keyboard-first (⌘K/Ctrl-K, ↑↓, ↵, Esc), debounced `/search`, results navigate into Explore. Client calls use the public Supabase token (RLS + GUC still enforce). `next build` green (all routes); `pnpm run check` green (17 tasks). |

**Exit (M2–M5):** cross-source cited edges; canonical questions answered correctly & honestly; explorable. ✅ **EPIC G COMPLETE** — G1 (inference) + G2 (read API) + G3 (AI engine) + G4 (Explore UI + Ask AI + ⌘K). The graph is queryable, explorable, and answerable — cited and confidence-tiered end to end. **Next: Phase P (P1 onboarding/UX polish → P2 hardening) → GA.** (Live data still pending connector creds — deferral ledger.)

---

## EPIC P — ✨ Polish — 🏗️ IN PROGRESS

> **Plan (user-chosen): seed demo data → P1 (onboarding/UX) → P2 (hardening); skip live-creds for now.**
> Demo data is seeded (`packages/ingest/scripts/seed-demo.ts` — Shopyard estate, org *Acme Verify*). UI overhauled: mono black&white shadcn theme + collapsible sidebar + `(app)` route group (persistent shell + `loading.tsx`) + `/settings` + `/dashboard`. shadcn CLI wired (`apps/web/components.json`).

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| P1.1 | Dashboard metrics (`GET /overview` + rich dashboard) | ✅ | 09 §5.2 | node/edge counts, kinds, certainty mix, sources, last-sync |
| P1.2 | **Onboarding wizard** (empty-state: choose source + instructions + **"Load sample data"** for TTFI<30min) | ✅ | 09 §8 | Empty graph → the onboarding first-run experience. **Backend:** shared `seedDemoData` extracted into `@atlas/ingest` (`demo-estate.ts`) — the Shopyard estate + org-scoped (atlas_app/`withOrgScope`, no admin) seeder driving the real MockConnector→runStagedSync→runInference; the CLI `seed:demo` script + new gated `POST /demo/seed` API both call it (DRY). `DemoModule` (Admin-only, `TenantScopeGuard`) gates to empty orgs (409 if a real source exists), idempotent by URN. **Frontend:** `Onboarding` — "Load sample data" primary path (client button → `/demo/seed` → `router.refresh`, admin-gated via `shell.role`) + AWS/GitHub setup instructions (real least-privilege IAM policy + External-ID steps / App read-scopes, copy-ready). **Repeatable UI system** (per user): extracted reusable primitives — `patterns/EmptyState`, `patterns/Steps`, `ui/CodeBlock` (+ shadcn `tabs` via CLI) — so onboarding composes the system, not bespoke markup. **Verified live:** shared seeder end-to-end on live DB (36 nodes, 29 observed + 12 inferred edges, sync succeeded); API boots with `DemoModule`, `POST /demo/seed` mapped + 401-guarded; web dev serves (/login 200, /dashboard 307). All `pnpm run check` gates green (17 tasks). Also added app-router `not-found.tsx` + `global-error.tsx` (designed error states). |
| — | **`next build` fix** (env, not a task) | ✅ | 17 | Root-caused the repo-wide `next build` failure: `.env` sets `NODE_ENV=development`; sourcing it before `next build` made React use its **dev** SSG renderer → null dispatcher → `useContext` null on prerender → misleading `<Html> should not be imported` on `/404`/`/500`. **Not** a version/our-code bug (a zero-hook component reproduces it). Fix: `apps/web` `build` script → `NODE_ENV=production next build` (overrides the ambient dev value). Build now green (11 routes) with the dev `.env` still sourced. See [[reference-local-dev-runbook]]. |
| P1.3 | State polish (empty/loading/error/honest-absence consistency) | 🔵 **NEXT** | 09 §7 | Reuse the new `EmptyState`/`Steps`/`CodeBlock` primitives across Explore/Ask/dashboard for consistency. |
| P2 | Hardening: load/perf (NFR), security pass, observability, DR drill, mutation tests + adversarial QA agent in CI | 📋 | 14, 13, 17 | MVP exit checklist |

**Exit (M6–M7):** NFR targets met; security suite green; DR drill passes.

---

## EPIC GA — 🚀 Launch — 📋 TODO

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| GA | Closed beta → fix activation → security package → GA | 📋 | 18, 13, 15 | design partners validate trust ≥90% |

---

## 🧭 Deferred during MVP build (IN-SCOPE — revisit, most in Sprint P2 / deploy)

> These are **intentional deferrals inside completed sprints**, not order violations. The binding dependency order (F1→F2→I1∥I2→G1→G2→G3∥G4→P1→P2→GA, A60) is intact. Tracked here so nothing is silently lost.

| Item | From | Where it lands | Doc |
|---|---|---|---|
| **Worker process + Redis/BullMQ** — queue/worker code exists but no running worker; API's `InMemoryQueue` only buffers, so enqueued syncs don't execute yet | F2.5 | deploy wiring (before live sync) | 02 §5, 17 |
| **Scheduler** — per-connection incremental/nightly cadence, leader-elected singleton, periodic `health()` re-check (FR-1.9), GitHub periodic reconcile (DD-2) | I1/I2 | with worker process | 06 §11, 07 §5, 02 §5.4 |
| **Live connector verification** — AWS read-only IAM role + GitHub App install | I1/I2 | when customer creds exist | 06 §2, 07 §2, 13 |
| **AWS additive services** — DynamoDB, ElastiCache, API Gateway, IAM-policy | I1.3 | additive anytime | 06 §9 |
| **GitHub gaps** — webhook API ingress route (verifier built, endpoint not wired); team→member resolution (US-10); IaC (TF/CFN) ref parsing; more manifest ecosystems (have npm/pypi/go); multi-branch structural parse | I2 | P1/P2 + additive | 07 §5/§7 |
| **OpenSearch hybrid search** — search runs behind `SearchProvider` with a Postgres (pg_trgm keyword) impl; the OpenSearch BM25+kNN driver + embedding/index pipeline + semantic ranking are the deploy target | G2.4 | deploy / P2 | 11 §3–§8 |
| **audit_events table + audit log** — RBAC/lifecycle built, but the append-only audit table isn't created | F1 | P2 (security pass) | 13 §8, 04 |
| **Connection purge-on-delete** — delete soft-deletes; source's nodes not purged | F2.8 | P1/P2 | 03, 04 |
| **Observability baseline** — structured logging + correlation ids (currently basic Nest logger) | F1.3 | P2 | 02 §9.4 |
| **Adversarial QA agent + API-contract/E2E/load/mutation CI stages** — CI is format/lint/typecheck/test + PG-RLS integration only today | F1.2 | P2 (+ when PRs start) | 14 §7–13 |
| **WebGL graph canvas** — the Explore impact/neighbor view is **list-first** (a11y source of truth); the interactive force-directed canvas is an optional visual enhancement on top | G4.3 | P1/P2 | 09 §5.4 |

---

## ⏸️ Backlog (Phase-1+ — deferred, from roadmap `docs/15` §8)
- v1.1 Trust & Depth: richer inference, culprit-PR ranking (US-6), saved views/deep-links
- v1.2 Enterprise on-ramp: multi-account AWS, **domain auto-join (`hd`)**, real-time CloudTrail ingestion
- v1.3 Enterprise security: SSO/SAML+SCIM, custom RBAC, SOC 2 Type II, data residency
- v2.0 Breadth: GCP/Azure, **Bitbucket (`07b`)/GitLab**, Datadog/PagerDuty
- v2.x Scale: dedicated graph DB (when `05` DD-3 trigger met), partitioning
- v3.0 Platform: proactive alerts, incident root-cause, **MCP/public API**, connector marketplace

---

## 📜 Activity Log (append newest at top; one line per work session/event)

| Date | Who | What |
|---|---|---|
| 2026-07-02 | architect | **P1.2 DONE — onboarding wizard + gated `POST /demo/seed` + a reusable UI system.** Empty graph now lands on a real first-run experience instead of a "go to settings" stub. (1) **Shared seeder** — extracted the Shopyard estate + seeding into `@atlas/ingest` `demo-estate.ts` (`seedDemoData`, org-scoped via `withOrgScope`/atlas_app — no admin needed, so the API can call it directly); the CLI `seed:demo` script and the new API endpoint both call the *same* function (DRY). Verified live: 36 nodes, 29 observed + 12 inferred edges, sync succeeded. (2) **API** — `DemoModule`: `POST /demo/seed` (Admin-only, `TenantScopeGuard`), gated to empty orgs (409 if a real source is connected), idempotent by URN. API boots with the module; route mapped + 401-guarded. (3) **Frontend** — `Onboarding` component: primary **"Load sample data"** (client → `/demo/seed` → `router.refresh`, admin-gated via new `shell.role`) + AWS/GitHub tabbed setup instructions with copy-ready least-privilege IAM policy / App read-scopes. Wired into the dashboard empty state. (4) **Repeatable UI system** (user asked for structured, reusable UI): extracted `patterns/EmptyState`, `patterns/Steps`, `ui/CodeBlock` primitives + pulled shadcn `tabs` via CLI — onboarding composes the system. (5) Added app-router `not-found.tsx` + `global-error.tsx` (designed error states, docs/09 §7). All `pnpm run check` gates green (17). **Env issue found:** `next build` is broken repo-wide (next 15.5.19 + react 19.2.7 drift vs the 15.1.x green builds used → pages-router error-page prerender throws `<Html>`); affects the committed baseline, not P1.2. Servers restarted (API :4290, web :4291). See [[reference-local-dev-runbook]]. **Next: P1.3 (state polish).** |
| 2026-07-02 | architect | **Phase P started (plan: seed→P1→P2, skip live-creds). Demo data + mono UI overhaul + P1.1.** (1) **Demo seed** — `packages/ingest/scripts/seed-demo.ts` drives the real MockConnector→runStagedSync→runInference to populate an org with a "Shopyard" estate (36 nodes, 29 observed + 12 inferred edges, atlas.service nodes); idempotent; `SEED_EMAIL`/`SEED_ORG_ID` (seeded *Acme Verify*). (2) **UI** — shadcn/ui, **monochrome black&white** theme (user directive) + semantic **status colors only** (success/warning/danger); replaced top-bar with a **collapsible sidebar** in an **`(app)` route group** (persistent `layout.tsx` + `loading.tsx` → instant nav); dashboard→`/dashboard`, `/` redirects/onboarding; added **`/settings`**; `requireShell()` React-`cache()`d. **shadcn CLI wired** (`apps/web/components.json`); badge re-pulled from registry; see [[feedback-shadcn-ui-workflow]]. (3) **P1.1 ✅** — `GET /overview` + rich dashboard (counts/kinds/certainty-mix/sources). Run notes: [[reference-local-dev-runbook]]. shadcn MCP init'd → `apps/web/.mcp.json` (move to repo root + restart to load). **Next: P1.2 onboarding wizard.** |
| 2026-07-01 | architect | **UI refresh — shadcn light theme + collapsible sidebar (design pass over G4).** Adopted the full shadcn/ui token system in **light mode** (background/card/primary/muted/accent/destructive/border/ring/sidebar), with the **certainty visual language** kept as first-class colors (observed/inferred-high/inferred-low/stale, now light-tuned with tinted badges). Vendored the canonical shadcn components (button, card, badge, input, separator, sheet, tooltip, dropdown-menu, skeleton, and the **collapsible sidebar** + `use-mobile`). Replaced the top-bar `AppShell` with a real **sidebar layout**: `AppSidebar` (brand + nav w/ active state + user dropdown w/ sign-out) beside a `SidebarInset` with a sticky header (sidebar toggle + ⌘K search box). Restyled login + onboarding + filters as shadcn blocks; migrated every page off the old dark tokens. Deps added: cva, @radix-ui/{slot,separator,dialog,tooltip,dropdown-menu}, tailwindcss-animate. `pnpm run check` green (17); `next build` green; CI green. Also fixed the local-dev 500 (stale 19h servers + `.next` deleted mid-build) — see [[reference-local-dev-runbook]]. |
| 2026-07-01 | architect | **G4.4 DONE — Ask AI surface + ⌘K; Sprint G4 & EPIC G COMPLETE.** **`/ask`** is a client chat that streams the G3.5 SSE endpoint (`streamAsk` parses SSE frames off a `fetch` body reader — EventSource is GET-only, and the endpoint is POST). Tokens append live; **citations** stream in as numbered chips linking to `/explore/[id]` / `/explore/edge/[id]` (P4 end to end — the AI's sources are the same nodes/edges Explore shows); a **ConfidenceBadge** tiers each answer and caveats render inline. The **four designed states** are all realized (docs/09 §7): *empty* (example prompts you can click), *streaming* (typing dots → caret), *answered* (cited + tiered), and **honest-absence** (`confidence: insufficient` → a muted "I don't know", never a fabrication — US-11, the AI's grounding gate surfaced in the UI). **⌘K command palette** (`CommandPalette` + a top-bar `CommandTrigger`, connected by a `window` custom event): keyboard-first (⌘K/Ctrl-K to open, ↑↓ to move, ↵ to open, Esc to close), debounced `/search`, results navigate into Explore — the *same* hybrid search seam the AI uses for entity resolution (docs/11 DD-1). Interactive client calls use the public Supabase access token (RLS + our GUC isolation still enforce access; nothing privileged is exposed). Needed `orgId` threaded through `AppShell`. Clean `next build` green (all routes: /ask, /explore*, /login, /); `pnpm run check` green (17 tasks). **EPIC G done — graph is queryable + explorable + answerable, cited and confidence-tiered end to end. Next: Phase P (P1 onboarding/UX → P2 hardening) → GA.** |
| 2026-07-01 | architect | **G4.3 DONE — impact analysis (blast-radius + dependencies), list-first with why-chains.** New route **`/explore/[id]/impact`** renders both G2 recursive-CTE traversals side by side: **Blast radius** (downstream — what breaks if this resource changes/fails, US-4) and **Dependencies** (upstream — what it needs). The reusable `ImpactPanel` is **list-first for a11y** (docs/09 §5.4 — the list is the source of truth; the WebGL graph canvas is an *optional* enhancement, deferred to the ledger): impacted nodes are grouped by **hop distance**, each showing its **pathConfidence** (the weakest edge on the path — P3, weakest-link) and an expandable **why-chain** built from native `<details>` (no client JS) that lists the edges back to the root, each linking to its `/explore/edge/[id]` evidence page (P4 provenance end to end). Truncation and traversal warnings are surfaced in a banner (never silently dropped, A21); a zero result reads as "a truthful empty result, not a failure" (honest-absence, consistent with the AI layer). Added an "Impact analysis" action to the node-detail header. `next build` green (8 routes); `pnpm run check` green (17 tasks). **Next: G4.4** (Ask AI surface + ⌘K → Sprint G4 & Phase G complete). |
| 2026-07-01 | architect | **G4.2 DONE — Explore surface (nodes list + node detail + provenance + edge "why?").** All server-rendered (secrets stay server-side, NFR-22). Added `requireShell()` — a session+org guard for org-scoped pages (no session → /login; no org → home create-org) — and `graph-types.ts` (web mirror of the read-API shapes). **`/explore`**: a GET-form filter bar (q · kind · status · confidence) so the URL is the source of truth (shareable, back-button, no-JS), a nodes table showing **certainty (ConfidenceBadge) + freshness (FreshnessTag) per row**, and keyset "Next page" carrying filters. **`/explore/[id]`**: node detail — header with confidence+freshness, attributes, a **Provenance** card (source / observed / sync-run / raw-snapshot, or an honest "derived, not directly observed" when none), and **Connections** split into incoming/outgoing with each edge's type + confidence + origin. **`/explore/edge/[id]`**: the **"why?"** provenance page (P4) — origin (observed vs inferred), the rule that derived it, the evidence JSON, and the raw source. Cross-tenant or missing ids render a graceful not-found (404-as-not-found, R8 — never leaks existence). `next build` green (7 routes); `pnpm run check` green (17 tasks). **Next: G4.3** (blast-radius panel + neighbor subgraph, list-first for a11y). |
| 2026-07-01 | architect | **G4.1 DONE — frontend foundation; login now lands on a real UI.** (Answers the user's "why no proper UI after login" — before this the web app was only the F1.5 login shell; the Explore UI is Sprint G4, now started.) Stood up the design system per docs/09 §3: Tailwind v3 + PostCSS, dark theme, and the **certainty visual language** as CSS-var tokens (`--observed`/`--inferred-high`/`--inferred-low`/`--stale`/`--danger` etc.) so trust is legible everywhere (P3/P4/trust-visible). Hand-vendored shadcn-style primitives (`Card`, `Badge`) + `cn()` (clsx+tailwind-merge); reusable certainty components `ConfidenceBadge`/`FreshnessTag`/`CitationLink`. `lib/api.ts`: `getSession` (Supabase server session → token/email) + `apiGet` (attaches `Authorization: Bearer` + `X-Atlas-Org`). `AppShell` (sticky TopBar: brand + org chip + nav Dashboard/Explore/Ask/Settings w/ lucide icons + email + sign-out; bounded content). Server-rendered `Dashboard` (docs/09 §5.2): connections list + recent-nodes preview (both with empty states). Rewrote `page.tsx`: no session → **Landing** (hero + Sign in); session but no membership → **create-org card**; authed+org → **AppShell + Dashboard**. `next build` green (5 routes); `pnpm run check` green (17 tasks). **Next: G4.2** (Explore nodes list + detail + provenance drawer + edges). |
| 2026-07-01 | architect | **G3.5 DONE — SSE /ai endpoints + conversation persistence + API wiring; Sprint G3 (AI engine) COMPLETE.** Added `answerQuestionStream` (SSE-shaped events: retrieval→token→citation→confidence→done). `AiModule` in the API: **`GraphRetrievalPort`** adapts the engine's RetrievalPort over the G2 GraphService + SearchProvider (the only seam between the NestJS-free `@atlas/ai` and the API; org-scoped, AE-7); the **LLM provider** is Claude when `ANTHROPIC_API_KEY` is set, else a dev `MockLLMProvider` (so retrieval/grounding/citation all work in dev/CI without a key). Migration **0013** adds `ai_conversations` + `ai_messages` (org-scoped RLS, R8). Endpoints (Member+): `POST /ai/conversations`, `GET /ai/conversations/:id`, `POST /ai/conversations/:id/messages` (manual SSE over the raw reply — bypasses the JSON envelope). `askStream` persists the user + assistant turns (assistant carries citations + overall confidence, so the transcript stays cited/tiered). 3 live-PG service tests (grounded+cited+persisted, honest-absence stream, cross-org 404); API boots clean with AiModule (/health 200). **G3 exit met** (canonical cited answers, honest-absence, eval green). **Next: G4 (Explore UI).** |
| 2026-07-01 | architect | **G3.4 DONE — answer pipeline (narrator + citation engine + confidence + eval).** `answerQuestion` orchestrates plan→retrieve→gate→narrate→deterministic post-process. If the gate fails it returns honest-absence **without ever invoking the LLM** (L1 — a wrong-context model can't fabricate). Narration streams from the LLMProvider at low temperature (L3). **Citation engine** (DD-5/L4): parses `[N#]/[E#]` markers, binds them to real node/edge ids + `provenanceUrl`, dedup + numbered by appearance; markers with no matching context fact are **dropped** (the model can't invent a source). **Uncited-claim detector** (L5): flags factual sentences (≥6 words) carrying no marker, excusing hedge/absence sentences — a hallucination is surfaced, not hidden. **Confidence scorer** (§5): overall = weakest cited tier (weakest-link, P3); caveats from the context's FRESHNESS notes. **AI eval set** (L7, docs/14 §11) over the MockLLMProvider — canonical (blast-radius answered, cited, `inferred-high`), honest-absence (unresolved entity + out-of-scope → refuse, `insufficient`, 0 citations), adversarial (a tempted fabrication is BLOCKED by the gate or CAUGHT by L5): escaped-hallucination rate 0 (<1% bar). 10 unit/eval tests; 37 in `@atlas/ai`. Gates green. **Next: G3.5** (SSE /ai endpoints + within-session memory + wire RetrievalPort adapter + ClaudeProvider into the API). |
| 2026-07-01 | architect | **G3.3 DONE — retrieval orchestration + context builder + grounding gate.** `orchestrate` executes the typed plan against the bounded, org-scoped RetrievalPort (retrieval-first — no LLM yet): blast_radius/dependents → inbound blastRadius; deploy_mapping → inbound DEPLOYS_TO edges; culprit → inbound CHANGED_BY + timeline window; timeline → window query; lookup/architecture → both-direction edges. `buildContext` (DD-3, the top hallucination defense): assembles the compact CONTEXT block where **every fact carries a stable `N#`/`E#` marker + `cite:<id>` + confidence + evidence**, with a FRESHNESS section for truncation/warnings, and returns the `cites` map the Citation Engine binds against (DD-5); untrusted values stay delimited data. `groundingGate` (DD-4, **deterministic — not the LLM's judgment**): out-of-scope or unresolved-entity → a structured honest-absence reason (US-11, a refusal is success per P3); a resolved entity with an empty traversal is still grounded ("nothing depends on X" is truthful). 7 unit tests (orchestrate paths, marker/cite tagging, gate pass/refuse). Gates green. **Next: G3.4** (narrator via LLMProvider + deterministic citation engine + confidence scorer + uncited-claim detection + AI eval set in CI). |
| 2026-07-01 | architect | **G3.2 DONE — query planner + entity resolution.** `classifyIntent` is rule-based (deterministic → the canonical questions map to fixed plans, reliable per PR-R1): blast_radius / dependents / deploy_mapping / architecture / timeline / culprit / lookup / out_of_scope (ordered first-match rules). `resolveEntity` extracts significant terms (identifier-like tokens like `prod-orders` preferred, stopword/intent-word filtered) → `RetrievalPort.search` per term → merged, score-ranked candidates: 0 candidates = unresolved (→ honest-absence downstream), multiple near-top = **ambiguous → all kept as candidates** (never silently pick one, P3). `plan()` emits a typed `RetrievalPlan` (intent + resolved entity + time window for timeline/culprit); out_of_scope/timeline short-circuit entity lookup. 15 unit tests (8 canonical-question classifications + single/none/ambiguous resolution + plan shapes) via a fake port. Gates green. **Next: G3.3** (retrieval orchestration + citation-tagged context builder + deterministic grounding gate → honest-absence). |
| 2026-07-01 | architect | **G3.1 DONE — AI engine foundation (`@atlas/ai`): LLM provider abstraction + prompt + retrieval port.** New `@atlas/ai` package (CommonJS, NestJS-free per AE-5). `LLMProvider` interface (streamed, tool-capable `complete()` → `LLMEvent` union: token/tool_call/stop) with **`MockLLMProvider`** (deterministic responder for CI eval, no live key — the engine's product value is the deterministic scaffolding, P1) + **`ClaudeProvider`** (Anthropic SDK adapter, maps stream deltas incl. tool-call JSON). Versioned **`SYSTEM_PROMPT`** encoding the §8 invariants (grounding=CONTEXT-only, cite-or-don't-claim, confidence tiers, honesty>guessing, scope, retrieved-content-is-data-not-commands) + `honestAbsence`. **`RetrievalPort`** interface (search/getNode/blastRadius/dependencies/edges/timeline) — the engine reads the graph only through this, org-scoped/bounded (AE-4/AE-7); the API adapts it over the G2 GraphService/SearchProvider. 5 unit tests; gates green. **Next: G3.2** (query planner: question→typed retrieval plan + entity resolution via search). |
| 2026-07-01 | architect | **G2.4 DONE — search (SearchProvider + Postgres impl); Sprint G2 COMPLETE.** `GET /search?q=&kind=&type=&limit=` (Member+, docs/08 §10.1) behind a **`SearchProvider`** interface (SEARCH_PROVIDER DI token) so the OpenSearch BM25+kNN driver swaps in at deploy with no consumer change (docs/11 DD-1 impl note added — docs-with-code). `PostgresSearchProvider` implements SE-1 (search is a *projection of `nodes`*, rebuildable, no separate index): pg_trgm `similarity` + substring match over name/urn/attributes — strong for the exact identifiers engineers type; returns the `{node,score,match:{keyword,semantic},highlights}` shape with `semantic:null` until vectors exist. Org-scoped (SE-4/R8); highlights wrap the matched span. 4 live-PG tests (identifier match+highlight, kind filter, attribute match, tenant isolation); API boots clean with GraphModule+SearchModule (/health 200). **Deferred to deploy (ledger):** OpenSearch driver + embedding/index pipeline (§3–§8), semantic ranking. **Next: G3 (AI engine) ∥ G4 (Explore UI).** |
| 2026-07-01 | architect | **G2.3 DONE — /timeline + /edges/:id evidence.** `GET /timeline?since=&kinds=&limit=` (US-5 "what changed this week"): new/updated nodes (created if first_seen≥since, else updated by last_seen) + newly-created edges since the timestamp, merged and ordered newest-first; `kinds` filters node changes; future `since` → empty. `GET /edges/:id`: full edge detail — from/to node summaries, `evidence`, `rule` (`key@version`), `provenance` (source/observedAt) + raw-snapshot ref for P4 click-through. Both Member+ and org-scoped (cross-tenant/malformed → 404). `TimelineQuerySchema` (z.coerce.date). 9 live-PG GraphService tests (added edge-detail + tenant-isolation, timeline window + kind filter + future-since). Gates green. **Next: G2.4** (search: SearchProvider abstraction + Postgres hybrid; OpenSearch driver deferred to deploy). |
| 2026-07-01 | architect | **G2.2 DONE — blast-radius + dependencies traversals (the signature endpoints).** `GET /nodes/:id/blast-radius` (inbound impact closure — "what breaks if this is deleted", US-4) and `/nodes/:id/dependencies` (outbound — "what this depends on", US-9), sharing a bounded recursive-CTE `traverse`. Walks the impact-bearing edge set (CONNECTS_TO/DEPENDS_ON/ROUTES_TO/STORES_IN/DEPLOYS_TO, overridable via `edgeTypes`); **cycle-guarded** (edge-path array `<> ALL`), depth ≤6 + nodeBudget ≤500 **clamped with `warnings`** (never unbounded, A21). Each impacted node returns `distance`, the **`via` why-chain** (each edge with type/confidence/evidence/rule/provenanceId, for cited AI answers, P4) and **`pathConfidence` = the weakest edge on the path** (docs/05 §8); `minConfidence` prunes low-confidence paths (the P3 high-confidence-only view). 7 live-PG `GraphService` tests: 2-hop chain, weakest-edge pathConfidence, minConfidence exclusion, outbound deps, depth-clamp warning. Gates green. **Next: G2.3** (/timeline + /edges/:id evidence). |
| 2026-07-01 | architect | **G2.1 DONE — graph read API (nodes list/detail/edges/neighbors + cursor pagination).** New `GraphModule` in the API (Member+ via TenantScopeGuard/RolesGuard): `GET /nodes` (filters: kind/region/status/confidence/`q` name-ILIKE; **keyset cursor** over (last_seen desc, id desc), opaque base64url, stable under concurrent crawl writes — DD-3), `GET /nodes/:id` (detail + latest raw-snapshot provenance for P4 click-through), `GET /nodes/:id/edges` (in/out/both + type/confidence filters), `GET /nodes/:id/neighbors` (depth-1 subgraph, node-budgeted). All org-scoped via withOrgScope (RLS; malformed/cross-tenant id → 404). Fixed `parseBody` to infer the schema OUTPUT type so `.default()`/`z.coerce` query schemas type correctly; added `invalid_cursor` error code. 6 live-PG `GraphService` tests (list+filter+paginate, detail+provenance, 404s, edge direction, neighbor subgraph, tenant isolation); API vitest gains DB timeouts. Gates green. **Next: G2.2** (blast-radius/dependencies recursive-CTE traversals with why-chains + pathConfidence). |
| 2026-07-01 | architect | **G1.4 DONE — determinism + precision + infer-stage wiring; Sprint G1 (inference) COMPLETE.** Pure `evaluateAll` (mirrors the engine's dependency-ordered fold, no DB) drives the quality tests. **Determinism golden file**: the docs/05 §9 worked example reproduces an exact edge set (CONNECTS_TO ecs→rds via R2, CONNECTS_TO lambda→rds via R3, DEPLOYS_TO repo→ecs via R1, IMPLEMENTS + RUNS + the derived atlas.service via R4, OWNED_BY svc→team via R5, CHANGED_BY svc→pr via R6); re-run yields an identical plan (IE-1/A19); every edge is tiered + carries evidence (P4/DD-4). **Precision-sampling harness (≥95%)**: labeled fixtures incl. negatives (R2 wrong-port, R8 wildcard-IAM) → **100% precision, zero false positives**. **Infer stage wired**: the sync worker runs `runInference(ALL_RULES)` after every non-failed sync (docs/05 §6.1); an inference error is logged but never fails the sync (P7, converges next run). Proven sync→infer end-to-end (a mock sync emitting a workflow-deploy signal yields a persisted DEPLOYS_TO). `@atlas/ingest` now depends on `@atlas/inference`; API boots clean with the transitive dep. 25 inference tests + 12 ingest tests. **G1 exit criterion MET.** **Next: G2** (read API + traversals/blast-radius + OpenSearch hybrid search). |
| 2026-07-01 | architect | **G1.3 DONE — R2/R3/R8 data-dependency edges (all 8 MVP rules now live).** **R3 config_ref_connects** (inferred-high): a Lambda/ECS env-var value referencing a crawled RDS endpoint host / S3 bucket / DynamoDB table → CONNECTS_TO (rds) or STORES_IN (s3/ddb); precise matching (exact / `s3://` / ARN-suffix) to avoid substring noise; evidence stores the env KEY + matched resource, never the raw value (may hold secrets). **R2 sg_correlation_connects** (inferred-high): resolves SG reachability — a compute node's SG allowed (ingress) into a datastore's SG on the engine port (RDS `endpointPort`/engine-derived) with a shared VPC → CONNECTS_TO; built from PROTECTS observed edges + `aws.sg.rules` signals + groupRef→SG-node mapping. **R8 iam_access_connects** (inferred-LOW, P3): observed ASSUMES_ROLE + an `aws.iam.policy_statements` Allow with a SPECIFIC resource ARN (wildcards ignored) → CONNECTS_TO/STORES_IN; low because permission ≠ use. Shared `refs.ts` (engine→port map, ARN→node resolution). 6 pure tests; ALL_RULES now R1–R8; existing live-DB integration unaffected (22 inference tests total). Gates green. **Next: G1.4** (determinism golden files + precision-sampling ≥95% + wire the infer stage post-sync). |
| 2026-07-01 | architect | **G1.2 DONE — R4 (atlas.service) + R5-propagation + R6 (CHANGED_BY).** Extended the engine so rules emit **derived nodes** (upserted first → their edges resolve in-scope) and read EARLIER rules' inferred edges in one dependency-ordered pass (R1→R4→R5/R6, accumulator folds each rule's output back into the input). **R4 service_derivation**: from each high-confidence `DEPLOYS_TO(repo→runtime)`, derive an `atlas.service` node (`atlas:<org-slug>:service:<repo-name>`) + `IMPLEMENTS`(repo→svc) + `RUNS`(runtime→svc); high-only (DD-1: a service is only as trustworthy as its deploy link); one service node per repo with RUNS×N for multiple runtimes. **R5 ownership_propagation**: observed repo `OWNED_BY` + `IMPLEMENTS` → service `OWNED_BY` (inferred-high). **R6 pr_changes_service**: a merged-PR `github.pr.files` signal + repo `IMPLEMENTS` → service `CHANGED_BY` pr (high if repo implements exactly one service, low for monorepo — honest culprit attribution, P3/US-6). `nodes.connection_id` made **nullable** (migration 0012 + `docs/04` §5.3) since derived nodes have no single connection; connection-scoped stale-reconcile naturally skips them. 16 inference tests (12 pure + 4 live-DB, incl. the full **R1→R4→R5→R6 worked-example** from docs/05 §9 reproduced end-to-end); ingest still green. Gates green. **Next: G1.3** (R2 SG-correlation, R3 env-refs, R8 IAM-access → CONNECTS_TO/STORES_IN). |
| 2026-07-01 | architect | **G1.1 DONE — inference engine core + signals persistence + R1 (Phase G started).** New `signals` table (migration 0010, org-scoped RLS; `docs/04` §5.4b added — docs-with-code) persisted during sync: the runner now calls `extractSignals`→upsert by (org, subject_urn, kind) (+`signals` stat). New **`@atlas/inference`** package: pure versioned `Rule` contract (deterministic, DD-2/P9) → candidate edges; the engine loads nodes+signals+observed-edges, resolves URNs→ids, **upserts inferred edges with provenance (rule id + evidence, P4)**, and **retires (status='retired', not delete — P4) any active inferred edge a rule no longer produces** → **convergent (IE-4: a no-change re-run writes nothing)**. `inference_rules` seeded (migration 0011: R1–R8 metadata, keyed for edge FK). **R1 `repo_deploys_to_runtime` → DEPLOYS_TO**: reads `github.workflow.deploy` signals, resolves targets vs AWS runtime nodes — exact ARN or a unique cluster/service (or function) name → `inferred-high`; multiple candidates → several `inferred-low`, never one wrong high (P3). 11 tests: 4 pure R1 tiering + 3 live-DB engine (derive+provenance, converge zero-churn, retire-on-evidence-loss); ingest still green (11). Gates green. **Next: G1.2** (R4 atlas.service + IMPLEMENTS/RUNS, R6 CHANGED_BY, R5 ownership propagation). |
| 2026-07-01 | architect | **I2.4 DONE — GitHub crawler CODE-COMPLETE (live client + crawl + webhook + wiring); Phase I complete.** `FetchGithubClient`: bearer install-token, `Link` pagination (+ `itemsKey` for `/installation/repositories`), header-aware rate-limit/retry (primary `x-ratelimit-reset` wait, secondary `retry-after`, 5xx backoff — all bounded, injectable sleep/clock). `crawlRepo` fetches repo detail + CODEOWNERS/manifests/workflows + recent PRs and yields repo/workflow/PR nodes **plus** the edge-target team/user/package nodes so OWNED_BY/DEPENDS_ON_PKG resolve in-scope; `plan()` = one scope per installed repo (bounded, resumable); one installation token+client cached per run (mirrors AWS AssumeRole). **Webhook HMAC verify** (timing-safe sha256, GHR-8/docs/13 §5) + event→descriptor mapping. **Wired into API:** real `GithubConnector` registered for "github" (factory injects App token provider + Secrets-Broker accessor); API boots clean with BOTH connectors (DI verified, /health 200); enqueue-on-verify already generic. 42 connector-github unit tests (webhook, client pagination + rate-limit via fetch mock, crawl + connector plan/discover/fetchDetail via fake client). **Open:** live-install verification (needs App creds) + webhook API route/worker (deploy). **Next: G1 (inference engine + edges)** — consumes the emitted signals to derive DEPLOYS_TO/CONNECTS_TO/etc. (docs/05 R1–R8). |
| 2026-07-01 | architect | **I2.3 DONE — GitHub pure parsers + entity modules (golden fixtures).** Pure parsers: **CODEOWNERS** (rules, comment/blank skip, `@org/team` vs `@user` vs email classification, distinct-owner dedup), **dependency manifests** (npm `package.json` deps+devDeps, Python `requirements.txt`, Go `go.mod` require block — direct deps only, other ecosystems additive, GHR-7 fail-soft), **workflow deploys** (`yaml`-parsed: AWS actions, ECS `with: {cluster,service}`, `run:` `--cluster/--service/--function-name` flags, ARNs anywhere). 6 entity modules dispatched by kind: **repository** (node + OWNED_BY from CODEOWNERS + DEPENDS_ON_PKG from manifests), **pull_request** (node + changed-files signal for R6), **workflow** (node + CONTAINS(repo→workflow) + deploy signal for R1), **team/user/package** (leaf nodes so the edges resolve). **DEPLOYS_TO stays inferred (R1/G1) — the connector emits deploy targets as signals, never a possibly-wrong observed edge (P3).** Connector now dispatches normalize/observedEdges/extractSignals; plan/discover/fetchDetail stubbed (I2.4). 31 unit tests (parsers + module golden fixtures). Gates green. **Next: I2.4** (live Octokit discoverers + rate-limit/retry + webhook HMAC ingress + wire into runner/registry/API). |
| 2026-07-01 | architect | **I2.2 DONE — GitHub App auth + verify/health + permission detection.** `GithubAppTokenProvider` signs a short-lived App JWT (RS256; iat backdated 60s for skew, exp +9min < GitHub's 10-min cap) using Node `createPrivateKey` (accepts the PKCS#1 key GitHub issues AND PKCS#8), then exchanges it for an **installation access token** (≤1h, in-memory, read-only — the GitHub analogue of AWS AssumeRole). App private key comes from the Secrets Broker (C1, never config). Permission detection is **direct** — the token response reports granted `permissions`, so verify compares to the required read set (metadata/contents/pull_requests/actions/members) and returns `degraded` with the missing `key:read` list (no probing needed, unlike AWS); malformed config/key or a failed exchange → `error`. `InstallationTokenProvider` interface keeps HTTP/JWT out of the connector for testability. 17 unit tests (config parse, permission logic, a real RS256 sign+verify round-trip via a generated keypair, verify decision matrix with a fake provider). Crawl stages stubbed (I2.3/I2.4). Gates green. **Next: I2.3** (pure parsers + normalize/extractSignals/observedEdges: workflows→deploy signals, CODEOWNERS→OWNED_BY, manifests→DEPENDS_ON_PKG, golden fixtures). |
| 2026-07-01 | architect | **I2.1 DONE — GitHub connector foundation (I2 started).** New `@atlas/connector-github` package (CommonJS, requireable by the CJS API). GitHub URN builders matching docs/05 §2.2: repo `github:<owner>/<repo>`, PR `…:pr:<n>`, workflow `…:workflow:<path>`, team `github:<owner>:team:<slug>`, user `github:user:<login>`, package `external:<ecosystem>:package:<name>` (I own the user/workflow/package schemes; grammar owned by docs/05). Case-significant keys (owner/repo/pkg name) preserved; login/ecosystem lowercased. `GITHUB_NODE_KINDS` catalog (5 github kinds + external.package). Migration **0009** seeds `external.package` (provider `external`, category `dependency`) — the DEPENDS_ON_PKG target (docs/07 §7.3); applied to live DB. 6 unit tests; gates green. **Next: I2.2** (GitHub App installation-token auth + verify/health + permission detection → degraded). |
| 2026-07-01 | architect | **I1.4 DONE — AWS crawler CODE-COMPLETE (live discoverers + resilience + wiring).** Added the live-crawl layer: a `Discoverer` interface + **14 discoverers** (EC2/VPC/Subnet/SG share one EC2 client; Lambda, ECS cluster/service/taskdef, ECR, ELB with target-group/health enrichment, Route53 manual record-paging, RDS, S3 multi-call config+tags, IAM inline-policy flattening) using SDK paginators + adaptive retry. Connector `discover`/`fetchDetail` collapse list+detail via a per-run payload cache; **one AssumeRole per run**. Resilience: `withRetry` (exp backoff + full jitter on throttling/transient; access-denied/not-found/fatal pass straight through) + `classifyAwsError` (docs/06 §7.3). Permission probes now **derived from discoverers** so verify does real detection. **Wired into the API:** ConnectorRegistry holds the real `AwsConnector` for "aws" (factory injects STS provider + Secrets-Broker accessor + probes); **enqueue-on-verify** creates an onboarding `sync_runs` row + `enqueueSync` (BR-SYNC-1 one-in-flight, 23505 swallowed); `JobQueue` = in-memory dev impl (BullMQ + worker process at deploy). **Incremental hash-diff** in the runner: unchanged content hash → skip re-snapshot/provenance + new `unchanged` stat (docs/06 §6). API boots clean (DI verified, /health 200); 54 connector-aws unit tests (aws-sdk-client-mock for EC2/Lambda/S3 + retry) + 11 ingest integration on live DB (incl. new incremental test). **Open: live-account verification** (needs a customer read-only role — no creds yet) + additive services (DynamoDB/ElastiCache/APIGW/IAM-policy). **Next: I2 (GitHub crawler).** |
| 2026-07-01 | architect | **I1.3 DONE — AWS service modules (pure normalize/observedEdges/extractSignals, golden fixtures).** Added a per-service `ServiceModule` layer the connector dispatches by node kind (additive per docs/06 §9). 14 modules — VPC, Subnet, SecurityGroup, EC2, Lambda, ECS cluster/service/taskdef, ECR, ELB, Route53, RDS, S3, IAM-role — covering **every observed edge type** in docs/05 §4: CONTAINS (vpc→subnet, cluster→service, subnet→ec2), PROTECTS (sg→ec2/lambda/rds/elb), ASSUMES_ROLE (ec2/lambda/taskdef→role), USES_IMAGE (taskdef→ecr, ECR-image parsing skips non-ECR), ROUTES_TO (elb→instance targets), TRIGGERS (s3→lambda). Signals emitted for inference: SG rules (R2), Lambda/ECS env (R3), RDS endpoint (R3), IAM policy statements (R8), ECS target-groups + Route53 alias (routing). Route53→ALB intentionally a **signal, not an observed edge** (alias DNS doesn't deterministically resolve to one ELB → P3, a missing edge beats a wrong one). Deterministic URNs via `awsUrn`; `plan()` fans region×service + global services once. Golden-fixture tests assert exact node URN/attrs + edge from/to/type + signal shape (100% observed-edge precision on fixtures). 44 unit tests. **Additive remaining (explicitly tracked, not dropped):** DynamoDB, ElastiCache, API Gateway, IAM-policy. **Next: I1.4** — live discover/fetchDetail (AWS SDK + pagination/retry/rate-limit), incremental hash-diff, and wiring into the runner/registry/API (enqueue-on-verify). |
| 2026-07-01 | architect | **I1.2 DONE — AWS credential provider + verify/health + permission detection.** `StsCredentialProvider` does `sts:AssumeRole` (Atlas's own identity → customer read-only role) with the per-connection **External ID** (confused-deputy defense, resolved from the Secrets Broker — C1, never in config) and `sessionName=atlas-{verify,health,sync}-<id>` so every action is attributable in the customer's CloudTrail; creds are ≤1h, in-memory only (A23/P8); account id parsed from the assumed-role ARN (URN scope). `parseAwsConfig` validates `roleArn` + a regions allow-list (A25). `verify`/`health` AssumeRole then probe each supported service → `AccessDenied` becomes a **`degraded`** connection listing the missing IAM actions (P3, FR-1.6, US-1); transient (non-deny) probe errors are NOT counted as missing; bad config / failed AssumeRole → `error` with actionable text. Permission-probe framework + `isAccessDenied` classification in place; concrete per-service probes attach in I1.3. 27 unit tests (config, verify decision matrix, STS via aws-sdk-client-mock). Crawl stages stubbed (throw, I1.3). Gates green. **Next: I1.3** (service discoverers + pure normalize/extractSignals/observedEdges, golden fixtures). |
| 2026-07-01 | architect | **I1.1 DONE — AWS connector foundation (Phase I started).** New `@atlas/connector-aws` package (CommonJS so the CJS NestJS API can `require` it). `awsUrn()` builds the deterministic, recomputable 5-segment URN (`aws:<region\|global>:<account>:<type>:<key>`) matching every documented pattern in docs/05 §2.2 — region in scope for multi-account safety, literal `global` for S3/Route53/IAM, natural key case-preserved (Lambda/DynamoDB names are case-significant). `AWS_NODE_KINDS` catalog = the 18 MVP kinds (docs/05 §3.1 / docs/06 §4) with URN type discriminator + region/global scope. Migration **0008_node_kinds_seed** seeds `node_kinds` global vocab (24 rows = 18 AWS + 5 GitHub + 1 `atlas.service`) — required before any node insert (FK `nodes.kind`). Idempotent (ON CONFLICT DO NOTHING). 9 unit tests; applied to live DB (counts verified). Gates green. **Next: I1.2** (STS AssumeRole + verify + degraded permission detection). |
| 2026-07-01 | architect | **Phase F COMPLETE — F2 (connector framework + secrets + queue) DONE.** F2.1 connections/sync_runs schema · F2.2 `@atlas/connector-sdk` (frozen Connector contract) · F2.3 graph persist schema (nodes/edges/provenance/raw_snapshots, composite-FK same-org edges, uq_edge NULLS NOT DISTINCT) · F2.4 `@atlas/ingest` staged sync runner + MockConnector (idempotent/resumable/BR-SYNC-2, exit proof) · F2.5 JobQueue (InMemory + BullMQ) + sync worker · F2.6 SecretBroker · F2.7 Supabase Storage snapshots (live round-trip) · F2.8 connection API (live create→verify→connected). New packages: `@atlas/connector-sdk`, `@atlas/ingest`. ~18 integration tests on live PG + Storage; CI green (check + integration jobs). **Next: I1 (AWS) ∥ I2 (GitHub) crawlers** implement the frozen SDK. |
| 2026-07-01 | architect | **F1.7 CI Postgres integration job DONE — F1 Foundation epic COMPLETE.** Added CI `integration` job (postgres:16 service → migrations as owner → grant atlas_app LOGIN via `setup-app-role.ts` → env-gated integration tests with two-role + RLS). Expanded `org-scope.test.ts`: organizations org-scope + fail-closed, invitations cross-org isolation, `app_user_memberships`/`app_invitation_by_token` resolvers, write-guard negative. 9 integration tests pass on live PG; skip in the no-DB job (21 unit tests green). Updated docs/14 ref + ci.yml header. **Next: F2** per docs/15. |
| 2026-07-01 | architect | **F1.6 Org / RBAC / memberships / invitations DONE — live-verified through the app.** Built `TenantScopeGuard` (active org + live role) + `@Roles`/`RolesGuard` (Owner>Admin>Member); orgs/members/invitations endpoints with BR-ORG-1 / BR-MEM-2/3 invariants; invitation accept by sha256-hashed capability token (never returned). Added `{data}`/`{error}` API envelopes (global interceptor + exception filter), zod validation, CORS. Migration 0005 (organizations org-scope RLS + `app_invitation_by_token` SECURITY DEFINER resolver). Synced docs/12 §5.2 + docs/08 §6. Frontend: create-org + org-management panel (members/invitations/invite). Verified live: real session → create org (Owner), invite (pending, hashed), members RLS-scoped read 200, last-Owner demote rejected (still Owner), cross-tenant 404. Unit tests RolesGuard+slug (api 15 tests). All gates green. |
| 2026-07-01 | architect | **F1.5 Supabase Auth DONE — live Google login verified end-to-end.** Rewrote `docs/12` §2–4 (v1.1, docs-before-code). Built NestJS auth (CoreModule env+atlas_app pool, `SupabaseJwtVerifier` via JWKS/ES256, `AuthGuard`, `UserMirrorService`, `MembershipService`, `GET /me`, claims unit tests) + Next.js Google sign-in (`@supabase/ssr` client/server/middleware, login page, `/auth/callback`, server home calling `/me`). Migrations 0003 (`app_user_memberships` SECURITY DEFINER resolver) + 0004 (identity-table RLS policies `TO atlas_app` — discovered Supabase auto-enables RLS on **all** public tables; global identity tables had no policy → fixed). Configured Supabase URL config (Site URL + redirect `localhost:4291/**`) via Chrome. Verified: real Google account → consent → `/me` 200 with mirrored user (`users` id=auth uid + `auth_identities` google sub + gmail.com domain), email ✓verified, empty-orgs onboarding path. Guard 401s confirmed. **Ports → API :4290 / web :4291** (avoid clashes, per user). All gates green. |
| 2026-07-01 | architect | **Google auth provider configured** (via Chrome): Google Cloud project + Google Auth Platform consent screen + OAuth client "Atlas Web" + redirect URI (Supabase callback) + test user added; **Supabase Google provider enabled** with real Client ID/secret. Prereq for F1.5 done. (Secrets in `.env`/dashboards only, never committed.) |
| 2026-06-30 | architect | **F1.4 ✅** — `@atlas/db` (plain `pg`, **Kysely removed**): 5 core tables, SQL migration runner, `atlas_app` non-bypass role + RLS, `withOrgScope`. **Verified on live Supabase** (RLS 3/3). Decisions from real testing: session-pooler/IPv4, two-role model (postgres BYPASSRLS), `NULLIF` GUC. |
| 2026-06-30 | architect | **Supabase project live** — connected, schema + RLS migrated to the real DB. `.env` set (app=atlas_app, migrations=postgres). Google provider + connection-string setup done via browser. |
| 2026-06-30 | architect | **Decision: adopt Supabase** (managed Postgres + Auth/Google + Storage). Backend/graph/AI unchanged; keep GUC-RLS isolation. Recorded in CLAUDE.md + memory; banners added to docs 02/04/12/13/17. Reordered F1 to DB-first. |
| 2026-06-30 | architect | **F1.3 ✅** — NestJS API shell (Fastify, `/health` boots OK) + Next.js 15 web shell + Turborepo build orchestration. `@atlas/config` → CJS dist consumed at runtime. All gates green. |
| 2026-06-30 | architect | **F1.2 ✅** — GitHub Actions CI (`ci.yml`) running format/lint/typecheck/test on push to main. First run green (22s). README badge added. |
| 2026-06-30 | architect | Adopted **main-only git workflow** (no branches/PRs yet; `dev` later). Cleaned history to 2 commits, no AI attribution. F1.1 on `main`. |
| 2026-06-30 | architect | **F1.1 ✅** — monorepo scaffold (pnpm workspaces, strict TS, eslint/prettier, vitest) + `@atlas/config` (Zod env). All gates green. |
| 2026-06-30 | architect | Pushed to **private** GitHub remote `anmolbhardwaj17/atlas`. |
| 2026-06-30 | architect | `git init` (repo `atlas`, branch `main`); initial commit `ec99660` with blueprint + scaffolding (28 files). SET-6 ✅. |
| 2026-06-30 | architect | Set up continuity scaffolding: CLAUDE.md, docs/README.md, this board, skills, memory (EPIC SETUP). |
| 2026-06-30 | architect | Completed full blueprint: wrote all 19 design docs `00`–`18` + `07b` (EPIC 0 ✅). |
| 2026-06-30 | architect | Interactive decisions captured: Bitbucket=Phase-2, Google-only auth + `hd` domain-join Phase-1, shadcn+MCP, adversarial QA agent. Propagated across 00/01/03/04/08/09/12/16. |
