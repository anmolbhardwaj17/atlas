# Atlas — Project Board

> **The single source of truth for "where we are."** Update this whenever a task changes status or work happens.
> Use the **`/board`** skill to update consistently. Last updated: **2026-07-11**.

**Status legend:** 📋 Todo · 🔵 In Progress · 🔍 Review · ✅ Done · ⛔ Blocked · ⏸️ Deferred (Phase-1+)

---

## 📍 YOU ARE HERE

> **Blueprint phase complete.** All 19 design docs are written (v1.0). Continuity scaffolding (this board, README, CLAUDE.md, memory, skills) is being set up.
> **Next milestone:** begin the build at **Sprint F1 — Foundation** (auth + tenancy + CI gates). Nothing is coded yet.
> **Suggested next action:** decide whether to `git init` the repo, then scaffold the monorepo per `docs/16` §2 and start F1.

**Progress at a glance**
- 📘 Blueprint (docs): **19 / 19 ✅ (100%)**
- 🧰 Project setup: **✅ done** (CLAUDE.md, README, board, skills, memory, git+GitHub)
- 🏗️ Build: **F1 ✅ + F2 ✅ COMPLETE** (Phase F done). **Now in Phase I (Ingest):** I1 (AWS crawler) ∥ I2 (GitHub crawler) implement the frozen SDK (`docs/06`/`07`). **I1 (AWS crawler) CODE-COMPLETE** (I1.1–I1.4; live-account verify pending creds). **Phase I CODE-COMPLETE** (I1 AWS + I2 GitHub, live verify pending creds). **Phase G: G1 (inference) ✅ COMPLETE** (all 8 rules, atlas.service, determinism + precision, infer stage wired). **G2 (read API) ✅ COMPLETE.** **Sprint G3 (AI engine) ✅ COMPLETE** — `@atlas/ai`: LLMProvider (Mock+Claude) + planner + retrieval/context + grounding gate + answer pipeline (cited, confidence-tiered, honest-absence, uncited-claim detection) + AI eval set (escaped-hallucination 0). Wired into the API: `/ai` SSE endpoints + conversation persistence (Claude in prod, mock in dev/CI). **Sprint G4 (Explore UI) ✅ COMPLETE** (G4.1 foundation + G4.2 Explore list/detail/provenance + G4.3 impact analysis + G4.4 Ask AI SSE chat + ⌘K). **✅ EPIC G COMPLETE** — the graph is queryable, explorable, and answerable (cited + confidence-tiered end to end). **Now in Phase P:** P1.1 (dashboard metrics) ✅ · **P1.2 (onboarding wizard + gated `POST /demo/seed` + reusable UI primitives) ✅** · **P1.3 (state polish — `EmptyState`/`ErrorState`/loading converged) ✅** · **P2 hardening first increment ✅** (observability correlation-id + structured logs · append-only audit log · CI web-build). `next build` fixed (NODE_ENV=production in the build script). **Next: remaining P2 is infra-bound** (load/perf, DR drill, mutation tests, adversarial QA agent + E2E/contract/AI-eval CI, security headers) → GA. **LIVE DATA NOW FLOWING** — a real **Bitbucket** workspace (Siemba) is connected end-to-end (60 repos, 155 merged PRs, 30-day insights) with a **durable encrypted secret store** (migration 0018) so re-sync survives restarts. Extensive P-phase consumer polish landed (see top of Activity Log). **AWS IS NOW LIVE (2026-07-05)** — real account (siemba/calsaws) connected via static keys: 150 resources, 27 edges, 86 signals synced; `degraded` pending 2 IAM read grants (`ecr:DescribeRepositories`, `route53:ListResourceRecordSets`). Sync visibility (live "Syncing…"/"Last synced · N resources"/missing-perms) on the Integrations hub + orphaned-run reaper (BR-SYNC-1 unblock). **🔐 Security & Vulnerability Intelligence — Phase 1 BUILT (2026-07-05):** dependency intelligence live end-to-end on the real Bitbucket graph — Bitbucket manifest parsing (npm/pypi/go/maven) → `external.package` nodes + `DEPENDS_ON_PKG` edges → **OSV.dev** enrichment stage → `security.vulnerability` nodes + `AFFECTS` edges → **Vulnerabilities / Blast-radius / Dependency-sprawl** findings in the dashboard + Ask AI (cited + knowledge-pack guidance). The **"exposed AND vulnerable" toxic combination (Phase 2)** is now unblocked (live AWS data flowing).

---

## 🧭 Operational-Intelligence build order (decided 2026-07-11)

> Full scope of the health→trace→advise north star + adjacent asks, sequenced **dependency-first**.
> Details: `docs/plans/operational-intelligence.md` + `docs/plans/intent-verification.md`.

**Spine (in order; each independently demoable on the live calsaws estate):**
1. **A · Finish the code↔infra bridge** — repo→ECS verified live (2/2 services, inferred-high). Gap = **Lambda SHA-probe** (`lambda:GetFunction` → `CodeSha256`/version/description → `DEPLOYS_TO` high; **20 Lambdas here = the live mass**, only name-tier today). The `DEPLOYS_TO` model stays runtime/cloud-general (compute_target + artifact + provenance witness; EC2/EKS/App Runner/Azure/GCP = "add an extractor"). Fold in: **duplicate-tier collapse** + **AI-suggested-edge reconnect bug**. *(building now)*
2. **B · Health layer (map turns red)** — CloudWatch alarms/metrics, ELB target health, ECS running-vs-desired, RDS status → lightweight health-only poll → `node_events`; red/amber on map + findings. The visible "wow". Some IAM needs the pending grant → builds now, lights on grant.
3. **C · Change timeline** — CloudTrail config events + deploy + pr_merged → unified cited per-node timeline.
4. **D · AI incident tracing** — onset (metric changepoint + log error-signature first-seen/rate), logs **on-demand** (Logs Insights, bounded), targeted code retrieval **@ deployed SHA**, ranked **classified verdict** (code/config/dependency/chronic/unknown), symptom-vs-cause via the graph, adversarial "no-culprit" eval. Needs A+B+C.
5. **E · Posture & personalized advisory** — mostly built (security/compliance findings); personalization + narration pass.

**Slotted:** **Email follow-ons (#44)** — immediate security emails + ops sync-failed + per-user notif prefs → **after B** (health findings feed the immediate emails).

**Blocked-on-user (~2 days, aligns with B):** grant AWS `SecurityAudit` + logs/cloudwatch IAM → re-sync (unlocks Phase-2b tail + B/D perms); switch LLM off `gpt-4o-mini`.

**Later epics (post-spine):** **Intent Verification** (Jira connector + PR↔Jira + coverage) — **SIFT** supplies code-review, not us, not yet available · **AI polish** (extended thinking behind an eval; the reconnect-bug fix rides in Wave A) · **Hardening P2** (load/perf, DR drill, mutation tests, adversarial QA in CI, RBAC sweep #43) · **Perf P0** DB co-location (needs a deploy).

---

## How to use this board (for future-me)
- Each **Epic** = a phase from `docs/15`. Each **Task** has an ID, Status, linked doc(s), and a **Status note** (the latest comment on it).
- When you work: set the task 🔵, leave a Status note, and **append a dated entry to the Activity Log** at the bottom.
- When done: set ✅, note what "done" means (which `docs/15` Definition-of-Done gates passed).
- Keep tasks small enough to reason about; add sub-tasks under a sprint as you start it.

---

## EPIC 0 — 📘 Blueprint / Documentation — ✅ DONE
*All design docs written, internally consistent, v1.0. See `docs/README.md`.*

| ID | Doc | Status | Note |
|---|---|---|---|
| DOC-00 | project-overview | ✅ | Vision, P1–P10, personas, metrics |
| DOC-01 | product-requirements | ✅ | FR/NFR/US + Gherkin |
| DOC-02 | system-architecture | ✅ | Modular monolith + workers, 5 planes |
| DOC-03 | domain-model | ✅ | Entities, BR-x, dual identity |
| DOC-04 | database-schema | ✅ | PG DDL, RLS, migrations; +Phase-1 auth tables |
| DOC-05 | knowledge-graph | ✅ | URN, edges, 8 inference rules, confidence |
| DOC-06 | aws-crawler | ✅ | Connector SDK, service catalog, partial-sync |
| DOC-07 | github-crawler | ✅ | App, webhooks, deploy inference |
| DOC-07b | bitbucket-crawler | ⏸️ | Phase-2 contingency (design only) |
| DOC-08 | api-specification | ✅ | REST, DTOs, SSE, OpenAPI; +Google/domain endpoints |
| DOC-09 | frontend-spec | ✅ | Next.js + shadcn, viz, 4 states, a11y |
| DOC-10 | ai-engine | ✅ | Grounded, cited, 7-layer anti-hallucination |
| DOC-11 | search-engine | ✅ | OpenSearch hybrid, embeddings |
| DOC-12 | authentication | ✅ | Google login, RBAC, hd-domain join (Phase-1) |
| DOC-13 | security | ✅ | Threat model, IAM, Persona-E package |
| DOC-14 | testing-strategy | ✅ | Pyramid + adversarial QA agent, DoD gates |
| DOC-15 | development-roadmap | ✅ | Sprints, DoD, MVP checklist |
| DOC-16 | coding-standards | ✅ | Gold standard, parse-don't-validate, shadcn-MCP |
| DOC-17 | deployment | ✅ | Fargate, CI/CD, DR |
| DOC-18 | business-model | ✅ | Pricing, GTM, moat |

---

## EPIC SETUP — 🧰 Project Continuity Scaffolding — ✅ DONE
*Make any future session instantly resumable (this session's work).*

| ID | Task | Status | Note |
|---|---|---|---|
| SET-1 | Root `CLAUDE.md` (rules + session protocol) | ✅ | Auto-loads every session |
| SET-2 | `docs/README.md` (index + cross-ref map) | ✅ | Navigation for all 19 docs |
| SET-3 | `docs/PROJECT-BOARD.md` (this board) | ✅ | Jira-lite tracking + activity log |
| SET-4 | Skills: `/resume`, `/board`, `/atlas-doc` | ✅ | In `.claude/skills/` |
| SET-5 | Memory files (decisions, state, prefs) | ✅ | 5 files + `MEMORY.md` in project memory dir |
| SET-6 | `git init` + GitHub remote | ✅ | Repo `atlas` on `main`; **private** GitHub remote `anmolbhardwaj17/atlas` pushed. |

---

## EPIC F — 🏗️ Foundation — 🔵 IN PROGRESS  *(walking skeleton + safety rails, `docs/15` Phase F)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| F1.1 | Monorepo scaffold + shared tooling + `config` package | ✅ | 16 §2/§4, 17 §6 | pnpm workspaces + strict TS + eslint(no-any) + prettier + vitest. `@atlas/config` (Zod env, parse-don't-validate). **All gates green: format/lint/typecheck/6 tests.** |
| F1.2 | CI gate harness (lint/typecheck/test) | ✅ | 14 §16, 17 §5 | GitHub Actions `.github/workflows/ci.yml`: pnpm + Node 22, format/lint/typecheck/test on push to main + PRs. **First run green (22s).** Badge on README. Heavier stages (integration/contract/adversarial-QA/E2E/load) added in later sprints. |
| F1.4 | DB foundation: Postgres (plain `pg`) + SQL migrations + core tables + RLS | ✅ | 04, 13 §6, 17 | `@atlas/db`: **plain `pg`, no ORM** (Kysely removed per user). Typed row schema, 5 core tables, own forward-only SQL migration runner (`schema_migrations`), `atlas_app` non-bypass role + RLS policies, `withOrgScope` GUC helper. **Verified end-to-end on live Supabase** — RLS isolation 3/3 (org A↔B + fail-closed), app connects as `atlas_app` / migrations as `postgres`. Fixes from real testing: `NULLIF` empty-string GUC (pooled conns), session-pooler (IPv4), two-role model (Supabase `postgres` has BYPASSRLS). `docker-compose.yml`+`.env.example` added. CI Postgres job = follow-up (F1.7). |
| F1.5 | Supabase Auth (Google) integration + session/JWT verify | ✅ | 12 | **Live Google login verified end-to-end** (real account → `/me` 200). `docs/12` §2–4 rewritten v1.1 (docs-before-code): Supabase-hosted Google OAuth (DD-1a), Atlas verifies the ES256 JWT via JWKS and mints no tokens (DD-2 revised), identity mirrored into `public.users` (id=auth uid) + `auth_identities`, active org resolved live via `app_user_memberships` SECURITY DEFINER fn (org not in token). **Backend:** `CoreModule` (env + atlas_app pool), `SupabaseJwtVerifier` (jose JWKS, iss/aud, HS256 fallback), `AuthGuard`, `UserMirrorService`, `MembershipService`, `GET /me`; claims unit tests. **Frontend:** `@supabase/ssr` browser/server/middleware clients, Sign-in-with-Google page, `/auth/callback`, server home calling `/me`. **Migrations:** 0003 resolver fn (EXECUTE→atlas_app), 0004 identity-table RLS policies `TO atlas_app` (Supabase auto-enables RLS on all public tables — global identity tables needed policies; anon/authenticated deliberately denied). Guard 401s (missing/bogus/malformed) verified. Ports moved to **API :4290 / web :4291** (avoid local clashes). All gates green. |
| F1.6 | Org / RBAC / memberships / invitations (endpoints + guards) | ✅ | 12, 03, 08 | **Live-verified through the app** (real Google session → create org → Owner; invite pending; members RLS-scoped read; last-Owner demote rejected; cross-tenant 404). **Guards:** `TenantScopeGuard` (resolves active org + live role from `X-Atlas-Org`/path via the SECURITY DEFINER resolver; path cross-tenant→404, header→403) + `@Roles`/`RolesGuard` (Owner>Admin>Member). **Endpoints:** POST /orgs (creator→Owner, BR-ORG-1), GET/PATCH /orgs/:id, GET /orgs/:id/members, PATCH/DELETE members (BR-MEM-2/3 invariants), POST/GET/DELETE invitations, POST /invitations/:token/accept (capability token, sha256-hashed, never returned). **API conventions:** `{data}` success envelope + `{error:{code,message,details,requestId}}` error model (docs/08 §4/§11) via global interceptor+filter; zod request validation; CORS for the web origin. **Migration 0005:** organizations org-scope RLS policy + `app_invitation_by_token` resolver. **Docs:** §5.2 RBAC + docs/08 §6 auth-endpoints synced. **Frontend:** create-org form + org-management panel (members/invitations/invite). Unit tests: RolesGuard, slug. All gates green. |
| F1.7 | Tenant-isolation test (US-12 foundation) + CI Postgres job | ✅ | 04 §10, 13 §6, 14 §16 | **CI `integration` job** added (`.github/workflows/ci.yml`): `postgres:16` service → run migrations as owner → grant `atlas_app` LOGIN (throwaway pw via `setup-app-role.ts`) → run env-gated integration tests against real PG with the two-role + RLS setup. **Expanded coverage** (`org-scope.test.ts`): organizations org-scope (+ fail-closed), invitations cross-org isolation, `app_user_memberships` + `app_invitation_by_token` resolvers, and a write-guard negative (can't INSERT another org's row while scoped). **9 integration tests pass** against live PG; skip cleanly in the no-DB `check` job (21 unit tests stay green). Expands to graph tables in the G sprints. |

### Sprint F2 — Connector framework + secrets + queue *(`docs/15` Phase F)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| F2.1 | `connections` + `sync_runs` schema (migration + RLS + types) | ✅ | 04 §5.2, 03 §3.5/3.6 | Migration 0006: both tables with org-scoped RLS (`TO atlas_app`), `updated_at` trigger, **BR-SYNC-1** partial-unique in-flight index. Row types + enums in `@atlas/db`. Integration test: connections cross-org isolation (10 integration tests pass on live PG). |
| F2.2 | `@atlas/connector-sdk` — frozen Connector contract | ✅ | 06 §3 (DD-1) | New pure-types package: `Connector` interface (verify/health/plan/discover/fetchDetail/normalize/extractSignals/observedEdges) + shared types (Connection, SyncRun, WorkPlan, Scope, ResourceRef, RawResource, NodeUpsert, Signal, EdgeUpsert, CrawlContext, SecretAccessor, VerifyResult). Provider union open (extensible). **Contract frozen in F2 (RMR-6).** |
| F2.3 | Graph persist schema (nodes/edges/provenance/raw_snapshots/node_kinds/inference_rules) | ✅ | 04 §5.3–5.4, §6, 05 | Migration 0007: full knowledge schema + indexes + composite FKs (same-org edges, BR-EDGE-1) + provenance-required/inferred-needs-rule constraints; `uq_edge` **NULLS NOT DISTINCT** (observed-edge dedupe, doc synced). Org-scope RLS on nodes/edges/provenance/raw_snapshots; global vocab policy on node_kinds/inference_rules. Row types in `@atlas/db`. **graph-scope.test.ts** (4 tests): node/edge org isolation, cross-tenant edge structurally rejected, observed-edge dedupe. **14 integration tests pass on live PG.** |
| F2.4 | Mock connector + staged sync runner (EXIT proof) | ✅ | 06, 02 §5.2, BR-SYNC-2 | **F2 exit criterion met.** New `@atlas/ingest`: `runStagedSync` (plan→per-scope discover→fetchDetail→normalize/observedEdges→persist→reconcile; each scope one tx; org-scoped via withOrgScope) + `MockConnector` (in-memory fake provider, failure injection, discover-call tracking) + `InMemorySnapshotStore` + dev SecretAccessor/logger. **4 integration tests on live PG:** idempotent persist (upsert by URN/uq_edge, no dupes), reaping (unseen → stale on clean sync), **BR-SYNC-2** (failed scope → partial → reconcile skipped → no false delete), resumability (checkpoint skips completed scopes). CI integration job now runs all integration tests. |
| F2.5 | BullMQ queue + worker runtime | ✅ | 02 §5 (DD-6) | Provider-agnostic `JobQueue` (keeps core decoupled / Temporal-swappable) + `InMemoryQueue` (dev/test, idempotent jobId dedupe, buffering) + `BullMQQueue` (Redis driver, thin) + sync-job worker (`createSyncHandler`/`registerSyncWorker`/`enqueueSync`, deterministic `syncJobId`). Worker handler loads connection → resolves connector → `runStagedSync`. Tests: InMemoryQueue unit (3) + worker→runner→persist on live PG (1). Scheduler + API enqueue-on-verify land with I1 (real connectors). |
| F2.6 | Secrets Broker (interface + dev impl) | ✅ | 13 §7 | `SecretBroker` (extends SDK `SecretAccessor`) + `InMemorySecretBroker` in `@atlas/ingest`. `put`→opaque `secret_ref` (BR-CONN-1, raw secret never on the row); `get`/`delete`. Prod = AWS Secrets Manager (later). |
| F2.7 | Raw-snapshot store (Supabase Storage) | ✅ | 04 §5.4, 13 | `SupabaseStorageSnapshotStore` (impl of `SnapshotStore`): private bucket, content-addressed (`<orgId>/<hash>.json`), idempotent upsert; service-role client (server-only); `storage_ref` = `<bucket>/<path>`. `ensureBucket` provisions programmatically. **Live round-trip verified** against real Supabase Storage (put→get, missing→null). |
| F2.8 | Connection API skeleton | ✅ | 08 §8 | **Live-verified** (real session → create→verify→connected). `/connections` (X-Atlas-Org scoping): POST (Admin), GET list (Member), GET :id (Member), POST :id/verify (Admin — stores creds via broker, calls connector.verify, maps status/health, 422 `connection_verification_failed` on error), DELETE :id (Admin, soft). `ConnectorRegistry` (placeholder mock connectors until I1/I2). zod DTOs; secrets never in responses (`secretConfigured` flag). Guard 401s verified; registry unit tests. |
| F1.3 | App shells: NestJS API + Next.js web + Turborepo | ✅ | 02, 09, 16, 17 | `apps/api` (Nest+Fastify, `/health` ✅ boots) + `apps/web` (Next 15/React 19 shell) + Turborepo build orchestration. `@atlas/config` now builds to CJS, consumed by API at runtime. All gates green. **Note:** full structured-logging/correlation-id observability package deferred to a follow-up (basic Nest logger for now). |
| F2 | Connector SDK interface + queue/worker/scheduler | 📋 | 06 §3, 02 §5 | BullMQ, the fork point |
| F2 | Secrets Broker + Secrets Manager | 📋 | 13 §7 | |
| F2 | Connections + sync_runs lifecycle + S3 snapshots | 📋 | 03, 04, 08 §8 | |

**Exit (M0):** Google login → create org → invite Member; US-12 + RLS pass; CI green; deploys to staging.

---

## EPIC I — 📥 Ingest — ✅ CODE-COMPLETE  *(both crawlers built; live verify pending creds)*

> **Phase I is CODE-COMPLETE — both crawlers built (I1 AWS + I2 GitHub), CI-green.** The only open items are **live verification** (need customer creds): AWS needs a read-only IAM role (ARN + Atlas External ID, docs/13 §4); GitHub needs an App installation (App id + installation id + private key, docs/13 §5). When creds exist: create connection → verify (→ connected/degraded + missing-perms) → confirm populated nodes/edges/provenance + raw snapshots. **Additive follow-ups** (no architecture change): AWS DynamoDB/ElastiCache/APIGW/IAM-policy (docs/06 §9); GitHub webhook API route + worker process (deploy). **Next milestone: G1 (inference engine + edges)** — reads the emitted signals → derives cross-source edges (`docs/05` R1–R8).

### Sprint I1 — AWS crawler *(`docs/06`)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| I1.1 | AWS connector foundation: package + URN grammar + node-kind vocab seed | ✅ | 05 §2/§3, 06 §4 | New `@atlas/connector-aws` (CJS, requireable by API). `awsUrn()` builds deterministic 5-segment URNs matching docs/05 §2.2 exactly (region-scoped + literal `global` for S3/Route53/IAM; natural key case-preserved). `AWS_NODE_KINDS` catalog (18 kinds + URN type/scope). Migration 0008 seeds `node_kinds` (24 = 18 AWS + 5 GitHub + 1 atlas) — global vocab needed before any node insert (FK). 9 unit tests; migration applied to live DB (verified counts). |
| I1.2 | Credential provider (STS AssumeRole + externalId) + verify/health + permission detection → degraded | ✅ | 06 §2/§8, 13 §4 | `StsCredentialProvider` (sts:AssumeRole via Atlas's own identity → ≤1h creds, in-memory only; `sessionName=atlas-…-<id>` for customer CloudTrail traceability; account id parsed from assumed-role ARN). External ID resolved from Secrets Broker (C1), never config. `parseAwsConfig` validates roleArn + regions allow-list. `verify`/`health` AssumeRole then run per-service permission probes: AccessDenied → **`degraded`** with the missing IAM actions (P3, US-1); transient errors ≠ missing perm; AssumeRole/config failure → `error`. Probe framework + `isAccessDenied` classification (real per-service probes land in I1.3). 27 unit tests (aws-sdk-client-mock for STS). |
| I1.3 | Service discoverers + pure normalize/extractSignals/observedEdges (golden fixtures) | ✅ | 06 §4/§5, 05, 14 §10 | Per-service `ServiceModule` layer (pure normalize/observedEdges/extractSignals dispatched by node kind; additive per docs/06 §9). **14 modules**: VPC, Subnet, SecurityGroup, EC2, Lambda, ECS (cluster/service/taskdef), ECR, ELB, Route53, RDS, S3, IAM-role — exercising **every observed edge type** (CONTAINS, PROTECTS, ASSUMES_ROLE, USES_IMAGE, ROUTES_TO, TRIGGERS) + signals for R2/R3/R8 inference. Deterministic URNs via `awsUrn`; `plan()` fans region×service + global-once. Route53→ALB kept as a **signal not observed edge** (alias DNS ≠ deterministic ELB → P3). Golden-fixture tests assert exact nodes/edges/signals (100% precision on fixtures). 44 unit tests. **Additive remaining** (not silently dropped): DynamoDB, ElastiCache, API Gateway, IAM-policy. discover/fetchDetail (live SDK) = I1.4. |
| I1.4 | Resilience (rate-limit/retry/pagination) + incremental hash-diff + wire into runner/registry/API | ✅ | 06 §7, 02 §5 | **Code-complete (live-account verify pending creds).** Discovery layer: `Discoverer` interface + 14 live discoverers (EC2/VPC/Subnet/SG via one client, Lambda, ECS×3, ECR, ELB w/ target-health, Route53, RDS, S3 multi-call, IAM inline-policies) with SDK paginators + adaptive retry; connector `discover`/`fetchDetail` collapse list+detail via a per-run payload cache; one AssumeRole per run. `withRetry` + `classifyAwsError` (throttling/transient backoff+jitter; access-denied/not-found/fatal pass-through, docs/06 §7.3). Permission probes derived from discoverers (verify now does real detection). **Registry wired** — real `AwsConnector` for "aws" (API boots clean). **Enqueue-on-verify** — onboarding `sync_runs` row + `enqueueSync` (BR-SYNC-1 one-in-flight). **Incremental hash-diff** in runner: unchanged content hash → skip re-snapshot (`unchanged` stat), live-DB tested. 54 connector-aws unit tests (incl. aws-sdk-client-mock) + 11 ingest integration. |

### Sprint I2 — GitHub crawler *(`docs/07`)*

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| I2.1 | GitHub connector foundation: package + URN grammar + external.package vocab | ✅ | 05 §2/§3, 07 | New `@atlas/connector-github` (CJS). URN builders match docs/05 §2.2 (repo `github:o/r`, PR `…:pr:n`, workflow `…:workflow:<path>`, team `github:o:team:slug`, user `github:user:login`, package `external:<eco>:package:<name>`); case-significant keys preserved. `GITHUB_NODE_KINDS` catalog (5 github + external.package). Migration **0009** seeds `external.package` (provider `external`, DEPENDS_ON_PKG target) — applied to live DB. 6 unit tests. |
| I2.2 | GitHub App auth (installation token) + verify/health + permission detection | ✅ | 07 §2, 12, 13 §5 | `GithubAppTokenProvider`: signs a short-lived App JWT (RS256, iat-backdated, exp +9min) via Node `createPrivateKey` (handles GitHub's PKCS#1 keys) → exchanges for an **installation access token** (≤1h, in-memory, read-only — mirrors AWS AssumeRole). App private key resolved from Secrets Broker (C1). Permission detection reads the token's granted `permissions` **directly** (no probing) → `degraded` listing missing `key:read` (metadata/contents/pull_requests/actions/members, docs/07 §2); bad config/key or failed exchange → `error`. 17 unit tests (config, permission logic, real RS256 JWT sign+verify via generated keypair, verify decision matrix with a fake token provider). Crawl stages stubbed (I2.3/I2.4). |
| I2.3 | Pure parsers + normalize/extractSignals/observedEdges (golden fixtures) | ✅ | 07 §6/§7, 05, 14 | Pure parsers: **CODEOWNERS** (rules + team/user classification), **manifests** (npm/pypi/go direct deps, additive ecosystems), **workflow deploys** (AWS actions + ECS `with:` + `run:` flags + ARNs via `yaml`). 6 entity modules dispatched by kind: repository (+ OWNED_BY from CODEOWNERS, DEPENDS_ON_PKG from manifests), pull_request (+ changed-files signal for R6), workflow (+ CONTAINS(repo→workflow) + deploy signal for R1), team/user/package (leaf nodes so edges resolve). **Deploy targets emitted as signals — DEPLOYS_TO is inferred (R1/G1), not observed (P3).** Connector dispatches normalize/observedEdges/extractSignals. 31 unit tests incl. golden fixtures. |
| I2.4 | Live discoverers + rate-limit/retry + webhook HMAC ingress + wiring | ✅ | 07 §5/§10, 13 §5 | **Code-complete (live-install verify pending App creds).** `FetchGithubClient` (bearer install-token, Link pagination, header-aware rate-limit/retry: primary `x-ratelimit-reset` wait, secondary `retry-after`, 5xx backoff — bounded, injectable). `crawlRepo` fetches repo + CODEOWNERS/manifests/workflows + recent PRs and yields repo/workflow/PR nodes **plus** the edge-target team/user/package nodes (so OWNED_BY/DEPENDS_ON_PKG resolve in-scope); `plan()` = one scope per installed repo (bounded/resumable); one installation token+client per run. **Webhook HMAC verify** (timing-safe, GHR-8) + event→descriptor mapping. **Registry wired** — real `GithubConnector` for "github" (API boots clean with both connectors); enqueue-on-verify already generic. 42 connector-github unit tests (webhook, client pagination+rate-limit via fetch mock, crawl+connector via fake client). Webhook API route + live crawl = deploy/live-creds step. |

**Exit (M1):** real AWS+GitHub → nodes + provenance; degraded reports missing perms; no false deletes.

---

## EPIC G — 🧠 Graph & Intelligence — 🔵 IN PROGRESS

### Sprint G1 — Inference engine + edges *(`docs/05` §6)* — ✅ DONE

> **G1 exit criterion MET:** the worked example (`docs/05` §9) reproduces — repo DEPLOYS_TO service, service CONNECTS_TO RDS, atlas.service + IMPLEMENTS/RUNS, OWNED_BY, CHANGED_BY — each cited + tiered; determinism golden file passes; precision 100% (≥95%) on the labeled set; convergence (zero churn on no-change); infer stage runs post-sync. **Next: G2 (read API + traversals + search).**

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G1.1 | Signals persistence + inference engine core + R1 (DEPLOYS_TO) | ✅ | 05 §6, 04 | New `signals` table (migration 0010 + `docs/04` §5.4b) persisted during sync (runner `extractSignals`→upsert). New `@atlas/inference` engine: pure versioned `Rule`s → candidate edges; engine loads nodes+signals+observed-edges, upserts inferred edges with **provenance + evidence + inference_rule_id**, and **retires** (not deletes) edges a rule no longer produces → **convergent (IE-4: re-run writes nothing)**. inference_rules seeded (migration 0011, R1–R8 metadata). **R1 repo→runtime DEPLOYS_TO**: workflow-deploy signals resolved vs AWS runtimes — exact ARN / unique name → `inferred-high`, ambiguous → multiple `inferred-low` (P3). 11 tests (4 pure R1 tiering + 3 live-DB engine: derive+provenance, converge, retire). |
| G1.2 | R4 (atlas.service + IMPLEMENTS/RUNS) + R6 (CHANGED_BY) + R5-propagation | ✅ | 05 §6.4 | Engine extended: rules now emit **derived nodes** (upserted first so their edges resolve) + read earlier rules' inferred edges in one dependency-ordered pass. **R4**: from a high `DEPLOYS_TO`, derive `atlas.service` (`atlas:<slug>:service:<repo>`) + `IMPLEMENTS`(repo→svc) + `RUNS`(runtime→svc); high-only (DD-1); one service per repo, RUNS×N runtimes. **R5-prop**: observed repo `OWNED_BY` + `IMPLEMENTS` → service `OWNED_BY` (high). **R6**: merged-PR `pr.files` signal + repo `IMPLEMENTS` → service `CHANGED_BY` pr (high if 1 service, low if monorepo — P3). `nodes.connection_id` made nullable (migration 0012 + `docs/04` §5.3) for derived nodes. 16 tests (12 pure R4/R5/R6/R1 + 4 live-DB incl. full R1→R4→R5→R6 worked-example chain). |
| G1.3 | R2 (SG correlation) + R3 (env refs) + R8 (IAM access) → CONNECTS_TO/STORES_IN | ✅ | 05 §6.4 | The data-dependency edges. **R3** (high): Lambda/ECS env value referencing a crawled RDS endpoint / S3 bucket / DynamoDB table → CONNECTS_TO / STORES_IN (evidence stores env KEY + resource, not raw value). **R2** (high): compute SG allowed (ingress) into a datastore SG on the engine port + same VPC → CONNECTS_TO (reachability, resolved via PROTECTS edges + sg.rules signals + groupRef→SG mapping). **R8** (low): ASSUMES_ROLE + IAM policy statement granting a specific resource ARN → CONNECTS_TO/STORES_IN; wildcard grants ignored (IAM permission ≠ use, P3). Shared `refs.ts` (engine-port, ARN→node resolution). 6 pure tests (+ engine integration unaffected). |
| G1.4 | Reconciliation/determinism golden files + precision harness ≥95% + wire infer stage | ✅ | 05 §6.5, 14 §10 | Pure `evaluateAll` (mirrors the engine fold, no DB) powers the quality tests. **Determinism golden file**: the docs/05 §9 worked example reproduces an exact 7-edge set (CONNECTS_TO×2, DEPLOYS_TO, IMPLEMENTS, RUNS, OWNED_BY, CHANGED_BY) + 1 derived atlas.service; re-run = identical plan (IE-1). **Precision harness** (≥95%): labeled fixtures incl. negative cases (wrong-port, wildcard-IAM) → **100% precision, zero false positives**. **Infer stage wired**: the sync worker runs `runInference` after each non-failed sync (docs/05 §6.1); inference errors are logged, never fail the sync (P7). Sync→infer proven end-to-end (mock sync w/ deploy signal → DEPLOYS_TO). 25 inference + 12 ingest tests; API boots clean. |

### Sprint G2 — Read API + traversals + search *(`docs/08` §9, `docs/11`)* — ✅ DONE

> **G2 exit:** the graph is queryable — nodes/edges/detail/neighbors, **blast-radius/dependencies** with why-chains + pathConfidence, /timeline, edge evidence, and **/search** (keyword; hybrid vector at deploy). All org-scoped + cited. Unblocks **G3 (AI) ∥ G4 (Explore UI)**. Search's OpenSearch vector driver deferred to deploy (ledger).

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G2.1 | Read API: nodes list/detail/edges/neighbors + cursor pagination | ✅ | 08 §9 | New `GraphModule` (Member+): `GET /nodes` (filter kind/region/status/confidence/`q`, keyset cursor over (last_seen,id) desc — stable under crawl writes, DD-3), `/nodes/:id` (detail + provenance/raw-snapshot click-through, P4), `/nodes/:id/edges` (in/out/both, type/confidence filters), `/nodes/:id/neighbors` (depth-1, node-budgeted). Org-scoped via withOrgScope (RLS); malformed/cross-tenant → 404. `parseBody` fixed for `.default()`/`.coerce` schemas. 6 live-PG service tests incl. tenant isolation. |
| G2.2 | Traversals: blast-radius (inbound) + dependencies (outbound) + why-chains | ✅ | 05 §7.2, 08 §9 | `GET /nodes/:id/blast-radius` (inbound impact, US-4) + `/dependencies` (outbound, US-9). Shared recursive-CTE `traverse`: bounded depth (≤6) + nodeBudget (≤500) + cycle guard (edge-path array), over-limit clamped with `warnings` (A21). Impact edge set CONNECTS_TO/DEPENDS_ON/ROUTES_TO/STORES_IN/DEPLOYS_TO (overridable). Each impacted node carries **`via` (why-chain edges w/ evidence + rule)** + **`pathConfidence` = weakest edge** (docs/05 §8); `minConfidence` prunes low paths (P3 high-only view). 7 live-PG tests incl. weakest-edge + minConfidence + depth-clamp. |
| G2.3 | /timeline + /edges/:id evidence + raw-snapshot link | ✅ | 08 §9/§10.3 | `GET /timeline?since=&kinds=&limit=` (US-5 "what changed"): new/updated nodes (first_seen vs last_seen → created/updated) + new edges since a timestamp, merged newest-first; `kinds` filters node changes. `GET /edges/:id`: full edge detail — from/to summaries, evidence, rule (`key@version`), provenance (source/observedAt) + raw-snapshot ref (P4 click-through). Both org-scoped (404/empty cross-tenant). 9 live-PG tests total (2 new: edge detail + isolation, timeline window + kind filter). |
| G2.4 | Search: SearchProvider + Postgres hybrid + index stage | ✅ | 11 | `GET /search?q=&kind=&type=` (Member+) behind a **`SearchProvider`** interface (SEARCH_PROVIDER token; OpenSearch driver swaps in at deploy — docs/11 impl note). `PostgresSearchProvider`: pg_trgm `similarity` + substring over name/urn/attributes (SE-1 — queries `nodes` directly, no separate index), returns `{node,score,match,highlights}` with `semantic:null` (keyword-only MVP). Org-scoped (SE-4/R8). 4 live-PG tests (identifier match + highlight, kind filter, attribute match, tenant isolation). API boots with both new modules. |

### Sprint G3 — AI engine *(`docs/10`)* — ✅ DONE  *(parallel with G4)*

> **G3 exit:** canonical questions answered with citations + confidence tiers; **honest-absence on zero grounding** (US-11); hallucination defenses L1–L7 in place; AI eval set (canonical + adversarial) green with escaped-hallucination rate 0 (<1% bar). New package `@atlas/ai`; `/ai` SSE endpoints live (Claude in prod, mock in dev/CI). **Next: G4 (Explore UI).**

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G3.1 | `@atlas/ai`: LLMProvider (Mock + Claude) + prompt + RetrievalPort | ✅ | 10 §3/§8 | New `@atlas/ai` pkg (CJS, NestJS-free). `LLMProvider` interface (streamed, tool-capable, AE-5) + `MockLLMProvider` (deterministic responder → LLMEvent stream, for CI eval) + `ClaudeProvider` (Anthropic SDK adapter; text + tool-call deltas). Versioned `SYSTEM_PROMPT` (§8 six invariants: grounding/citation/confidence/honesty/scope/injection-resistance) + `honestAbsence`. `RetrievalPort` interface (search/getNode/blastRadius/dependencies/edges/timeline) — the API adapts it over G2. 5 unit tests. |
| G3.2 | Query planner (intent → retrieval plan) + entity resolution | ✅ | 10 §4.2 | `classifyIntent` (rule-based, deterministic → reliable canonical questions): blast_radius/dependents/deploy_mapping/architecture/timeline/culprit/lookup/out_of_scope. `resolveEntity`: extract identifier-like/content terms (stopword-filtered) → `port.search` → merged, ranked candidates; 0 = unresolved (→ honest-absence), >1 near-top = ambiguous (candidates, P3 — never silently pick). `plan()` → typed `RetrievalPlan` (intent + entity + window). 15 unit tests (8 canonical-question classifications + resolution + ambiguity). |
| G3.3 | Retrieval orchestration + context builder + grounding gate (honest-absence) | ✅ | 10 §4.3–§4.5 | `orchestrate` runs the plan via RetrievalPort (blast_radius/dependents→blastRadius; deploy_mapping→in-DEPLOYS_TO; culprit→in-CHANGED_BY+timeline; timeline→window; lookup/architecture→edges) — retrieval-first, no LLM. `buildContext` (DD-3): compact CONTEXT block where every node/edge carries a stable `N#/E#` marker + `cite:id` + confidence + evidence + FRESHNESS notes; returns the `cites` map for deterministic binding (DD-5). `groundingGate` (DD-4, deterministic): out-of-scope / unresolved-entity → structured honest-absence reason (US-11); resolved entity with empty traversal still grounded ("nothing depends on X" is truthful). 7 unit tests (fake port). |
| G3.4 | Answer pipeline: narrator + citation engine + confidence scorer + AI eval in CI | ✅ | 10 §5–§7, 14 §11 | `answerQuestion` = plan→retrieve→gate→narrate→post-process. Gate fails → honest-absence (never calls the LLM, L1). Narrate via LLMProvider (low temp). **Citation engine** (DD-5/L4): binds `[N#]/[E#]` markers → real ids + provenanceUrl, dedup+numbered; unknown markers dropped (can't fabricate a source). **Uncited-claim detector** (L5): flags factual sentences lacking a marker (hedges excused). **Confidence scorer** (§5): overall = weakest cited tier; caveats from FRESHNESS. **AI eval set** (L7, docs/14 §11): canonical (cited+tiered) + honest-absence (unresolved/out-of-scope) + adversarial (fabrication blocked by gate / caught by L5) — escaped-hallucination rate 0. 10 unit+eval tests (37 pkg total). |
| G3.5 | SSE /ai endpoints + conversation memory + wire into API | ✅ | 08 §10.2, 10 §9 | `answerQuestionStream` (SSE events: retrieval→token→citation→confidence→done). `AiModule`: `GraphRetrievalPort` adapts the engine's port over G2 GraphService+SearchProvider; LLM provider = Claude when `ANTHROPIC_API_KEY` set, else dev mock (retrieval/grounding/citations still work). `ai_conversations`/`ai_messages` tables (migration 0013, org-scoped RLS). `POST /ai/conversations`, `POST /ai/conversations/:id/messages` (manual SSE via raw reply), `GET /ai/conversations/:id`. `askStream` persists user + assistant turns (citations + confidence). 3 live-PG tests (grounded+cited+persisted, honest-absence, cross-org 404); API boots with AiModule (/health 200). |

### Sprint G4 — Explore UI *(`docs/09`)* — ✅ DONE

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| G4.1 | Frontend foundation: design system + tokens + AppShell + Dashboard | ✅ | 09 §3, §5.1–5.2 | Tailwind v3 + **certainty visual language** tokens (observed/inferred-high/inferred-low/stale as CSS vars); hand-vendored shadcn-style primitives (Card, Badge) + `cn()`; `ConfidenceBadge`/`FreshnessTag`/`CitationLink`; `lib/api.ts` (`getSession`/`apiGet` — Bearer + X-Atlas-Org); `AppShell` (TopBar + nav Dashboard/Explore/Ask/Settings + sign-out); server-rendered `Dashboard` (connections + recent-nodes preview + empty states). `page.tsx` now: Landing (no session) → create-org card (no membership) → **AppShell + Dashboard** (authed). Login now lands on a real home. `next build` green; `pnpm run check` green. |
| G4.2 | Explore: nodes list (filter/search) + node detail + provenance drawer + edges | ✅ | 09 §5.3 | Server-rendered (secrets stay server-side, NFR-22): `requireShell()` (session+org guard, redirects), `graph-types.ts` (read-API shapes). **`/explore`** — filter bar (GET form: q/kind/status/confidence; URL is source of truth, works no-JS) + nodes table (certainty + freshness per row) + keyset "Next page". **`/explore/[id]`** — node detail: header (confidence+freshness), attributes, **provenance** (source/observed/sync-run/raw-snapshot; "derived, not observed" when none), connections split incoming/outgoing with per-edge confidence + origin. **`/explore/edge/[id]`** — the **"why?"** page (P4): origin, derived-by rule, evidence JSON, raw source. Cross-tenant/absent → graceful not-found (404-as-not-found, R8). `next build` green (7 routes); `pnpm run check` green. |
| G4.3 | Blast-radius panel + neighbor subgraph (list-first, a11y) + graph canvas | ✅ | 09 §5.4 | **`/explore/[id]/impact`** — side-by-side **Blast radius** (downstream: what breaks if this changes) + **Dependencies** (upstream: what this needs), both from the G2 recursive-CTE traversals. **List-first (a11y)**: impacted nodes grouped by hop distance, each with **pathConfidence** (weakest-link, P3) and an expandable **why-chain** (native `<details>` — the edges back to root, each linking to its evidence page, P4). Truncation/warning banner surfaced (never silent, A21); empty = "truthful empty result, not a failure" (honest-absence). "Impact analysis" entry added to node detail. **WebGL graph canvas deferred to ledger** (optional enhancement; the list is the source of truth). `next build` green (8 routes); `pnpm run check` green. |
| G4.4 | Ask AI surface (SSE chat, citations/confidence) + ⌘K search + 4 UI states | ✅ | 09 §5.5, 10 | **`/ask`** — client chat streaming the G3.5 SSE `/ai/conversations/:id/messages` endpoint (`streamAsk` parses the SSE frames off a fetch reader — EventSource is GET-only). Tokens append live; **citations** render as numbered chips linking to `/explore/[id]` or `/explore/edge/[id]` (P4); **ConfidenceBadge** on each answer; caveats surfaced. The **four designed states** (docs/09 §7): empty (example prompts) · streaming (typing dots + caret) · answered (cited + tiered) · **honest-absence** (`confidence: insufficient` → muted "I don't know", never a fabrication — US-11). **⌘K command palette** (`CommandPalette` + `CommandTrigger`, connected by a window event): keyboard-first (⌘K/Ctrl-K, ↑↓, ↵, Esc), debounced `/search`, results navigate into Explore. Client calls use the public Supabase token (RLS + GUC still enforce). `next build` green (all routes); `pnpm run check` green (17 tasks). |

**Exit (M2–M5):** cross-source cited edges; canonical questions answered correctly & honestly; explorable. ✅ **EPIC G COMPLETE** — G1 (inference) + G2 (read API) + G3 (AI engine) + G4 (Explore UI + Ask AI + ⌘K). The graph is queryable, explorable, and answerable — cited and confidence-tiered end to end. **Next: Phase P (P1 onboarding/UX polish → P2 hardening) → GA.** (Live data still pending connector creds — deferral ledger.)

### AI Knowledge Engine — evolving Ask AI into an agentic Graph-RAG expert (`docs/plans/ai-knowledge-engine.md`)

| ID | Title | Status | Doc | Note |
|---|---|---|---|---|
| KE-0 | Game plan + P1 detailed design | ✅ | plans/ai-knowledge-engine(.md/-p1-design.md), 10 | Agentic Graph-RAG; **fact/advice trust model** (facts graph-only+cited; expertise=labelled advice anchored to facts); provider knowledge packs; roadmap P0→P4; P1 tool-loop design (tool-turn msgs, hybrid router, `ContextAccumulator`, budgets, evals) **+ WebSocket transport step** (live cancel/smoother progress). |
| KE-P0 | Estate slice — aggregate questions | ✅ | 10 §4.2 | New `estate` intent + `estateOverview` port (reuses `GraphService.summary`) + **computed `A`-marker** citations. "top contributors / how many repos / what needs attention" now grounded+cited. Verified vs real Siemba graph (Mohit=87, Sahil=35…). 51 ai tests pass. **Not committed yet.** |
| KE-P1 | Agentic tool-calling retrieval loop | ✅ | plans/…p1-design.md, 08 §10.2 | **All 9 steps done + verified.** Tool-turn `ChatMessage` (DD-P1-1) · tool registry (6 bounded read-only tools) · `loop.ts` (agentic loop + `ContextAccumulator`) · hybrid router · `retrieval_step` SSE + `ask-chat` trace · **WebSocket transport** (`/ai/ws`, DD-P1-5: first-frame auth+membership, cancel/interrupt, ping/pong, origin-pinned; client `streamAskWS` + Stop button + SSE fallback; `signal` threaded askStream→loop→provider→fetch) · golden+adversarial eval set · docs/08+10 updated. **61 ai tests** (loop determinism, estate golden, agentic-route, adversarial L5). Verified live (gpt-4o-mini `search→get_node→get_neighbors`→50-cite answer) + WS auth-reject path. DoD: tests+typecheck+lint green; US-11/P3/P4 preserved (trust back-half unchanged). **Next: P2 (AWS knowledge pack + advisory answers) when the AWS role lands.** |
| KE-P2 | Advisory answers + knowledge packs | ✅ | 10, plans/ai-knowledge-engine.md §5-6 | **Advisory slice done + verified live** (provider-agnostic; works on Bitbucket now, AWS-ready). New `advisory` intent + **fact/advice trust model**: findings (graph) cited as facts (`A`-markers), guidance (`knowledge.ts` `guidanceFor(category)` — why/fix/pillar/source, keyed by finding category) rendered as **labelled recommendations** by `ADVISORY_SYSTEM`; new `advisory` confidence tier (UI badge "recommendation"). **68 ai tests.** Verified live: "how do I improve my setup" → "56 repos have no CI/CD [A1] → Recommendation: add pipelines…", tier advisory, cited. **Deferred within P2:** `describe_config`/`find_by_condition` per-resource config tools + the full AWS Well-Architected rule library (need the AWS connector's config projections — do when the role lands). |

---

## EPIC P — ✨ Polish — 🏗️ IN PROGRESS

> **Plan (user-chosen): seed demo data → P1 (onboarding/UX) → P2 (hardening); skip live-creds for now.**
> Demo data is seeded (`packages/ingest/scripts/seed-demo.ts` — Shopyard estate). UI overhauled: mono black&white shadcn theme + collapsible sidebar + `(app)` route group (persistent shell + `loading.tsx`) + `/settings` + `/dashboard`. shadcn CLI wired (`apps/web/components.json`). **Working org is now *Siemba*** (real Bitbucket workspace connected; renamed from the seeded *Acme Verify*).

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| P1.1 | Dashboard metrics (`GET /overview` + rich dashboard) | ✅ | 09 §5.2 | node/edge counts, kinds, certainty mix, sources, last-sync |
| P1.2 | **Onboarding wizard** (empty-state: choose source + instructions + **"Load sample data"** for TTFI<30min) | ✅ | 09 §8 | Empty graph → the onboarding first-run experience. **Backend:** shared `seedDemoData` extracted into `@atlas/ingest` (`demo-estate.ts`) — the Shopyard estate + org-scoped (atlas_app/`withOrgScope`, no admin) seeder driving the real MockConnector→runStagedSync→runInference; the CLI `seed:demo` script + new gated `POST /demo/seed` API both call it (DRY). `DemoModule` (Admin-only, `TenantScopeGuard`) gates to empty orgs (409 if a real source exists), idempotent by URN. **Frontend:** `Onboarding` — "Load sample data" primary path (client button → `/demo/seed` → `router.refresh`, admin-gated via `shell.role`) + AWS/GitHub setup instructions (real least-privilege IAM policy + External-ID steps / App read-scopes, copy-ready). **Repeatable UI system** (per user): extracted reusable primitives — `patterns/EmptyState`, `patterns/Steps`, `ui/CodeBlock` (+ shadcn `tabs` via CLI) — so onboarding composes the system, not bespoke markup. **Verified live:** shared seeder end-to-end on live DB (36 nodes, 29 observed + 12 inferred edges, sync succeeded); API boots with `DemoModule`, `POST /demo/seed` mapped + 401-guarded; web dev serves (/login 200, /dashboard 307). All `pnpm run check` gates green (17 tasks). Also added app-router `not-found.tsx` + `global-error.tsx` (designed error states). |
| — | **`next build` fix** (env, not a task) | ✅ | 17 | Root-caused the repo-wide `next build` failure: `.env` sets `NODE_ENV=development`; sourcing it before `next build` made React use its **dev** SSG renderer → null dispatcher → `useContext` null on prerender → misleading `<Html> should not be imported` on `/404`/`/500`. **Not** a version/our-code bug (a zero-hook component reproduces it). Fix: `apps/web` `build` script → `NODE_ENV=production next build` (overrides the ambient dev value). Build now green (11 routes) with the dev `.env` still sourced. See [[reference-local-dev-runbook]]. |
| P1.3 | State polish (empty/loading/error/honest-absence consistency) | ✅ | 09 §7 | Converged the four designed states onto the shared pattern library. Extended `EmptyState` with a `tone` (default/danger) + added `ErrorState` (the one "couldn't load/compute" surface). Applied across: Explore nodes-list empty (`SearchX`), Explore load-error, impact-panel compute-error (bare, in-card), settings connected-sources empty (`Plug`). Added route-specific `explore/loading.tsx` skeleton (table-shaped, matches the page vs the generic app skeleton). **Color normalize:** error text/badges now all use the semantic **`danger`** token — the mono theme defines shadcn's `--destructive` as *grayscale*, so the login/create-org form errors were rendering gray (easy to miss); switched them to `danger` (red). Ask chat's inline error aligned to `danger` too. Honest-absence (impact "truthful empty result"; Ask `insufficient` "I don't know") kept + consistent. All gates green (17); prod build 5/5 static, 11 routes. |
| P2 | Hardening: load/perf (NFR), security pass, observability, DR drill, mutation tests + adversarial QA agent in CI | 🔵 | 14, 13, 17 | **First increment DONE (in-repo, testable):** (1) **Observability baseline** (docs/02 §9.4) — request correlation id (Fastify `genReqId` reads `x-request-id` or generates; echoed in the response header), a structured `LoggingInterceptor` (one JSON access line/request: `{requestId,method,url,status,durationMs}`, no secrets), and the error envelope now reuses the same `requestId` (logs↔responses↔client correlate). (2) **Audit log** (docs/13 §8) — migration 0014 `audit_events`, **append-only** (atlas_app INSERT+SELECT only; UPDATE/DELETE REVOKEd) + org-scoped RLS; `AuditService` (global, best-effort — never breaks the op) wired into the security-relevant mutations: org.create, member.role_change, member.remove, invitation.create/revoke, connection.verify/disconnect, demo.seed. 4 live-PG tests (record+read, tenant-isolation, append-only rejects UPDATE/DELETE, best-effort swallow). (3) **CI** — new `web-build` job (production `next build`, catches build/NODE_ENV regressions); audit test runs in the existing `integration` job. (4) **Audit read path (trust is visible)** — `GET /audit` (Admin+, org-scoped, actor email joined from `users`) + a Settings **Activity log** card (admin-only, human-labeled actions + relative time); `AuditService.list` + 5th live-PG test (newest-first ordering, actor join, limit clamp). (5) **Security headers** — baseline `x-content-type-options: nosniff` / `x-frame-options: DENY` / `referrer-policy: no-referrer` on all API responses (dependency-free; HSTS at the TLS edge in deploy). **Remaining (infra-bound, still ledgered):** load/perf tests, DR drill, mutation tests, the adversarial QA agent + API-contract/E2E/AI-eval CI stages, connection purge-on-delete. |

**Exit (M6–M7):** NFR targets met; security suite green; DR drill passes.

---

## EPIC GA — 🚀 Launch — 📋 TODO

| ID | Task | Status | Docs | Note |
|---|---|---|---|---|
| GA | Closed beta → fix activation → security package → GA | 📋 | 18, 13, 15 | design partners validate trust ≥90% |

---

## 🧭 Deferred during MVP build (IN-SCOPE — revisit, most in Sprint P2 / deploy)

> These are **intentional deferrals inside completed sprints**, not order violations. The binding dependency order (F1→F2→I1∥I2→G1→G2→G3∥G4→P1→P2→GA, A60) is intact. Tracked here so nothing is silently lost.

| Item | From | Where it lands | Doc |
|---|---|---|---|
| **Worker process + Redis/BullMQ** — queue/worker code exists but no running worker; API's `InMemoryQueue` only buffers, so enqueued syncs don't execute yet | F2.5 | deploy wiring (before live sync) | 02 §5, 17 |
| **Scheduler** — per-connection incremental/nightly cadence, leader-elected singleton, periodic `health()` re-check (FR-1.9), GitHub periodic reconcile (DD-2) | I1/I2 | with worker process | 06 §11, 07 §5, 02 §5.4 |
| **Live connector verification** — AWS read-only IAM role + GitHub App install | I1/I2 | when customer creds exist | 06 §2, 07 §2, 13 |
| **AWS additive services** — ✅ **service modules DONE** (DynamoDB, ElastiCache, API Gateway — pure normalize/edges/signals + 6 golden tests; `services/additive.ts`). IAM-policy intentionally not a browsable node (NG6) — its statements already feed R8 via the IAM-role signal. **Remaining: live discoverers** for the 3 kinds (fetch layer, needs creds to verify) | I1.3 | discoverers with creds | 06 §9 |
| **GitHub gaps** — ✅ **webhook ingress route DONE** (`POST /webhooks/github/:connectionId`, HMAC-verified, → org resolve via `app_connection_org` SECURITY DEFINER → incremental sync_run; 4 live-PG tests). **Remaining:** team→member resolution (US-10); IaC (TF/CFN) ref parsing; more manifest ecosystems (have npm/pypi/go); multi-branch structural parse | I2 | additive | 07 §5/§7 |
| **OpenSearch hybrid search** — search runs behind `SearchProvider` with a Postgres (pg_trgm keyword) impl; the OpenSearch BM25+kNN driver + embedding/index pipeline + semantic ranking are the deploy target | G2.4 | deploy / P2 | 11 §3–§8 |
| ~~**audit_events table + audit log**~~ — ✅ **DONE (P2)**: migration 0014, append-only + org-scoped `AuditService` wired into org/member/invitation/connection/demo mutations | F1 | ~~P2~~ done | 13 §8, 04 |
| ~~**Connection purge-on-delete**~~ — ✅ **DONE**: `disconnect` now purges the source's nodes/edges/signals/snapshots + orphaned derived nodes + unreferenced provenance (org-scoped tx; composite-FK cascade), keeping the soft-deleted row for history. 2 live-PG tests | F2.8 | done | 03, 04 |
| ~~**Observability baseline**~~ — ✅ **DONE (P2)**: correlation id (`x-request-id`) + structured JSON access logs + error envelope reuses the requestId. Metrics/tracing exporters remain for deploy | F1.3 | ~~P2~~ done (exporters at deploy) | 02 §9.4 |
| **Adversarial QA agent + API-contract/E2E/load/mutation CI stages** — CI is format/lint/typecheck/test + PG-RLS integration only today | F1.2 | P2 (+ when PRs start) | 14 §7–13 |
| **Interactive graph canvas** — ✅ **DONE**: the **`/map` infrastructure map** (React Flow + dagre) renders resources + connections framed into environment lanes, click-to-inspect, env filter, certainty-coded. (Explore stays list-first for a11y; the map is the visual view.) | G4.3 | done | 09 §5.4 |

---

## 🗂️ Planned deep-dives (captured feature plans, not yet scheduled)
- **⚡ Perceived-performance pass (product-owner concern, 2026-07-10)** → **`docs/plans/performance.md`** (measured + planned). **Root cause found & first fix shipped.** Measured: Supabase PG is in **Sydney (ap-southeast-2)**, ~**137 ms per DB round-trip** from the dev laptop; cost = (sequential round-trips) × RTT. `withOrgScope` was **4 hops** per scoped op (BEGIN/set_config/query/COMMIT = ~610 ms for one read) → **✅ folded BEGIN+set_config into 1 hop** (RLS verified intact). **Next (P0)** the dominant *prod* lever = **co-locate the ECS API in ap-southeast-2** (turns 137 ms hops into ~1–3 ms). **P1** consolidate multi-`withOrgScope` endpoints + batch reads (measured 170 ms vs 610 ms for one round-trip); **P2** de-dupe the per-page `/me` + client cache (SWR); **P3** Suspense streaming; **P4** prefetch + bundle split. Much of the *dev* lag is on-demand compilation (gone in prod) — measure a prod build before attributing more to the app.
- **🔒 Security & QA leak/hollow sweep (2026-07-10, requested)** → **`docs/plans/security-qa-sweep.md`** — full audit for where the system is leaky or hollow (tenant isolation/RLS, auth/session, injection/validation, read-only P2, secrets, storage-bucket exposure, abuse/rate-limits, error info-leak, test-coverage gaps). Findings recorded with severity + location + fix so we can start fixing on return.
- **Operational Intelligence (health → trace → advise)** → `docs/plans/operational-intelligence.md` — **the product owner's north star (2026-07-05)**: map turns red when things break; AI traces alarm → service → deploy → PR diff to ranked, cited root-cause hypotheses; personalized posture/advisory rules. Phases: **A** deploy inference (repo→service, the keystone code↔infra edge) ∥ **B** health layer (CloudWatch/target-health/ECS + 1-2min health poll) → **C** change timeline (CloudTrail + deploys + PRs in `node_events`) → **D** AI incident tracing (diagnose + get_pr_diff tools in the KE-P1 loop) → **E** posture & advisory. All read-only/agent-less (P2); RCA = cited hypotheses, never oracle claims.
- **Security & Vulnerability Intelligence** → `docs/plans/security-vulnerabilities.md` — "exposed AND vulnerable" toxic-combination over code+cloud; free OSV path first (manifest parsing → OSV → CVE nodes), cloud-posture rules on the AWS graph. Steps 1–2 buildable on Bitbucket today; toxic-combo needs AWS. Strong Siemba fit.

## ⏸️ Backlog (Phase-1+ — deferred, from roadmap `docs/15` §8)
- v1.1 Trust & Depth: richer inference, culprit-PR ranking (US-6), saved views/deep-links
- v1.2 Enterprise on-ramp: multi-account AWS, **domain auto-join (`hd`)**, real-time CloudTrail ingestion
- v1.3 Enterprise security: SSO/SAML+SCIM, custom RBAC, SOC 2 Type II, data residency
- v2.0 Breadth: GCP/Azure, **Bitbucket (`07b`)/GitLab**, Datadog/PagerDuty
- v2.x Scale: dedicated graph DB (when `05` DD-3 trigger met), partitioning
- v3.0 Platform: proactive alerts, incident root-cause, **MCP/public API**, connector marketplace

---

## 📜 Activity Log (append newest at top; one line per work session/event)

| Date | Who | What |
|---|---|---|
| 2026-07-11 | architect | **🚨 War Room — v2 polish + proper flow (iterated on live feedback).** Turned the v1 into a real incident surface: (1) **dedicated map** (React Flow, reuses ResourceNode icons/health) showing just the node + **dynamically-revealed** blast radius — nodes appear + **connections turn red** as the trace walks them; the **actual cited edges** are drawn thicker red as the culprit path. (2) **investigation is a live AI chat** — auto-diagnosis first turn, then **ask follow-ups** in the same conversation; the first turn is elevated into a **"Likely cause" card**; steps collapse to an accordion. (3) **markdown verdict** (headings/bold/lists/code + citation chips) not raw text. (4) **context bar** (health/type/env/blast-radius/last-change) + **real change timeline** (`node_events`) — reads as a dashboard. (5) **Resources traced** chips with real provider logos. (6) **Flow fixed** — incidents are their own operational surface: new **"War Room" nav item** (Crosshair) + ⌘K + `/war-room` home (active/closed), **primary entry = a red map node's "Take to War Room"** button, finding entry secondary; moved "Past incidents" out of Insights. All typecheck/lint/web-build green across iterations. Follow-up: structured verdict classification enum (currently the model's cited narration); log-onset waits on the AWS grant. |
| 2026-07-11 | architect | **🚨 War Room v1 SHIPPED — the face for Phase D incident tracing.** A "Take to War Room" button (on an Insights finding) opens a page that shows the broken node + its **blast radius on the live map** (reusing the map's `focusId` highlight) beside a **streamed, cited investigation** — every step is a *real tool call* from the existing agentic diagnose loop (no theater), rendered as it runs, ending in a cited verdict + confidence. **Persisted + revisitable:** new org-scoped `incidents` table (migration 0050, RLS + composite FK, one live incident/node), `IncidentsService`/controller (open idempotent · get · list · update); the transcript is saved on completion so a reopened incident **replays** instead of re-running; a **"Past incidents"** list on Insights. Reused the entire streaming stack (`streamAsk` + `retrieval_step` events + the map's health-lens/blast-radius) — recon #4 confirmed the loop already emits per-step tool events, so this was assembly, not new AI plumbing. Actions: Mark resolved / Dismiss. **Guardrails honored:** real steps not spinners, ranked cited verdict (model already produces "no culprit" honestly), fixes stay advisory (deep code = SIFT). Web build green (`/war-room/[id]` 6.3 kB), api/typecheck/lint clean. **Buildable-now proof:** works today on the diagnose engine, no AWS grant needed. **Follow-ups:** map-node entry button (avoided infra-map surgery this pass), structured multi-hypothesis verdict UI, ECS deploy events, log-onset enrichment (waits on grant). |
| 2026-07-11 | architect | **🛰️ Phase D re-scoped (already built!) + deploy events shipped + 🆕 War Room epic captured.** Recon #3 (saved a full rebuild): **Phase D — AI incident tracing — is already built** — `diagnose` + `get_pr_diff` tools are wired into the agentic loop and tested (golden-incident + adversarial quiet-window honest-absence); CloudTrail `config_change`, health transitions, and derived `pr_merged` all flow through `node_events`. **The real gap: `kind='deploy'` events were never written** — yet a deploy right before an incident is *the* correlation signal, and diagnose ranks by time×proximity. **Shipped `deploy-events.ts`** (permission-free): `lambdaDeployEvent` (pure, tested) maps a Lambda's `LastModified` → a `deploy` node_event, `deriveDeployEvents` persists per-function, idempotent by `deploy:<urn>:<timestamp>` (redeploy = one new event); wired as a best-effort post-sync stage. ingest tests green. **ECS/other runtimes slot in the same way** once their deploy timestamp is captured (follow-up). **🆕 `docs/plans/war-room.md`:** the presentation layer for Phase D — a "Take to War Room" button (from a red map node / finding / alert) opens a page with the node + **live blast-radius map**, **streams the real diagnosis steps** (parallel lanes when the loop fans out, each cited — NOT theater), a **ranked verdict** + advisory fix, and **persists to Insights** (new `incidents` table) so investigations are revisitable. Buildable NOW on the existing diagnose engine (permission-free); sharpens when logs/onset land. Deep code-fixes stay SIFT's job. |
| 2026-07-11 | architect | **✅ Op-intel Wave A COMPLETE (2/2 fixes) + spine re-scoped from live code.** Shipped the two fold-in fixes: (1) **duplicate-tier collapse** — the map showed the same `(from,to,type)` edge twice at conflicting tiers because `uq_edge` includes `inference_rule_id` (R1/R12/R17 each persist their own `DEPLOYS_TO` row); the map read now collapses to ONE edge per pair at the strongest confidence (observed>inferred-high>low>ai-suggested), non-destructive (all rows stay as provenance, P3). (2) **AI-suggested-edge reconnect bug** — a retired suggestion never came back on re-sync: the active-only existence check passed, then `ON CONFLICT DO NOTHING` collided with the soft-retired row; now **revive-on-conflict** (safe — rejected pairs live in `edge_suggestion_rejections` and are pre-filtered). api 53 green, typecheck/lint clean, committed+pushed. **🔭 Spine re-scoped by reading the live code (recon discipline, twice-saved from rebuilding):** **Phase A ✅** (repo→ECS was already high-confidence; R17 added Lambda). **Phase B (health / map turns red) ✅ ALREADY BUILT** — `health-collect.ts` (ELB target health / ECS running-vs-desired / RDS status / CloudWatch alarms) + `health-poller.bootstrap` + `health_targets` (0023); the map node DTO carries `health` and the web map has a **health lens** that lights broken nodes. **Phase C (change timeline) PARTIAL** — `cloudtrail-collect.ts` exists; needs verification it's wired to a per-node timeline. **Phase D (AI incident tracing) = the real net-new** (onset detection, on-demand Logs Insights, `diagnose` tool, ranked verdict) — needs the logs IAM (~2 days) + careful design/eval. **Phase E** mostly built via security. **So the north-star's remaining work is C-verify → D.** |
| 2026-07-11 | architect | **🛠️ Op-intel Wave A (1/2) — Lambda commit-provenance shipped (`R17`).** The code↔infra bridge's Lambda gap: repo→ECS was already high-confidence, but 20 Lambdas (the live mass here) had only name-tier links. Now **`R17 lambda_commit_provenance`** (sibling of R12) derives `DEPLOYS_TO(repo → lambda)` by matching a git SHA found on the function — its container **image tag**, its **Description**, or a **tag value** — by prefix to a crawled PR commit → `inferred-high` (single repo) / `inferred-low` (multi, P3). **`CodeSha256` deliberately ignored** (it's a zip hash, not a git SHA — matching it would fabricate edges). Connector: Lambda module now captures `description`/`version`/`codeSha256`/`lastModified`/`packageType` (free from ListFunctions) + `imageUri` via a **single `GetFunction` per container Lambda** (graceful on denial); new **`lambda:GetFunction` posture probe** so the permission gap surfaces honestly on the hub/compliance (added to the docs/13 read-only policy). migration **0049** seeds the rule; registered before R4. **Tests:** 7 new R17 unit tests (description/image-tag/tag SHA → high; multi-repo → low; CodeSha256 not matched; no-SHA → none) — inference 86 green, connector-aws 84 green, typecheck/lint clean. Noticed `cloudtrail-collect`/`health-collect` tests already exist → **Phases B/C have prior groundwork** (check before building). **Next in Wave A: duplicate-tier collapse + AI-suggested-edge reconnect bug; then Wave B (health).** |
| 2026-07-11 | architect | **🧭 Operational-intelligence design nailed down (pre-build) + 🆕 Intent-Verification epic captured.** Worked through the hard questions before building. **Op-intel decisions** (recorded in `docs/plans/operational-intelligence.md` → new "Code context" + "Decisions" sections): (1) **we don't vectorize the whole codebase** — retrieval is *targeted*, driven by the error's own stack trace; layered L0 structural (PR↔files) → L1 fetch the blamed files **at the deployed SHA** → L2 symbol index (later) → L3 embeddings (fallback only for trace-less cases). The model brings comprehension; Atlas assembles the evidence bundle. (2) **Onset vs chronic** via error-signature fingerprinting (first-seen + rate) — answers the "5-year-old silent bug": new-signature-after-deploy = clean attribution; old signature = flagged "not new", reframed not blamed. (3) Attribution is a **ranked, classified verdict** (`code/config/dependency-correlated | chronic | unknown`), never "the last PR". (4) **Deploy-provenance (git SHA) is auto-probed from ECS image tags / Lambda metadata, not asked**; `:latest` → repo-level only, said out loud. (5) **Logs on-demand at incident time for v1**, continuous poll (15/30/60m) later; logs IAM (`logs:StartQuery`/`FilterLogEvents`, `cloudwatch:GetMetricData`) is **beyond SecurityAudit** → separate grant. (6) **v1 scope = acute-onset correlation**; chronic reframed honestly, latent-bug *discovery* explicitly out. **Silent-error taxonomy:** logged-but-not-crashing ✅ (logs layer) · unlogged-metric-visible 🟡 (later) · unlogged-no-signal ❌ (invisible to runtime — needs intent verification). **NEW epic → `docs/plans/intent-verification.md`:** spec→code — link PR↔Jira (even without the key: branch/temporal/semantic, tiered + confirm/reject), read story+subtasks+**comments**, judge whether the intent was implemented (hedged reviewer *questions*, cited to AC+diff, own adversarial eval). Needs a **Jira connector**; Phase-2+, own feature, captured not started. **Division of labour recorded: the deep code-logic review = SIFT** (product owner's friend's product, already on the platform as "Coming soon", Bitbucket-integrated) — we USE SIFT, don't rebuild code review; Atlas owns intent linkage + coverage; likely bidirectional integration when SIFT lands (TBD). **`DEPLOYS_TO` bridge must be runtime/cloud-general** (compute_target + artifact + provenance-witness abstraction; ECS(+Fargate)/Lambda/EC2/EKS/App Runner + Azure/GCP all "add an extractor", tiered by how knowable the artifact is; `unknown` first-class) — recorded in op-intel Phase A. **RECON (Explore agent): op-intel Phase A is ~80% already built** — pipeline-yml fetch + parse + `bitbucket.pipeline.deploy` signal + R1 `DEPLOYS_TO` (ECR-image chain) + R12 image-tag-SHA all exist; genuine gap = **Lambda artifact-probing** (capture `CodeSha256`/`Version`/`Description` via `lambda:GetFunction`, then a rule matching embedded SHA → PR commit). **Next: verify/harden the existing repo→ECS bridge + build Lambda SHA-probe; then CloudWatch health/logs read.** |
| 2026-07-11 | architect | **✉️ Weekly digest email — BUILT (#44, first email of the strategy).** A per-org posture summary on the existing Resend plumbing: `WeeklyDigestService.buildDigest` reuses `GraphService.summary()` so the email shows the *same* cited findings/posture/inventory as the dashboard — top-6 findings each linking to `/insights/<id>`, open-count by severity, averaged posture score, and an honest "all clear" state (no fabricated problems, P3/P4). **Delivery** via `EmailService.sendWeeklyDigest` (new branded `list` block in `emailShell` — colored severity dots). **Recipients** `app_weekly_digest_recipients()` (SECURITY DEFINER, cross-org: active, non-opted-out members of orgs *with* a connection — no empty-org mail). **Scheduling** `WeeklyDigestBootstrap` ticks **hourly** (a 7-day `setInterval` never fires — deploys reset it); once past the week's send moment (Mon 14:00 UTC) it claims the week via `app_claim_digest_period` (atomic ON CONFLICT) so it sends **exactly once/week across instances+restarts**, and a late boot still catches up. **Opt-out** `memberships.digest_opt_out` + a one-click HMAC-signed unsubscribe (`/unsubscribe` page POSTs on mount — link-scanners doing GET can't unsubscribe by accident → `POST /email/unsubscribe`, token *is* the auth → `app_digest_unsubscribe`). Migrations **0047** (opt-out + recipient/unsubscribe fns) + **0048** (`digest_runs` ledger + claim fn), both applied live. Docs: new `docs/plans/email.md` (full email set + triggers + the digest design). Typecheck/lint/tests green (api 53). **Remaining #44 (designed, next):** per-user notification prefs + immediate security emails (new-critical-finding, compliance-drift) + ops (sync-failed). |
| 2026-07-11 | architect | **🃏 Ask-Atlas inline resource cards + ✉️ email plan decided.** Replaced the small "Sources" chip rail in Ask Atlas with a **"Referenced resources"** grid of rich cards (real provider/kind icon, name, kind · region · env, health dot, click-to-peek) — `getNode` already fetched the node, so we keep the full detail. Pairs with the markdown block renderer + uncaged narrator. **Email (#44) decision:** the moments worth emailing are new-critical-finding / exposed+vulnerable / compliance-drift (security), sync-failed-degraded / first-sync (ops), **weekly digest** (engagement), + account/team events. **User picked: build the WEEKLY DIGEST first, others later.** Per-user prefs (immediate/digest/off) + link-to-finding + honor finding-trust (no false alarms). |
| 2026-07-11 | architect | **🔐 Phase 2b finished (CloudTrail) + 🎯 finding-trust pass.** (1) **CloudTrail audit-logging** control — the account discoverer now also checks for a multi-region trail actively logging (CIS 3.1), modeled as `aws.account.cloudTrailEnabled: true/false/null`; the `no-cloudtrail` finding fires ONLY on known-false (never null → no false-fire when denied — the absence-handling I'd deferred), with a `cloudtrail:DescribeTrails` posture probe for the permission. Phase-2b core is now complete: framework + root-MFA + public-S3 + encryption + CloudTrail; encryption-in-transit / flow-logs / per-user-MFA are the tail (shown honestly as "not yet crawled"). **Built before AWS perms land** — each control shows "grant X" until the user attaches SecurityAudit (~2 days), then lights up. (2) **Finding-trust pass** (after the SG=443 fix): `iam-wildcard` now excludes AWS service-linked roles (wildcard by design, not customer-actionable); `rds-single-az` excludes dev/test/staging DBs (the finding claims "production" but flagged all). Validated live — no legitimate finding dropped. |
| 2026-07-11 | architect | **🤖 Atlas AI — made genuinely smarter + better presentation (user: "felt like a wrapper").** Root cause (confirmed by a full pipeline audit): the AI is on Opus but the main narrator prompt was a **cage** — "use ONLY the context, not a general assistant, never use outside knowledge" — so for most questions it could only *restate graph facts*, never reason; only the advisory path could think. Also: no knowledge of Atlas itself, and a 3-token-regex renderer (bold/code/citations) that made every answer a text blob. **Fixes:** (1) rewrote the narrator (`atlas-narrator@5`) with an explicit **facts-vs-interpretation** contract — facts about their estate stay graph-grounded + cited (honesty preserved), but reasoning, implications, tradeoffs, and general AWS/security knowledge are now the model's to give freely (Opus had the knowledge; the cage forbade it); (2) added an **ATLAS_CAPABILITIES** self-knowledge block so it can guide users to the Map/Explore/Insights/Compliance; (3) a real **block renderer** (tables, lists, headings) reusing the citation-aware inline renderer, so structured answers finally look structured. 90 ai tests green. **Noted lever:** extended thinking (off today; latency tradeoff — add with an eval). Also: **README** rewritten (was stale "code not started") — logo, feature matrix, architecture, quickstart. |
| 2026-07-11 | architect | **🔐 Security Phase 2b STARTED — permissions-visibility framework + first crawl slice (root MFA).** (1) **Framework (the centerpiece):** compliance assessability is now per-org + dynamic — each capability maps to its required read-only IAM action(s); `ComplianceService` reads the org's denied actions from `connections.health.missingPermissions` and marks a control `not-assessable` for a DISTINCT reason: *"not crawled yet"* (roadmap) vs *"grant IAM action X"* (permission gap, with the exact actions as code chips + the `SecurityAudit` shortcut). (2) **First vertical slice — AWS account security:** new `aws.account` node (global discoverer via `iam:GetAccountSummary`) captures root MFA / root access keys / password policy; new `root-no-mfa` finding (HIGH); the `access.mfa-privileged` control flips to assessable. Because the discoverer declares its own `iamAction`, a missing permission surfaces cleanly on both the Integrations hub and Compliance. migration 0046, docs/13 policy updated, 84 connector tests green. **Remaining Phase-2b (same pattern):** S3 public-access, RDS/EBS/S3 encryption, CloudTrail/flow-logs. **User todos added:** (a) **Atlas AI** needs to be smarter / more production-grade + better data presentation; (b) a **polished GitHub README** (logo + all). Also open: finding-trust pass, email strategy (#44), operational-intelligence C/D. |
| 2026-07-11 | architect | **🛡️ Reliability hardening — audit + 5 fixes (P2).** Full concurrency/security audit (confirmed much is already solid: rate limiter atomic, sync single-flight via unique index, GUC no-leak, tenancy strong). Fixed: (1) **outbound HTTP timeouts** everywhere (`fetchWithTimeout` in connector-sdk → Bitbucket/GitHub/Jenkins/OSV; 5-min LLM deadline) so a hung socket can't stall the worker while it holds a DB tx; (2) **inference upsert race** — reindex-during-sync hit `uq_edge` (23505) and rolled back the run → per-org `pg_advisory_xact_lock` + `ON CONFLICT DO UPDATE` (verified: 2 concurrent runs serialize); (3) **rate-limited expensive endpoints** (reindex/suggest-edges/search/summary/insights/compliance); (4) **per-user AI burst** (was per-org) threaded through SSE+WS; (5) **background sync reaper** (`app_reap_orphaned_syncs`, migration 0045) + **compare-and-set finalize** (no run resurrection). **Deferred #6** (summary fan-out cap) — deliberate parallelism, backstopped by the heavy_read limit; real fix = read replica. |
| 2026-07-11 | architect | **✨ UI polish.** Compliance page reviewed (security/consumer/UX) + rebuilt to the system's vocabulary (Card table, segmented filter, semantic tokens), then **severity-weighted + attention-first** (lead with failing-count & high-severity, rank fails high→low + severity chip, not-assessable → quiet collapsible). Sidebar icons (Map=Network, Explore=Compass, Insights=Radar, Integrations=Blocks). Sift page staggered entrance animation. |
| 2026-07-11 | architect | **⚖️ Compliance — technical-controls monitor across 6 frameworks BUILT (v1).** Maps Atlas's technical evidence onto the **infrastructure-observable subset** of PCI-DSS, CIS/AWS-FSBP, NIST 800-53, ISO 27001, HIPAA, GDPR. **One shared control catalog** (13 controls) — each maps to the many framework IDs it satisfies at once, and fails when its backing finding is open (world-open SG, wildcard IAM, known vulns, exposed+vulnerable, single-AZ, no-CI). **Honest by construction (P4):** four states incl. an explicit **`not-assessable`** for data Atlas doesn't crawl (encryption, public-S3, audit-log, MFA) — never a silent pass — + a per-framework coverage caveat so it never implies certification (the not-assessable set = the connector roadmap). Pure catalog + eval in `@atlas/ai` (`compliance.ts`, 6 tests); `GET /compliance` (`ComplianceService` reuses `summary()` + inventory); `/compliance` page (framework tabs, coverage band, controls with mapped IDs + evidence links) + sidebar nav. `docs/plans/compliance.md`. **Next:** close not-assessable gaps via connector (each flip lights up several controls). |
| 2026-07-11 | architect | **🖱️ Exposed-vulnerable finding click-through wired + map Exposed lens.** The dashboard "internet-exposed vulnerability" finding now opens `/map?lens=exposed` — a new Exposed lens (appears when anything is exposed) that lights exposed compute and dims the rest; deep-linkable, threaded through the server page. Per-resource + blast-radius drill-down stays on `/insights/[id]`. |
| 2026-07-11 | architect | **🔐 Security Phase 2 — "exposed AND vulnerable" (toxic combination) BUILT.** The finding that matters (reachable > buried): an internet-exposed resource running known-vulnerable code. Exposure is now a first-class, cited graph fact. **Built:** (1) ELB `aws.elb.targetgroups` signal (read-only connector addition — ARNs already fetched); (2) **R15 `alb_routes_to_service`** — recovers ALB→ECS-service `ROUTES_TO` by shared target-group ARN (the missing routing link); (3) **R16 `internet_exposure`** — derives `EXPOSED_VIA(resource→sg|elb)` from world-open SGs (`0.0.0.0/0`) and internet-facing LBs, precision-first (P3 — public IP/subnet/S3 NOT inferred, uncrawled = Phase 2b); (4) dashboard headline finding **`exposed-vulnerable`** in `summary()` walking CVE→pkg→repo→DEPLOYS_TO→resource→EXPOSED_VIA (critical/high only, evidence-cited); (5) Ask-AI guidance `reachable vulnerability`; migration `0044_exposure_seed`. **Verified live on Siemba:** ran inference → 3 `EXPOSED_VIA` edges created (OpenVPN via world-open SG; report-server + Jenkins via internet-facing ELBs — incl. a "private"-named ELB that's actually internet-facing). 8 new rule unit tests pass; full inference suite green (79). **Needs an AWS re-sync** to populate the new ELB signal so R15 lights up the ECS path (Siemba's vulnerable repos deploy to ECS); the toxic finding then fires. docs/05 (R15/R16 + `EXPOSED_VIA` edge + calibration), plan doc (Phase 2 → built), knowledge pack updated. **Map visuals shipped too:** exposed compute now shows an orange **"Internet-exposed"** chip (Globe) on the map — EXPOSED_VIA rendered as a node-level fact (kept off the canvas as an edge, since it would merge with the PROTECTS line). Findings surface on **/insights** (Security pillar, "Reachable vulnerability"), **/dashboard** (Needs attention + posture), and **/ask**. |
| 2026-07-11 | architect | **🗺️ Map ECS-visibility — REAL root cause found + fixed (correction to the entry below).** The stale-client / truncation theories were wrong: a hard refresh still stranded `API Backend - Provapt` in the *no infra link* shelf. Traced it fully — the DEPLOYS_TO repo→`calsaws-backend-api-service` edge is active with both nodes active, and Siemba has only 161 visible nodes (no truncation). The bug is client-side: the map **default-collapses every container**, and `hiddenSet` hides a collapsed container's `CONTAINS` descendants. An **ECS cluster CONTAINS its services**, so both `ecs.service` nodes were hidden on first paint — severing the repo→service `DEPLOYS_TO`, the service→taskdef `USES`, and the cluster→service `CONTAINS` edges at once, dropping the repo to the code shelf and the task-defs + cluster to the infra shelf (matches the screenshots exactly). **Fix:** a node that participates in the **flow** (any non-containment edge) is a first-class map node and is never listed as a collapsible child, so folding its container can't hide it. Extracted the rule to a pure `containmentChildren()` helper in `map-layout.ts`. (Note: web has no vitest harness, so no unit test landed — verified by full DB trace + typecheck.) The edge-aware node budget + Refresh control from the entry below are valid hardening but were NOT the cause. |
| 2026-07-11 | architect | **🗺️ Map ECS-visibility — diagnosed (stale client) + fixed the real latent bug in the same area.** Root cause of "I only see Lambda links, my ECS repo sits in the *no infra link* shelf": the map holds its server-rendered snapshot and never refetches, so a view loaded before the AWS sync/R14/USES edges finished was **stale** — the live `/graph` query already returns all links (verified: 161 nodes incl. both ECS + all 5 EC2; deploy edges 23 Lambda + 4 ECS + 4 EC2, every endpoint in the map set). **Fix 1:** a **Refresh** control on the map (`router.refresh()` via `useTransition`, spinner) so a sync/Rebuild-links/Find-AI-links result appears without a full reload. **Fix 2 (the real bug):** the estate-map node budget took the `limit` most-recently-seen nodes and kept only edges with *both* endpoints inside — so on any estate >budget, an older repo (not re-synced this cycle) drops past the cut, its `DEPLOYS_TO` edge is dropped, and it's **falsely** reported unlinked. Made the budget **edge-aware**: pull the missing endpoint of any touching edge back into view (bounded by the over-fetch cap) so a repo + its runtime are never split (P3/P4 — never hide a real observed link for a display budget). New live-PG regression test (budget=1 still returns both neighbours + both edges); `docs/08 §9` updated. Siemba (161<400) unaffected today; the fix prevents the silent false-unlinked as estates grow. |
| 2026-07-11 | architect | **🔗 Repo→runtime linking, 3 phases (reconnect fix + EC2 coverage + AI-suggested edges) — all shipped.** Root-caused why a repo→Lambda edge vanished after a Bitbucket disconnect/reconnect: inference DOES re-run globally on reconnect (not the gap); the edge only stays gone if the resync didn't finish clean (infer skipped on `failed`) or its signal wasn't regenerated. **Phase 1:** `POST /connections/reindex` + an Admin **"Rebuild links"** button (Integrations) re-runs inference over the current graph with no re-crawl — a safe recovery lever + diagnostic. **Phase 2:** extended R11 to match the EC2 **`Name` tag** (always `inferred-low` — a display label, weaker than a code tag), the main repo→EC2 bridge over data we already crawl (no new IAM); 5 tests, mutation gate held (62.65%). **Phase 3 (the big one) — AI-suggested edges:** migration 0041 adds `origin='ai_suggested'`/`'confirmed'` + `confidence='ai-suggested'` + `edge_suggestion_rejections` (edges outside the retire-pass lifecycle); a pure `@atlas/ai matchEdges` reasons over unmatched runtimes + candidate repos (strict JSON, validated vs real URNs so no hallucinated edge, P4); `POST /ai/suggest-edges` (Admin, **"Find AI links"** button) generates them; the graph renders them **cyan + dotted** with a legend, and the edge **"why?"** panel shows the model's reasoning + **Confirm** (→ `confirmed`, promoted high) / **Reject** (→ delete + never re-propose). Needs a real model (BYO-LLM or ANTHROPIC_API_KEY). docs/05 §6 + confidence model updated; captured in memory ([[project-ai-assisted-edge-suggestions]]). **Future (noted):** Phase 2b EC2 bridges needing new IAM (ASG/launch-template/user-data). |
| 2026-07-11 | architect | **✉️ Email: production-grade templates + real deliverability wired.** Diagnosed why invites never arrived — Resend's default `onboarding@resend.dev` only delivers to the account owner's own address (403 for anyone else); a verified domain is the only fix. User added `anmolbhardwaj.com` to Resend; set `EMAIL_FROM="Atlas <atlas@anmolbhardwaj.com>"` (works once DNS verifies). Rebuilt the invite + welcome HTML to production quality: bulletproof VML Outlook button, full dark-mode, mobile, hidden preheader, real detail block (org/role/invited-by/expiry) + copy-link fallback. Invite flow now reports the REAL send result ("emailed ✓" vs the honest fallback). Also: **delete-organization** (Owner-only, 2-step type-to-confirm, single cascade sweep — all org tables are `ON DELETE CASCADE`; 1 live-PG test) + **⌘K Sift/Shift** + **org-switch loader** earlier. New todo #44: define the full transactional-email set + triggers. |
| 2026-07-11 | architect | **🧭 Onboarding v2 — 2-step org creation with profile questions + product-analytics pipeline (docs-first, shipped).** Org creation was just name+logo; now it's a **2-step wizard** (`docs/12 §6.3`): (1) name+logo creates the org and switches you into it; (2) a **fully-skippable** questions step — role (persona A–E), team size, use-cases, self-reported stack (from the real integrations catalog), industry, referral. Answers land in a new **`org_profile`** table and do double duty: **personalization + segmentation analytics**. Also built the **product-analytics foundation**: new **`analytics_events`** stream (append-only, org-scoped) + `AnalyticsService` recording the activation funnel — `org.created` → `onboarding.completed` — instrumenting the `<30-min TTFI` north-star. **Connecting a source is deliberately NOT in creation** (stays in the dashboard onboarding, per product-owner call). Reconciled SEC-10: product analytics (new class **C6**) is account-about-itself data, distinct from graph-ingestion minimization — disclosed + GDPR-deletable. **migration 0040** (org_profile + analytics_events; RLS + append-only grants) applied live; **API** `PUT/GET /orgs/:orgId/profile` (Admin+) + strict keyed DTO; **web** multi-step wizard (profile save best-effort — never traps you out of the new workspace); **docs-first** (`04 §5.7`, `12 §6.3`, `13 C6`). **3 live-PG tests pass** (org_profile RLS isolation + analytics_events append-only). Earlier this session: mutation testing (Stryker) on the inference core banked at 62% floor; create-org bug fixed (existing users can now create); post-auth welcome glow reworked to the logo gradient. |
| 2026-07-10 | architect | **🛡️ RBAC sweep (UI + API) — no holes found; result documented (`docs/plans/rbac-audit.md`).** Product owner asked to verify role enforcement works on both UI and API (e.g. "a Member cannot change the company logo"). Enumerated every role-gated route + its UI control. **API:** every sensitive mutation is `@Roles("Admin")` (org name/logo, member role/remove, invites, connections connect/verify/sync/disconnect, AI/BYO-LLM settings, audit log, alert channels, dashboard sync, demo seed); `TenantScopeGuard` resolves the role LIVE from DB membership per request; **Owner-protection is server-enforced** (only an Owner may modify/grant Owner; never demote/remove the last Owner — BR-MEM-2/3). **UI:** every Admin-only control is hidden/disabled for Members to match — `OrgCard canEdit` (the logo example: no edit affordance for a Member, picker only in edit mode), AI-settings/audit-log conditionally rendered, member-management gated (+Owner rules mirrored), Integrations `canManage` threaded through connect/disconnect/alert-channels, dashboard sync + demo-seed gated. Command palette is nav+search only (no privileged actions); UI `role` comes from server `/me`, and the API is the real boundary (a forced control → 403). **No mismatch in either direction.** Two by-design policy notes (mute-finding is Member; AI conversations are org-shared not per-user) — product calls, not defects. Nothing to fix. |
| 2026-07-10 | architect | **🔒 Security & QA sweep — ALL Highs + Mediums fixed, most Lows too (13 findings + ops, shipped).** Worked `docs/plans/security-qa-sweep.md` top-down. **H1 Jenkins SSRF:** new `ssrf-guard.ts` classifies private/loopback/link-local/CGNAT/IPv4-mapped ranges (v4+v6, fail-closed); rejected at parse time AND re-checked post-DNS before every fetch (catches a public name → internal IP + most rebinding); dropped the absolute-URL passthrough; 16 tests. **M1/L2 upload:** dropped `image/svg+xml` (stored-XSS) from allowlist (client + server + `<input accept>`), added magic-byte check so a declared MIME can't lie. **H2/M4 rate limiting:** built a durable, org-scoped fixed-window limiter (`RateLimitService` + `rate_limits` table, migration **0039**, RLS, applied live) instead of `@nestjs/throttler` (that's HTTP-only — can't cover the WS Ask path — and in-memory). AI ask gets a per-org burst limit + a per-org DAILY budget on Atlas's shared Claude key (BYO orgs uncapped); invites get a rate limit + pending-invite cap. **M3:** SSE/WS error paths only surface our own ApiException; raw DB/provider errors are logged, not sent. **H3 web headers:** `next.config` `headers()` — CSP `frame-ancestors 'none'` + base-uri/object-src/form-action (omits default-src to avoid breaking hydration/API calls), X-Frame DENY, nosniff, Referrer-Policy, HSTS, Permissions-Policy (verified served). **M2 JWT:** pinned algorithms (ES256/HS256), ≥32-byte secret floor, fallback only on key/alg errors (never expiry/network). **L1:** explicit Fastify `bodyLimit` 4 MB so image caps are reachable. **H4 tests:** 25 new unit tests (no DB) on the previously-untested security-critical logic — JWT verify, TenantScopeGuard 404-vs-403, invite email-gate, image validation. **L3:** atomic invite-accept (claim-then-insert). **L8:** GitHub/Bitbucket refuse off-origin pagination follows (token-egress guard). **L4:** reconciled invite-token doc drift (returned once to creator, docs 08/12 updated). **Ops:** fail-closed **startup role assertion** — the API refuses to boot if the DB pool can bypass RLS (a misconfigured `DATABASE_URL`→owner would silently disable all tenant isolation, R8); verified live ("DB role check passed"). Full gate green (21/21); API tests 28→53. **Still open (deferred, lower risk/CI-tooling):** L5 per-tenant webhook secret, L6 SECURITY DEFINER arg binding, L7 postcss bump + `pnpm audit` CI, compile-time AWS `*Command` allowlist, `check:integration`. **No cross-tenant leak existed; isolation model confirmed solid.** Next: RBAC UI+API enforcement sweep (user-requested). |
| 2026-07-10 | architect | **⚡ Perf plan P1–P4 code fixes SHIPPED (only P0 infra remains).** Worked the `docs/plans/performance.md` backlog end-to-end. **Model correction that drove the work:** within one `withOrgScope`, multiple `c.query` on the *same* connection **serialize** (node-postgres has no pipelining), so the real levers are parallelizing *scopes* (separate connections = true parallelism) and merging round-trips into one SQL statement — not `Promise.all` on one client. **API (`perf(api): cut sequential DB round-trips…`):** `summary()` folds the sequential `recentActivity` into a 4-way parallel fan-out (speeds `/summary` + every caller — insights/findingDetail/findingsForNode); `findingDetail()` runs the affected-lookup + up-to-6 blast-radius traversals as ONE concurrent wave (was up to **7 back-to-back scopes** — worst endpoint in the audit); `listFeed()` (the bell, every page) merges `syncFeed`'s write into the read scope (2 scopes→1) + folds the unread count into the items query; `getNode()` and node-metrics collapse their two sequential SELECTs into one `LATERAL` join; `/me` runs the mirror-upsert + membership read (and the two upserts inside `ensureUser`) concurrently; **db pool 10→16** so parallel scopes don't queue. **Web (`perf(web): hydrate OrgSwitcher…`):** P2a — `requireShell()` carries memberships to the client so `OrgSwitcher` hydrates from the server render instead of a duplicate mount-time `/me` (only re-fetches on `ORG_UPDATED_EVENT`); P3 — dashboard/map/insights commit on auth then **stream** their heavy fetch behind `<Suspense>` (fallback = the route's own `loading.tsx`, seamless hand-off); P4 — `InfraMap` loads via `next/dynamic({ssr:false})`, keeping React Flow + CSS out of the initial route JS. **No behaviour change; each scope still sets `atlas.current_org` (RLS/R8 intact).** Typecheck + lint + prettier clean; API + web recompiled/booted without error. **Deliberately NOT done:** HTTP-caching `/me` in Next's shared data cache — it's token-bearing and not auth-keyed, so caching risks cross-user leak. **Remaining: P0 co-locate the API in ap-southeast-2 (Sydney) — infra-only, unblocked at deploy.** Next: the security/QA sweep fixes. |
| 2026-07-10 | architect | **⚡ Perf root-caused + 🔒 full security/QA sweep saved (both → `docs/plans/`).** Measured the "feels slow" concern: Supabase PG is in **Sydney**, ~**137 ms per DB round-trip** from dev; cost = sequential-hops × RTT. `withOrgScope` was **4 hops/op** (~610 ms for one read) → **✅ folded BEGIN+set_config into 1 hop** (RLS verified intact, `atlas_app`). Plan in `docs/plans/performance.md`: **P0 co-locate ECS API in ap-southeast-2** (the dominant *prod* lever) → P1 consolidate multi-scope endpoints/batch reads → P2 de-dupe per-page `/me` + client cache → P3 streaming. Then ran a **6-dimension security/QA sweep** (fan-out audits + manual verify of every High/Med) → `docs/plans/security-qa-sweep.md`. **No cross-tenant leak found** (isolation model is solid). Real work: **Highs** = Jenkins-connector **SSRF** (no private-IP guard), **no rate-limiting** (AI ask cost-DoS on Atlas's shared Claude key; invite email-bombing), **web app ships no security headers**, and **zero tests** on auth verifier / invite-accept / tenant-scope guard / image upload. **Meds** = **SVG stored-XSS** in the public logo/avatar bucket, HS256 JWT-fallback hardening, SSE raw-error leak. Fix order + full "verified-safe" list in the doc. Nothing fixed yet — this is the saved starting point for next session. |
| 2026-07-10 | architect | **🖼️ Org logos (first real image-upload flow) + invite-email diagnosis + ⚡ perf tracked.** Orgs can now carry a brand logo: pick one when **creating** an org (dashed logo tile in the create form) and **edit/replace/remove** it in Settings → Organization (Admin+). Client downscales the image to ≤512px and sends a `data:` URL; the API (`OrgLogoService`, service-role) validates mime + size (≤1.5 MB), uploads to a **public `org-logos` Supabase Storage bucket** (lazy-created public via `ensureBucket({public})`), and stores only the public URL on `organizations.logo_url` (migration **0038**, which also DROP+CREATEs `app_user_memberships` to return `org_logo_url` so the switcher gets it in the `/me` round-trip). New reusable `<OrgLogo>` (logo or generated monogram) renders in the **org switcher** (top bar) + settings; `PATCH /orgs/:id` generalised from rename→`update` (name and/or logo, `logo:null` clears). Gate green (21/21); 0038 applied live. **Email diagnosis:** the invite to `anmol.bhardwaj@siemba.io` never arrived because `EMAIL_FROM` is still the Resend test sender `onboarding@resend.dev`, which can **only deliver to the Resend account owner's own address** — arbitrary recipients are rejected (and our sends are non-fatal, so the UI still said "Invited"). Fix is user-side: verify a domain (siemba.io) in Resend (SPF/DKIM) + set `EMAIL_FROM` to an address on it. **Perf:** the product-owner's "everything feels slow" concern is now a tracked deep-dive on the board (dev-mode compile lag vs. real `force-dynamic`+no-client-cache; plan = measure prod → SWR caching → Suspense streaming → prefetch). |
| 2026-07-10 | architect | **⏸️ Advisor surface DEFERRED (honest product call) + sync self-heal + Bitbucket cleanup.** After building the architecture-redesign Advisor (before/after graphs, discuss loop) the product owner asked me to "be real" about whether it earns a top-level page. Honest answer: **not yet** — on the live estate it only fired 2 Multi-AZ proposals, which duplicate an Insights finding, and without cost data it's Well-Architected reliability = Insights, not "optimization." So we **removed the standalone `/advisor` page + nav** and **folded it into Insights**; kept the **pattern engine + `GET /advisor/proposals` as the backend foundation**. It graduates back to its own surface when (1) Cost Explorer/CloudWatch give real $/right-sizing numbers and (2) a richer pattern library makes the before/after graph earn its place (`docs/plans/optimization.md`). Also fixed: **sync self-healing** — `sync_runs` gained `updated_at` (migration 0037, bumps per checkpoint) so the orphaned-run reaper keys off **staleness (no progress 15m)** not a blunt 60-min timeout, so a restart-orphaned run clears on next list instead of showing "Syncing…" forever; and cleaned up the one Bitbucket run stranded by this session's repeated API restarts (marked it succeeded with its real stats — 258 resources, 977 edges — since it had persisted the full estate). Full gate green (22/22). |
| 2026-07-10 | architect | **🧭 Advisor — architecture redesign you can see and argue with (new surface).** First attempt (findings re-ranked) was wrong per the product owner — that's Insights. The real vision: Atlas proposes **structural changes** (EC2→ECS + a load balancer, single-AZ→Multi-AZ, managed DB…) shown as a **current-vs-proposed architecture graph** (before/after) you can **discuss**. Built (`docs/plans/optimization.md` rewritten): a **pure pattern engine** (`architecture-patterns.ts`) over the graph → **Proposals** carrying a small before/after subgraph (added nodes flagged), rationale, and honest tradeoff; `GraphService.architectureProposals` loads compute/datastore/LB nodes+edges once; **`GET /advisor/proposals`**; the **`/advisor` page** renders each proposal as **Current → Proposed** mini-graphs (green ghosted "proposed" nodes) + a **"Discuss with Atlas"** button (debate-seeded `/ask`). Seed patterns: **single-AZ RDS→Multi-AZ** (+standby) and **standalone EC2→ECS/Fargate+ALB+auto-scaling**. **Honesty (P3/P4):** the current graph is observed truth, the proposed graph is a clearly-labelled recommendation (never asserted); infra EC2s (VPN/Jenkins/migration/CLI) are filtered out so we don't propose an ALB for a bastion; cost $ numbers await Cost Explorer (Tier 2 teaser), never fabricated. **Verified live: 2 real Multi-AZ proposals** (calsaws-docdb-production, calsaws-rds-instance-1); the 3 standalone EC2s are all infra boxes → correctly no ECS proposal (P3). 5 pattern unit tests; full gate green (22/22). Also this session: fixed the Jenkins connect flow (provider accepted · bare-host URL · network-vs-auth classification · persist-on-failure + no-dup rows · private-network warning callout · Re-verify button) + the "Connected · not synced yet" subtitle. |
| 2026-07-10 | architect | **✅ Signal-enrichment — proved R11/R12/R13 end-to-end through the real engine (live-PG).** The three matchers had pure unit tests but nothing exercising them through the actual inference engine — so the integration surface (URN→id resolution, provenance rows, the `inference_rule_id` FK from migrations 0034–0036, convergence) was unverified. New **`enrichment.integration.test.ts`** (env-gated like `engine.test.ts`; runs in CI's integration job) drives each rule via `runInference` against Postgres and asserts the `DEPLOYS_TO` edge lands at the right confidence **and is cited to its rule**: R11 tag→Lambda (`rule:tag_code_correlation`), R12 image-SHA→ECS service (`rule:image_commit_provenance`), R13 `DD_SERVICE`→Lambda (`rule:service_name_env_correlation`), + convergence (second identical run writes nothing, IE-4). **Ran live: 4/4 pass against the real DB.** Turns three dormant-but-unit-tested slices into verified, integration-proven behaviour. Full gate green. |
| 2026-07-10 | architect | **📡 Signal-enrichment slice 3 — R13 service-name env correlation (canonical-name link, zero new crawl).** **New rule R13 (`service_name_env_correlation` → DEPLOYS_TO)** — reads the runtime's self-reported service name from env vars we *already* crawl (`aws.lambda.env`/`aws.ecs.env` signals): `OTEL_SERVICE_NAME`, `DD_SERVICE`, `SERVICE_NAME`, `APP_NAME`, `NEW_RELIC_APP_NAME`, and `service.name=` inside `OTEL_RESOURCE_ATTRIBUTES`. Matches the value by **exact normalized equality** to a repo slug → repo `DEPLOYS_TO` the runtime (the Lambda directly, or the ECS service running the task-def whose env it is). Unique → **inferred-high** (a runtime self-reporting its name is strong intent), ambiguous → inferred-low each, generic/short skipped (shared `GENERIC_TOKENS`). Distinct from R3 (which matches env values against *datastore endpoints*). No connector change — pure reuse of existing signals. **docs/05** rule catalog + confidence table + **migration 0036** (applied live ✓); plan S4 row marked built. Tests: 6 R13 golden fixtures (key/OTEL-attr extraction; lambda→high; taskdef→service resolution; generic→none; ambiguous→low; no-match→none). Full gate green (21/21). *Three code→infra matchers now share one precision discipline (exact-equality + generic guard + ambiguity→low): R11 tags · R12 image-SHA · R13 service-name.* |
| 2026-07-10 | architect | **🧬 Signal-enrichment slice 2 — R12 image→commit provenance (the strongest code→infra matcher).** After slice 1 (tags) + a CI-gate cleanup (jenkins `--passWithNoTests` + format drift + ignore local settings → `pnpm run check` green 21/21), built the highest-precision code→infra link: **new rule R12 (`image_commit_provenance` → DEPLOYS_TO)** — extracts a git short-SHA from an ECS task-definition's **image tag** (7–40 hex requiring an a–f letter so build-date ints like `20240115` are excluded; `@sha256` docker digests stripped) and **prefix-matches** it to a crawled PR's commit hash. A hit = *this running image was built from that commit* → its repo `DEPLOYS_TO` the ECS service (**inferred-high**, near-observed — a SHA match is near-certain; ambiguous SHA→several repos = inferred-low each, P3). **Architectural honesty:** a rule can only emit inferred tiers — `observed` is reserved for connector-read facts, so a *directly-read* deploy record (the `07c` CI/CD connector) is the true-observed path; the plan's slice-2 row was corrected from "observed" → "inferred-high (near-observed)". **Repo side:** capture PR commit SHAs (merge + head) — **Bitbucket reads `merge_commit.hash` + `source.commit.hash` from the live payload (real now)**; GitHub wired for parity (populated when the crawl lands). **docs/05** rule catalog + confidence table + **migration 0035** (applied live ✓). Tests: 6 R12 golden fixtures (SHA-extraction negatives: digest/date/no-SHA; match→high; no-match→none; ambiguous→low; no-PR→none) + 2 Bitbucket SHA-capture tests. Full gate green (21/21). *Deferred (noted): Lambda container-image SHA capture (needs a GetFunction call); GitHub PR-SHA crawl population.* |
| 2026-07-10 | architect | **🔗 Signal-enrichment slice 1 — R11 tag→code correlation (densifying the code↔infra graph).** From a design discussion on why the map is sparsely connected — diagnosed as **~20% matcher, ~80% signal**: (1) a *data gap* (the repos that deploy to the live calsaws AWS account aren't connected — R10 already yields 0 repo edges, correctly); (2) *discarded/uncollected keys* (tags unread + uncaptured on Lambda/ECS/RDS; image digests stripped; no IaC parsed); (3) deliberate P3 conservatism. Wrote **`docs/plans/signal-enrichment.md`** (sequenced roadmap: tags → image-digest→git-SHA (observed) → IaC parsing → OTEL/DD service names → log-content mining → semantic+human-confirm). Built the first key: **new rule R11 (`tag_code_correlation` → DEPLOYS_TO)** — matches a code-identifying AWS tag (`repository`/`service`/`application`/CloudFormation stack/…) by **exact normalized equality** to a crawled repo slug → unique = inferred-high (a deliberate label to one repo), ambiguous = inferred-low each (P3, never one wrong high), generic/short skipped (shared `GENERIC_TOKENS` with R10); compute-only targets (lambda/ecs.service/ec2 — a tag on a datastore is ownership, deferred); cited evidence (P4). Connector now captures `tags` in normalize for Lambda/ECS-service/ECS-taskdef/RDS/ElastiCache (EC2/S3/DynamoDB already did; RDS `TagList` + EC2 flow live now, Lambda/ECS/ElastiCache need a discoverer tag-fetch + IAM grant — noted in the plan). **docs/05** rule catalog + confidence table updated in the same change (docs-before-code); **migration 0034** seeds the rule row (applied live ✓). Tests: **10 R11 golden fixtures** (positive + P3 negatives: generic value, ambiguous→low, datastore-not-a-target, no-repos) + **7 tag-capture** tests — inference/connector-aws/db green, typecheck + lint clean, files formatted. *Pre-existing gate debt surfaced (NOT this change): 4 unformatted files + `@atlas/connector-jenkins` has 0 test files (vitest exits non-zero on empty) — flagged for a separate chore.* |
| 2026-07-09 | architect | **⚙️ Settings — wired the org/member management the API already had but the UI hid.** From a settings UX critique (user chose "function-first, no nav restructure"): the backend supported role-change / member-removal / invite-revocation / org-rename (audit labels existed) but **none was exposed**. Added client helpers (`renameOrg`, `changeMemberRole`, `removeMember`, `revokeInvitation`) and surfaced them in **OrgPanel** — a per-member actions menu (change role · remove, with a confirm `AlertDialog`), a **member count**, a **"You"** marker, **revoke** buttons on pending invites, and a shadcn **Select** for the invite role (de-native'd). All Admin+, honoring the role hierarchy + last-Owner rules (illegal options hidden; server rejections toast). New **OrgCard** replaces the debug-panel `dl`: **inline org rename**, **RoleBadge** (not plain text), member count, copyable org ID. LLM settings model dropdown also de-native'd to a grouped shadcn Select. Fixed a layout regression from the taller Org card — `md:items-start` so the Profile card no longer stretches half-empty. Typecheck/lint/prettier green (web). |
| 2026-07-09 | architect | **🔎 Finding detail page redesigned answer-first + Insights band/trend fixes.** New enriching endpoint **`GET /insights/:id`** (`findingDetail` on GraphService, reuses `summary()` so it never drifts from the list): returns the live finding + its **affected resources enriched with env + runtime health** (sorted prod/unhealthy first) + a **bounded blast-radius impact** aggregate (union of the inbound closure of up to 6 affected roots — "what depends on these", distinct, capped sample). Rewrote **`/insights/[id]`** from a data-sheet into **answer → prove → act**: a **severity-accented hero** (severity + pillar chips, human subtitle, freshness) with the **primary actions moved up top** (Ask Atlas how to fix · Mute); an **"if left unfixed" impact band** turning generic guidance into *their* blast radius (chips deep-link to Explore); **affected-resources list** with node glyph · env chip · health badge (prod/unhealthy float up); **numbered fix steps** (split guidance.fix into sentences) + an "ask for exact steps" nudge; a **lifecycle timeline** (first seen → came back? → still open); an honest **provenance footer** (observed from your latest sync · guidance source). (Built from a consumer-persona critique; user chose "full redesign incl. impact".) Also fixed **Insights top band overlap** (dropped the fixed `lg:h-52` that clipped the stacked severity tiles into the tabs/search; radar now width-capped instead) and moved the **trend's dotted paper to the whole card** (below the "Trend" label, lines-only). Restarted the dev server. Typecheck/lint/prettier green (api + web). |
| 2026-07-09 | architect | **📊 Insights → prioritized posture view.** Reworked the Insights page from a flat findings list (guided by a UX critique + user asks): **unified severity colors** to the shared `severityMeta` (red/amber/**sky**, matching the dashboard — "low" was `inferred-low` before; added a `text` field to `SeverityMeta`); **removed the CI/CD-coverage tile** + redundant bottom link (a metric among severity counts, already a finding). Added: **"Fix first"** strip — the 1–3 highest-priority active findings (severity × regression × blast) + an "Ask what to fix first" shortcut; **Posture by area** — per-pillar 0-100 bars from the same computation as the dashboard (API `/insights` payload now returns `summary.posture`); **This week** trend (opened/fixed/regressed from lifecycle data); **Ask Atlas per finding** (AI "how to fix this" on each row). Typecheck/lint/prettier green (api + web). |
| 2026-07-09 | architect | **💬 Ask Atlas: peekable citations + sources rail + copy; Explore graph polish.** **Ask Atlas** — citations no longer navigate away: a node citation (inline `[n]` or a chip in the new **Sources rail** under each answer) opens a right-side **peek drawer** (shadcn Sheet) with the resource's key facts · health · Open-in-Explore / View-on-map, so you keep your place in the chat (client `getNode` with a per-id cache; rail names resolve lazily); edge/computed citations still deep-link out; every finished answer gets a **Copy** button. (Chosen from an Ask-page UX critique — the cited experience was breaking the conversation; the map chat was ahead. Remaining ideas parked: follow-up chips, answer feedback 👍/👎, history search + date-grouping, data-aware empty state.) **Explore node detail** iterations: dropped the env chip, single AI CTA (header "Ask about this"; Diagnose only in the health hero), "seen"→clock icon, **vertical Timeline** graph, **Key facts as rows** side-by-side with Timeline (equal height). **Explore connection/blast graphs**: switched to map-style **smoothstep flows** (no curvy beziers, no repeated on-canvas labels) + a **blast-radius radar pulse** — filled rings sized to reach the farthest impacted node. Typecheck/lint/prettier green throughout. |
| 2026-07-09 | architect | **🧾 Node detail redesigned answer-first (Explore/[id]).** Reframed the resource page from a data-sheet into an answer: a **health HERO banner** for unhealthy/degraded nodes (state · reason · since); a curated **Key facts** card (per-kind engine/endpoint/runtime via new `lib/node-facts`) with the raw camelCase dump demoted behind an **All attributes** disclosure and Provenance behind an **Evidence & provenance** disclosure; an **action row** (View on map · View metrics ↗ CloudWatch · Copy URN) with the URN demoted to a quiet copyable chip; an **environment chip** (getNode now returns inferred `environment`); primary CTA = Diagnose (unhealthy) or **Ask about this** (always). **View on map** deep-links `/map?node=<id>` → the map opens with that node selected + centred (verified live). Added a **Risks** section: `GET /nodes/:id/findings` (`findingsForNode`, reusing the authoritative `summary()` findings filtered to those naming the node by evidence-id / `/explore/:id` / `/explore/:id/impact` href — no drift) + client `NodeRisks`. **Verified working live** — docdb detail shows Risks ×3 (unhealthy · single-AZ · SPOF), rds shows ×2 (matched by evidence); renders after its async client fetch resolves (a beat after paint). (Earlier "doesn't paint" was a red herring — I was screenshotting before `getClientToken`+fetch settled, slower right after a dev restart's token refresh.) Also spotted (not yet fixed): the map DetailPanel has a `toLocaleTimeString` AM/am SSR-hydration warning. Typecheck/lint/prettier green. |
| 2026-07-09 | architect | **🎛️ Explore filter bar overhaul + sidebar nav renamed.** Rapid UX pass on Explore filters: replaced the OS-native `<select>`s with **shadcn multi-select facets** (Type/Source/Health/Status) — OR within a facet, AND across; the menu stays open while toggling; the trigger reads "Unhealthy +1"; each option carries its real **icon/colour** (provider logos, category glyphs, health/status dots). Search box gained a **magnifier icon** + an **animated cycling placeholder**. Added a **Clear filters** button once any facet is active. Made it **responsive**: navigation runs in a `useTransition` and the results **dim + a spinner shows** while the next page loads, so a filter change never reads as a frozen "did it lag?" moment. Backend: `GET /nodes` facets (`source`/`category`/`status`/`health`) now accept comma-separated multi-values (IN / OR groups); vendored shadcn `select` then settled on `DropdownMenu`-based multi-select; docs/08 updated. Also: **sidebar nav** — the single generic "Platform" group became two intent-revealing groups, **Workspace** (Dashboard · Map · Explore · Ask Atlas · Insights) and **Manage** (Integrations · Settings); item names kept (each mirrors its page H1). Typecheck/lint/prettier green. |
| 2026-07-09 | architect | **🧭 Explore made state-aware + map Ask became a docked chat + map chats marked in Ask history.** Three consumer/UX passes after the map-Ask landing. (1) **Map Ask → persistent right-docked chat:** asking opens a chat docked to the right; the canvas reflows beside it (nothing hidden) and reframes; the chat holds the full conversation with a follow-up input (one reused backend conversation → context preserved). Left search (navigate/jump) and right chat (converse) *layer* rather than fight — a deliberate node selection out-ranks the chat's ambient citation glow (`litSet`: focusSet > citations > lenses), so you can jump mid-conversation and it takes over, then deselect to get the glow back. Verified live: multi-turn context, canvas spotlight, jump-while-chatting. (2) **Map chats are first-class in Ask history:** new `ai_conversations.origin` column (migration 0033, backfills legacy 'Map · …' chats; documented in docs/04 §5.6) → API create/list carry it → Ask Atlas history badges map-started chats with a waypoints icon and strips the legacy title prefix. (3) **Explore state-aware list** (chosen from a UX critique — the list was a flat inventory ignoring the 'red when broken' thesis; 3 of 5 columns were constant): added a **Health** column (Healthy/Degraded/Unhealthy/Not-checked, from `attributes.health`) replacing the constant Confidence column, a **server-side health facet** on `GET /nodes` (whole-estate, not just the page), and a **result count** via new `page.total` (COUNT over the same filters); distinct over-filtered vs empty-estate states; docs/08 updated. Verified: unhealthy docdb/rds show red; health=unhealthy → 2 matching. Typecheck/lint/prettier green across db+api+web throughout. |
| 2026-07-09 | architect | **🤖 Map "Ask" wired to real cited AI + on-canvas spotlight (task #28 done) — and a shell z-index fix.** The map's command bar is now genuinely dual-mode: type a resource name to **jump** to it (typeahead, unchanged) or ask a **natural-language question** routed through the same retrieval-first, cited engine as Ask Atlas (`streamAskWS`, WS→SSE fallback + cancel). The dropdown's first row is always the AI ask (default Enter action); resource matches follow under a "Jump to a resource" divider, all arrow-navigable. The answer streams in a compact card under the bar — live retrieval "show your work" trace → cited answer (bold/code/inline citation superscripts) → **Sources** chip row (real node names + glyphs, click to jump) → confidence trust line. **Node citations AND cited-edge endpoints light up on the canvas** while everything else dims (reused `queryIds` litSet), and the view frames the cited set once the answer lands — the AI's citations rendered directly on the graph (P4). Verified live in Chrome: "what is unhealthy right now?" (Observed, computed-fact) and "what does calsaws-rds-instance-1 depend on?" (7 edge-cited deps → RDS + dependencies spotlit, rest dimmed, chips jump to nodes); no console errors. Replaces the old keyword-highlight-all Enter behaviour. **Also fixed:** the sticky app header was `z-10`, tying with the map overlays (Legend `z-10`, search `z-20`) which sit later in the DOM inside a non-stacking-context container — so the Legend punched **through** the notifications dropdown. Lifted the header to `z-30` (dropdown stays `z-50` inside it) so global chrome always sits above page content. Web typecheck/lint/prettier green. |
| 2026-07-09 | architect | **🗺️ Map turned from a diagram into an operational instrument (9-feature pass).** Designed + built, guided by the operational-intelligence north star (`docs/10`): **Health lens** (recolour the graph by runtime health — broken/degraded stay lit, healthy recedes: "what's on fire" at a glance); **blast-radius on click** (a node lights its connected up/down-stream chain, dims the rest); **find/ask box** (top-left) — ↑↓+Enter jumps to one result, plain Enter *highlights every match* across the graph, auto-expanding the collapsed shelves so hits aren't missed, fitting to them; **kind filter** chips; **Changed lens** (recent/drifted nodes — needs real multi-day sync history to discriminate; demo seeds `first_seen` uniformly); **node legend** (health/protection, not just edges); **collapsible unlinked shelves** (the 59-repo + 34-infra walls now collapse to labelled banners with actionable hints — default map went from ~104 rendered nodes to the 11-node flow). All focus modes (blast-radius · Health · Changed · kind filter · ask) unified through one `litSet` (nodes + edges dim consistently). Plus polish: dotted background, legend/minimap/attribution cleanup, Security toggle moved below the header, in-place Security toggle (no re-fit), wider node/column spacing, clearer subtext. Also a big dashboard polish pass earlier same session (tall Needs-attention rail + heat-ramp Open-findings donut + full-width Recent-activity; pillar chips now shared with Insights via an API `pillar` tag). Web typecheck/lint/prettier green throughout. |
| 2026-07-08 | architect | **📘 New design doc `07c` — CI/CD & deployment linking (Jenkins-first, the code↔infra keystone).** Wrote the connector+inference spec (delta vs `06`/`07`, like `07b`) for ingesting Jenkins/CI-CD: the thesis is that CI/CD **upgrades the `DEPLOYS_TO` code→infra edge from a low-confidence naming *inference* (`05` R1) to an *observed* fact**, closing the two graph islands (code, infra) into one system graph and enabling **RCA-to-the-PR** (`10` north star). Three linking signals (CI-declared target · artifact/image **digest** · resource deploy-tags), new rule extensions R1-obs/R7/R8, deploys modeled as edges+timeline events (not heavyweight nodes), read-only token auth, generalized to Actions/GitLab/Argo/CodePipeline. Indexed in `docs/README`. Phase-2 but flagged **keystone priority** (raises graph connectivity, not just breadth — P1). Also this session: large UI polish pass (dark Ask Atlas, aligned posture+inventory, node-page attributes/dividers, impact icons, blast-radius + unhealthy-node danger pulse, readable degraded-connection callout, dark Test button, dashboard skeleton). |
| 2026-07-08 | architect | **🎨 Dashboard bold redesign (brand direction seeded) + finding lifecycle history.** Kicked off the **brand/UI revamp** at the dashboard (user: "such a nice dashboard, why is ours so boring"). New **`--brand` token** (Atlas green from the AI mark) in `globals.css`+`tailwind` — the one identity hue, used sparingly. Dashboard rebuilt: **hero greeting** (time-of-day + estate pulse), a **hero grid** with a real focal point — an **Estate-health card in Atlas green** (SVG score ring; health is an HONEST heuristic roll-up of findings-by-severity + source health, no fabricated metric), an **Open-findings** card (severity segmented bar → Insights), a **Sources** health card; inventory **stats** restyled with colored icon chips (matching Insights category enums) + bigger numbers. All from the existing `/summary` payload. Also completed the **finding lifecycle** (migration `0031_finding_state`): sync-time reconcile (worker `onSyncComplete` hook → `GraphService.reconcileFindings`, reuses the dashboard's own finding computation so no drift) records open/resolved/regressed; `/insights` overlays firstSeenAt/regressedAt + returns a **resolved** list; UI gains a **Fixed tab** (resolved history, "fixed Xd ago"), **aging** ("open 5d") on active rows, and a **Regressed** badge. Earlier same-day: Insights search + severity filter + per-category color enums; data-freshness header line. Typecheck/lint/prettier green across db+api+ingest+web. |
| 2026-07-08 | architect | **🔎 Insights redesign + finding lifecycle; welcome-on-signin; nav-lag + /login fixes.** **Insights** rebuilt for scale: a scannable **findings table** (severity · finding · category · affected) with posture tiles + pillar filter, and a **/insights/[id] detail page** (finding ids are stable rule slugs, so deep-links are reliable; a cleared id shows a friendly 'resolved on its own' state). Added the **finding lifecycle** (migration `0030_muted_findings`): findings are DERIVED live so we don't fake a 'fixed' flag — instead **Mute/accept-risk** (persisted, org-scoped, keyed by finding id → 'Muted' tab; posture counts reflect active only) + **'I fixed it → recheck'** which re-syncs the sources and honestly confirms against the fresh graph whether it cleared. **Welcome overlay** now fires **once on entry after sign-in** (auth callback drops a one-shot `atlas_welcome` cookie; `WelcomeGate` consumes it → plays a ~4s blur→green-blob→greeting, greens sampled from the Atlas AI logo; 'Hey {name}!'); reloads/return visits don't re-trigger. **Login**: full-screen split, inset dark hero with faint watermark + liquid-metal mark, step icons, black Google button, Terms/Privacy + legal pages. **Fixes:** sidebar nav shows an instant pending spinner (force-dynamic routes felt frozen with no Chrome spinner); middleware redirects signed-in users off /login. Typecheck/lint/prettier green across db+api+web. |
| 2026-07-08 | architect | **🔐 Login redesign + legal pages (last queued item done).** Rebuilt `/login` as a split-screen: an inset, rounded dark-green hero card (liquid-metal Atlas shader mark + confident, em-dash-free copy: "See everything you run." + 3 step cards) beside a clean Google sign-in with a real Google mark. Removed the "Atlas" wordmark from the hero; **Terms of Service + Privacy Policy** links anchored at the bottom of the sign-in panel. Built the previously-approved **legal templates**: `/legal/terms` + `/legal/privacy` with a shared layout (Atlas header, back-to-sign-in, footer links) and reasonable B2B boilerplate clearly banner-marked "template for review". Typecheck/lint/prettier green. **No user-requested items outstanding.** |
| 2026-07-08 | architect | **🎨 Avatars everywhere + palette/error polish.** Switched avatars to **avvvatars** (local, no network): initials by default + pickable geometric shapes; new shared `UserAvatar` renders a photo URL, `avv:character`, or `avv:shape:<seed>`. **Member list** now shows each person's real avatar (`/orgs/:id/members` returns `avatarUrl`) so your row matches your profile; Members role badge is a clean outlined chip (was washed-out grey). **ErrorState** ('Something went wrong') now shows the gently-spinning Atlas mark (new `EmptyState` `iconSlot`). **⌘K palette** got a richer surface (blur backdrop, icon chips, active-row highlight, keyboard-hint footer). Profile+Org cards are equal-height side-by-side; profile edit spacing fixed. Confirmed toasts already fire on model save/remove + channel connect/test/disconnect. Typecheck/lint/prettier green. **⏳ Still queued (user-approved): Terms + Privacy template legal pages.** |
| 2026-07-08 | architect | **🎛️ Multi-channel alerts (Discord/Teams) + resource-icon notifications + personal profile (avatars) + settings polish.** A long UI/UX pass, all verified live: **(1) Alerts** — `NotificationService` is now channel-generic (migration `0028`): Slack + **Discord** + **Microsoft Teams** incoming webhooks, per-kind URL validation, `dispatch()` fans out to every enabled channel with per-platform message transcoding (autonomous diagnosis computed once/org). Settings 'Alerts & digest' card connects one-or-more channels with real **brand icons** + per-channel Test/Disconnect. **(2) Notification feed icons** — migration `0029` adds `notifications.node_kind` (backfilled); the bell shows the **real resource logo** (AWS RDS/DocDB) on a neutral circle; read rows sit on a darker grey (no icon dimming); panel closes on outside-click/Escape (was clipped by the header's backdrop-blur). **(3) Personal profile** — `/me` PATCH updates the user's own **name + avatar** (kept across re-login); Settings 'Your profile' card (separate from Organization) with the Google photo + an avatar picker (Google photo or **DiceBear 'Dylan'** generated faces); sidebar footer shows name + avatar (DiceBear fallback); `next.config` allowlists googleusercontent + api.dicebear.com. **(4) Polish** — settings is one full-width column with profile+org **side-by-side equal-height**; consistent title icons on every card; removed the redundant org name from the sidebar brand + bigger 'Atlas' wordmark; the active Ask-AI-model box reads green; 'Update model' disabled until a key is entered; top-right toasts on invite/connect/sync/model/delete. Typecheck/lint/prettier green across db+api+web. **⏳ Still queued (user-approved, not built): Terms + Privacy template legal pages in Settings.** |
| 2026-07-08 | architect | **🔔 In-app notification center (bell + inbox) + top-right toasts on actions.** Two layers of "the system tells you things": (1) **Toasts** — moved the app toast surface (sonner) to top-right with richColors + close button, and fire success/error toasts on the actions that complete something (invite teammate, connect source, start sync, save/remove AI model, delete chat, disconnect). (2) **Notification center** — a durable inbox behind a bell in the top bar. Migration `0027_notification_feed`: org-scoped `notifications` table (RLS, unique `dedupe_key`, org-level read state). `NotificationService.listFeed/markRead/markAllRead` + `syncFeed`, which idempotently materialises feed rows from `health_transition` node_events (`dedupe_key = health:<eventId>`) so the bell is always fresh on open — no cross-org background job needed. New `/notifications/inbox` routes (list + read + read-all, any Member). `NotificationBell` header component: unread badge, polls every 45s, panel with severity dots + deep links + mark-all-read. **Verified live: bell showed 4 real health notifications (docdb/rds went down), mark-all-read cleared the badge.** Also restored Settings to a full-width single column (the tabbed sub-nav + narrow centered/left-shifted variants were all worse for the little content). Typecheck/lint/prettier green across db+api+web. **Still queued from the user:** more alert channels beyond Slack (Discord/Teams/webhook; email needs a provider) + Terms/Privacy legal pages in Settings. |
| 2026-07-08 | architect | **🗑️ Disconnect confirm-modal + success toast; Settings de-duplicated.** Disconnecting a source purges its entire subgraph, so the inline check/✕ confirm was too easy to trigger by accident. Replaced it with a proper shadcn **AlertDialog** ("Disconnect X? …purges everything Atlas learned from it — resources, edges, signals — can't be undone", Keep connected / Disconnect) that stays open while the request is in flight; on success a **sonner toast** ("Disconnected X · removed from your graph"), on failure an error toast. Added the `alert-dialog` + `sonner` primitives via the shadcn CLI and mounted `<Toaster/>` in the root layout (dropped its `next-themes` dep — app is light-only). Disconnect button uses the `danger` token, not shadcn's `destructive` (this mono theme neutralizes `--destructive` to gray; `--danger` is the red). Also **removed the Integrations section from Settings** — it was a read-only duplicate of the sidebar Integrations hub (the one place to manage sources); Settings no longer fetches `/connections`. Verified live: AWS-card trash → red-buttoned confirm modal renders with the right copy (did NOT execute a real delete against the 172 live AWS resources); Settings sub-nav now General/Members/Notifications/AI model/Security. Typecheck/lint/prettier green. |
| 2026-07-08 | architect | **⚙️ Settings sectioned into a left sub-nav** (this commit) — replaced the single long scroll of six cards with a focused left sub-nav (General / Members / Integrations / Notifications / AI model / Security); admin-only sections (Notifications/AI/Security) are hidden from the nav for non-admins, matching what the API enforces. New client `settings-view.tsx` tab-switcher + slimmed server `page.tsx`. **Gotcha fixed:** the async server-component `AuditLog` (uses `apiGet` → `next/headers`) can't be imported into a `"use client"` component — it triggered a `next/headers`-in-client build error. Resolved with the slot pattern: `page.tsx` renders `<AuditLog>` on the server and passes it as a `securitySlot` ReactNode prop, so it stays server-rendered without dragging `next/headers` into the client bundle. Verified all sections live in the browser (General org card, Security activity log, AI model BYO-LLM card, Integrations connected-sources). Typecheck/lint/prettier green. Part of the ongoing consumer-polish pass ahead of the agreed **brand guidelines + full UI/UX revamp** finale. |
| 2026-07-07 | architect | **🩺 One-click Diagnose button** (`11798a9`) — Phase D incident tracing was built but unreachable (only fired if someone typed 'why is X unhealthy'). Every degraded/unhealthy node now shows a red 'Diagnose with Atlas AI' button (map detail panel + Explore header) → opens `/ask?q=<prefilled>` and auto-submits. Verified the question classifies as `culprit` → agentic loop → search+diagnose tool, so one click runs the real trace (health + blast radius + change timeline → cited hypotheses). North star made a single gesture. **Next agreed with user: brand guidelines + full UI/UX revamp** once feature work settles. |
| 2026-07-07 | architect | **🔔 Proactive notifications: Slack alerts + daily digest** (`383f236`) — the pull→push feature that turns Atlas from a thing you visit into one that watches your back. Migration `0026 notification_channels` (org-scoped RLS, Slack webhook, alert+digest watermarks, `app_notification_orgs()` SECURITY DEFINER). `NotificationService`: set/test/disable (webhook write-only, GET returns a masked hint only); `dispatch()` sends real-time alerts on health transitions since the watermark + a daily digest (24h merged-PRs/config-changes/health-changes + current unhealthy + vuln count), best-effort with self-advancing watermarks. `NotificationDispatcherBootstrap` (in-process dev tick, NOTIFY_INTERVAL_MINUTES, cross-org). Controller (Admin) + Settings 'Alerts & digest' card (paste webhook → Test → masked hint → disconnect). **Verified live: dispatcher boots; alert+digest SQL return real data** (both DBs' unhealthy transitions, 4 changes/24h, 2 unhealthy, vuln count). Watermark starts at now() on connect (future alerts, not a historical dump); digest fires once on first tick as a welcome. Slack POST itself untestable without a workspace (trivial fetch). **This is the first retention mechanic — makes health/posture/timeline/diagnose matter daily, not on-demand.** |
| 2026-07-07 | architect | **💬 Ask Atlas: warmer voice + conversation memory + bold formatting** (`e344c7e`, `19f3e3f`, user feedback). **(1) Voice** — narrator/advisor prompts rewritten with a VOICE section (knowledgeable teammate, lead with the answer then explain why it matters, connect the dots, prose not bullet-dumps); narration temperature 0→0.4, maxTokens 1024→1500 (warmth can't fabricate — grounding is the closed-context citation gate, not temperature; planner/loop stay 0). **(2) Cross-turn memory** — `askStream` never fed history back, so 'explain that simpler' got a generic estate dump; now loads prior turns → `planningQuestion()` prepends the last user turn for short/deictic follow-ups so RETRIEVAL targets the topic, `historyMessages()` gives the narrator real multi-turn context. **(3) Formatting** — prompts now request **bold** for key numbers/names + \`code\` for identifiers; the chat renderer already supported both, was never fed them. All 79 AI tests green (hallucination-zero eval, citations, honest-absence intact); prompt versions → narrator@3/advisor@3. |
| 2026-07-07 | architect | **🗺️ Map arrangement, corrected: two shelves, all repos visible** (`41b8f54`, user: 'they cant see all repos and missing connections'). Reverted the over-aggressive hide from `d8aa265` — the tall COLUMN was the problem, not the repos existing. Now only granular code activity (projects/pipelines/PRs/users) leaves the canvas; **every repository stays**: deploying repos sit in the flow beside their compute, non-deploying repos drop into a compact **'Repositories — no infrastructure link found yet'** grid shelf (that label IS the missing-connection signal). The unconnected-infra shelf became its own separate frame. buildLayout refactored to a reusable `addShelf` helper stacking two grids below the flow. Typecheck+lint clean; browser verify still blocked by hidden-tab throttle. |
| 2026-07-07 | architect | **🗺️ Map arrangement: infrastructure-first** (`d8aa265`, user: 'better way to arrange these'). The canvas was rendering two graphs at once — the AWS flow AND the full Bitbucket code tree (4 projects → 61 repos → PRs), the latter a tall column dominating everything despite only 4 repos bridging to infra. Now code nodes (project/pipeline/PR/user/team) are dropped from the map, and a repository appears ONLY if it has a DEPLOYS_TO edge — sitting left of the compute it ships to. Full code graph stays in Explore; a subtitle note links there with the hidden count. Verified by data (61 repos → 4 bridge shown; 56 infra nodes; 57 repos + 4 projects → Explore). Browser screenshot blocked by hidden-tab render throttle (known); logic proven by typecheck + DB math. |
| 2026-07-07 | architect | **🔗 R10 recall widening verified: 1 → 8 probable code↔infra links, zero noise** (`HEAD`). Plural folding (integrations↔integration-df) + rare-short-token matching ('chat' names only 2 repos → distinctive) + vendor-solution filter (WAFSecurityAutomations workloads skipped; 'automation/security/helper' generic-listed after gluing QA repos to Amazon's WAF at first pass — caught live, fixed same hour). **Final live table: API Backend→calsaws-backend-api-service · integrations→calsaws-integration-service · Chat_Functions + provapt-web-chat → all 3 chat lambdas** — every edge inferred-low with sharedTokens cited. The reverse-engineering layer now demonstrably ties Bitbucket code to the compute running it, from log names alone. |
| 2026-07-06 | architect | **🔗 R10 v2 - probable matching + a live precision catch** (`853312a`, user: 'inexact is fine, call it probable'). Token-level tier added: no substring match → tokenize both sides (camelCase+separators, ≥4 chars, generic-word stoplist), link on ≥2 shared tokens or one ≥6-char distinctive token → ALWAYS inferred-low, `sharedTokens` in evidence, ≤3 repos/workload. **Live verification caught a false positive at HIGH confidence within the hour**: env-suffix stripping reduced repo `lambda-test` to bare 'lambda' → substring-matched `calsaws-daily-stop-lambda` (P3 violation). Fixed (generic residues can't strong-match), regression test, edge retired. **Net result - the first log-derived code↔infra link on the real estate: `API Backend - Provapt` --DEPLOYS_TO (probable, tokens: backend)--> `calsaws-backend-api-service`.** 38 inference tests. Also affirmed to the user: everything is org-generic (RLS multi-tenancy, per-org connections, rules over structure — zero Siemba-specific code). |
| 2026-07-06 | architect | **✅ Phase E core slice verified live + ingest fix** (`HEAD`). Fresh-dist resync populated `multiAz` → **both calsaws databases flag single-AZ** (medium finding, live). Wildcard-IAM stays quiet (account clean on the strict rule — honest). Fixed a latent ingest bug the work exposed: `persistNode` replaced attributes wholesale each crawl, wiping the health annotation until the next poll tick — the out-of-band `health` key now survives the upsert. **Live posture findings now: world-open SGs (OpenVPN admin ports!), misnamed public ELB, 2 single-AZ DBs** — all in Insights/Ask Atlas with Well-Architected guidance. Phase E remaining: toxic combo (blocked on DevOps connections), WAF coverage (needs wafv2:ListWebACLs — add to DevOps ask). |
| 2026-07-06 | architect | **🛡️ Phase E continued: wildcard-IAM + single-AZ rules** (`4247324`). Wildcard IAM (Allow action `*` on resource `*` from policy signals — reads the retained iam.role data; calsaws is clean on the strict rule, finding stays quiet honestly) + single-AZ databases (RDS crawl now records `MultiAZ`; re-sync run to populate → both calsaws DBs expected to flag). Remaining Phase E: toxic combo (blocked on DevOps account connections), WAF-coverage (needs wafv2 read). |
| 2026-07-06 | architect | **🛡️ Phase E STARTED - posture rules first slice** (`cd5d307`). Two deterministic rules over existing crawl data, live findings on the real estate: **(1) world-open SGs** (aws.sg.rules signals, `0.0.0.0/0`/`::/0` ingress, ports named) → found `calsaws-elb-sg` :443 + **OpenVPN admin ports 943/945 open to the internet**; **(2) scheme-vs-name mismatch** → **`calsaws-private-elb` is internet-facing despite its name** (either the name or the exposure is wrong). Both high-severity 'Security posture' findings → dashboard/Insights/Ask Atlas; knowledge pack gains the category (Well-Architected Security). **Remaining Phase E:** toxic combo (needs the repo↔service bridge = DevOps account connections), public-ALB-without-WAF, single-AZ RDS (needs MultiAZ attr in crawl), wildcard-IAM (reads the retained iam.role data). **DevOps ask list handed to the user** (3 IAM actions on calsaws + read-only creds for the Siemba-backend AWS account + calsaws-code workspace token). |
| 2026-07-06 | architect | **🧹 IAM roles demoted to on-demand support data** (`0d02bba`, user call): 96 `aws.iam.role` nodes were the LARGEST kind in the graph, flooding the estate browse. Now excluded from Explore browse/facets, the map canvas, and the AI estate breakdown - reachable via explicit `?kind=aws.iam.role`, search, and Ask Atlas IAM questions. The graph keeps them (R8/posture rules need `ASSUMES_ROLE` + policy signals - Phase E's wildcard-IAM check reads exactly this data); only the default surfaces stop showing them. User's stated focus re-confirmed: the reverse-engineering linkage (logs→workloads→repos→compute) IS the point; its infrastructure is live and waiting on matching repos/accounts to connect. |
| 2026-07-06 | architect | **🕵️ Log-intelligence layer BUILT & live** (`9d0184e`, user: 'reverse-engineer what runs inside EC2/ECS from CloudWatch and link it to Bitbucket code'). CloudWatch log-group NAMES = free read-only evidence of what runs inside opaque compute. Chain: **logs discoverer** (`logs:DescribeLogGroups` — granted!) → `aws.logs.group` support-data nodes (excluded from map/browse/AI-breakdown like external.package) + **`aws.logs.workload` signals** (parsed name + host: /aws/lambda/x→lambda · /ecs/x→ecs · /ec2|/var/log→ec2 · bare→unknown; AWS-managed namespaces filtered) → **R10 `log_workload_correlation`** (migration 0025 seeds kind+rule): normalized name matching (env suffixes stripped, ≥5-char substring so 'api' can't glue the estate) → DEPLOYS_TO(repo→host) with honest tiers (lambda/ecs unique = inferred-high double-witness; EC2 = inferred-low capped at 3 instances; ambiguous → nothing, P3). 5 rule tests; kind catalog 18→19. **Live: 22 log groups → 21 workloads** incl. `calsaws-backend-api-df`/`calsaws-integration-df` (confirms ECS families), the chat lambdas, and **`calsaws-daily-stop-lambda` — likely WHY the RDS instances are 'stopped' (scheduled cost-parking, not an incident)**. 0 repo edges — CORRECT: calsaws code isn't in the Siemba workspace; edges materialize automatically when matching repos connect. |
| 2026-07-06 | architect | **🧭 Map polish per user direction** (`81efce6`, `95db8f8`): IAM roles off the unconnected shelf (identity plumbing, not runtime estate; still in Explore, still in flow when ASSUMES_ROLE is visible) · CloudWatch panel section became **awareness, not a viewer** - zero-call Monitoring row ('CloudWatch metrics available → Open in AWS console ↗' deep link per resource) replacing the live sparklines; `GET /nodes/:id/metrics` kept server-side for future AI RCA depth. |
| 2026-07-06 | architect | **🗺️ Map: unconnected shelf + 📈 on-demand CloudWatch metrics** (`b1911b4`, `fa29655`, both user asks). **(1) Shelf:** isolated resources (unattached EC2s/buckets/ECR/roles) were silently dropped from the canvas; now they render as a grid inside a dashed 'No observed connections yet' frame below the flow (health rings + shield chips apply; people/PR cards stay off). Replaced the count-only disclosure. **(2) CloudWatch on click:** `GET /nodes/:id/metrics` (NodeMetricsController in connections module, Member read, org-scoped node lookup) → `AwsConnector.collectMetrics` → `collectNodeMetrics` (GetMetricData, last 24h, period 300): EC2 CPU/status-checks/network · Lambda invocations/errors/duration · RDS CPU/connections/free-storage. Map panel fetches on select and renders dependency-free SVG sparklines + latest value. NOTHING persisted (hard boundary: not a TSDB). **Live-probed against the real account: call chain works; the parked calsaws estate returns 0 datapoints right now (DBs stopped, Lambdas idle) — panel states that honestly.** Gates green (aws 77, api/web typecheck+lint). |
| 2026-07-06 | architect | **🧠 Phase D BUILT - AI incident tracing (diagnose + get_pr_diff)** (`3732447`) **→ operational-intelligence Phases A-D ALL SHIPPED.** RetrievalPort += `nodeEvents` (Phase C timeline) + `prDiff` (raw Bitbucket PR diff via the org's own connection through the Secrets Broker; `FetchBitbucketClient.diff()`); `RetrievedNode` carries live health. New agentic tools: **diagnose** (health + blast radius + deployers + change timeline for node AND deployers in a clamped window; instructs ranked cited hypotheses + explicit honest absence) and **get_pr_diff** (12k-char clamped unified diff so the model judges plausibility from real code). ContextAccumulator renders events as citable A-marker facts (shared counter with estate; markers stay [NEA]\\d+) and diffs as reference sections tied to the PR's N marker. **culprit intent routes agentic** + planner rule for 'why is X unhealthy/broken/failing/down'. 3 deterministic evals: golden incident (config change + deployer merge cited), adversarial quiet window (no invented culprit), diff flow. **Also: map honesty note** (user caught 30 services on dashboard vs ~10 on map): isolated nodes were silently dropped; the map now states how many resources are not drawn + links to Explore. Gates: ai 79, bitbucket 32, api 23, web all green. **Ask 'why is calsaws-rds-instance-1 unhealthy?' in Ask Atlas to see D live** (real-provider path; expect health + stopped-status + timeline reasoning; CloudTrail events flow once cloudtrail:LookupEvents is granted). |
| 2026-07-06 | architect | **⏱️ Phase C (change timeline) BUILT + 🤖 AI estate blindness fixed** (`d5386af`). **Phase C:** migration `0024 node_events` (org-scoped RLS, composite FK, `uq(org_id,dedupe_key)` idempotent ingestion) · `collectCloudTrailEvents` in connector-aws (LookupEvents write-events, conservative URN mapping across 11 resource types, unmappable dropped per P3; 3 tests) · `applyNodeEvents` in ingest + `applyHealthObservations` now emits **health_transition** events on state change (prev-state read + annotation write in one statement) · health poller runs a changes pass every tick (1h lookback; dedupe makes overlap free) · `GET /nodes/:id/events` = node_events ∪ **derived pr_merged** from the graph's own merged-PR nodes (no backfill table for history the graph already proves; 50 real merges verified on API Backend - Provapt) · Explore node detail gains a **Timeline card**. **Live finding: `cloudtrail:LookupEvents` is DENIED** for readonly-anmol → named in skipped; IAM grant list is now ecr:DescribeRepositories + route53:ListResourceRecordSets + cloudtrail:LookupEvents (health checks all passed — CloudWatch/ELB/ECS/RDS reads work). **AI fix (user: 'wtf', Ask Atlas said it only knew 1 cloud account despite 150 AWS nodes):** the estate snapshot was flat counts only. New `GraphService.infrastructureBreakdown` (per-kind counts + freshest names + live not-healthy) flows into EstateOverview → estate context (new A-marker facts incl. live-health line) → estate_overview tool. All gates green (ai 76, connector-aws 77, api 23, web+ingest). **Phase D (AI diagnose/get_pr_diff) is next — C gave it the timeline to reason over.** |
| 2026-07-06 | architect | **🔎 Map detail panel upgrade** (`acb53f6`, user: 'when I click I don't get much knowledge'). The /graph payload already carried full node attributes - the panel ignored them. Now kind-aware key facts (Lambda runtime+handler · RDS engine+endpoint:port · ELB type/scheme/DNS · ECS cluster+desired+taskdef · EC2 type/state/private-IP · repo language/branch/updated) + capped generic scalar fallback (max 6 rows), 'Protected by <sgs>' row, health row shows checked-at time on hover. Zero API changes. |
| 2026-07-06 | architect | **🛡 Map: pair-merged edges + Security toggle** (`6944f77`, `HEAD`). (1) The user spotted a dashed rail skewering the S3→WAF-lambda row: TWO edges between the same pair in opposite directions (observed stores-in + inferred connects-to) - the backward one fought the LR layout and smoothstep wrapped it around the row. Canvas now draws ONE line per node pair (observed direction/style wins; hover label lists every relationship 'stores in · connects to'; detail panel unchanged). (2) User asked for the removed SGs back on demand → **Security pill** next to the legend: OFF = clean flow + shield chips (default), ON = SGs + PROTECTS edges return for the boundary-audit view. Gates green. |
| 2026-07-06 | architect | **🗺️ Map de-noise: protection → shield chips, labels → hover-only** (`24f8e94`, user feedback on the protects/connects clutter). PROTECTS edges no longer draw on the canvas (protection is a property, not a flow): protected nodes get a **shield chip** (SG names on hover; detail panel keeps the full list), and SGs whose only edges were PROTECTS drop off the canvas automatically (still in Explore) — canvas went 20→15 nodes, the 5 longest rail-edges gone. **Edge labels are hover/selection-only** now: hovering a node reveals + bolds its edges with labels, fades everything else to 12% ('connects to' ×5 wallpaper eliminated; labels used to detach from long orthogonal paths anyway). decorateEdges keeps the fitView effect separate so hover never re-fits the viewport. Verified in-browser by the user (hover on waf-logs → 'stores in' lights up, rest ghosts). Debug note: in a HIDDEN tab React Flow edge-paths may never mount (rAF-frozen measurement) even though nodes render — verify edges via a React-fiber props probe, not the DOM. |
| 2026-07-06 | architect | **🚦 Operational Intelligence Phases A+B BUILT & verified live** (`09a3205`; plan `docs/plans/operational-intelligence.md`). **Phase A (deploy inference, the code↔infra keystone):** Bitbucket crawl now fetches `bitbucket-pipelines.yml` + the repo's NON-SECURED pipeline/deployment-environment variables (readable via API; probed live first - Siemba's deploy targets all live in env vars like `CLUSTER_NAME=siemba-backend-production`); `parsePipelineDeploys` substitutes per environment and extracts concrete ecs/lambda targets + ECR image pushes, skipping anything still `$VAR` (P3). New `bitbucket.pipeline.deploy` signal → **R1 v2**: consumes github+bitbucket signals, adds the **ECR image chain** (pushed image → ECR node → observed USES_IMAGE taskdef → unique ECS service ⇒ inferred-high, chain cited); 10 R1 tests. **Live truth:** siemba repos yield concrete targets (api-backend-provapt → siemba-backend-demo/pilot/production · node_service) but those clusters are in an UNCONNECTED AWS account → 0 edges, correctly (P3); the calsaws services are deployed outside this workspace's pipelines. Edges light up the moment Siemba's own AWS account is connected. **Phase B (the map turns red):** `collectAwsHealth` in connector-aws (ELB DescribeTargetHealth, ECS running/desired+rollout, RDS status, firing CloudWatch alarms mapped onto lambda/rds/elb/ecs nodes; worst-state-wins merge; denied checks in `skipped`, never silent) + `applyHealthObservations` (org-scoped `attributes.health`) + migration `0023 app_health_targets` + `HealthPollerBootstrap` (HEALTH_INTERVAL_MINUTES=2 in dev, overlap-guarded) + map red/amber ring + reason badge + detail-panel Health row + a leading **'Service health' finding**. 5 collector tests. **First live tick found real trouble: BOTH RDS instances are STOPPED** (calsaws-rds-instance-1, calsaws-docdb-production → red 'instance status: stopped' on the map, verified in-browser). All gates green (connector-bitbucket 32, connector-aws 74, inference 30, ingest, api 23; typecheck+lint clean). **Next:** Phase C (change timeline: CloudTrail + deploys + PRs in node_events) → D (AI diagnose/get_pr_diff tools). |
| 2026-07-05 | architect | **🎯 North star captured — Operational Intelligence plan written (`docs/plans/operational-intelligence.md`).** The user articulated the endgame: not an inventory map but an operational brain — red boxes when services break, AI tracing a failure through the graph to the deploy + PR diff that likely caused it, and later personalized infra advice. Assessed feasibility honestly (all buildable read-only/agent-less; SSM-dependent depth on raw EC2; RCA = ranked cited hypotheses) and wrote the full multi-phase plan: **A** deploy inference (`repo -DEPLOYS_TO-> service`, evidence-tiered — THE missing keystone edge) ∥ **B** health layer (CloudWatch alarms, `DescribeTargetHealth`, ECS tasks, SSM inventory; new lightweight `health` sync type @1-2min; nodes gain `healthy/degraded/unhealthy/unknown`) → **C** change timeline (new org-scoped `node_events` table: CloudTrail `LookupEvents` + pipeline deploys + PR merges) → **D** AI incident tracing (`diagnose` + `get_pr_diff` tools in the existing agentic loop; honest-absence eval mandatory) → **E** posture/advisory rules (joins security-plan Phase 2 toxic combo). IAM delta listed (all Describe/List/Get). Board deep-dive pointer added. Next work should start at **Phase A ∥ B**. |
| 2026-07-05 | architect | **🗺️ Map rework: one left-to-right architecture flow; multi-env disabled (user direction).** The user found the map unreadable - vertical stacks, LB/DB relationships invisible. Root cause: the lane-per-environment layout ranked only intra-lane edges, but most real edges (ELB→targets, SG→protects, VPC→contains) crossed the Production/'Code & shared' boundary, so they never shaped the layout - nodes piled into vertical columns and edges drew as long detached rails with floating labels. Rewrote `buildLayout` as ONE dagre LR graph where EVERY edge ranks: entry/boundary (SGs, S3, VPC, ECS) left → ELBs + Lambdas middle → RDS/DocDB right; connects-to/protects edges short + attached. Removed the environment/cloud/account grouping UI (segmented control, lane frames, filter chips, map-page copy), the dashboard Environments stat, and the detail-panel Environment row - multi-env is deliberately parked until a customer needs it (env inference stays in the API, data still captured). Verified live in the browser. Debugging note for future-me: a screenshot of a **hidden/background tab** shows a blank React Flow canvas - Chrome freezes rAF + CSS animations there (`document.timeline` doesn't advance, `animate-in` entrance stays pending at opacity 0). Not a code bug; force-finish animations + set the viewport transform manually to verify layouts headlessly. Commit `1242a6d`. |
| 2026-07-05 | architect | **🔄 AWS live sync verified (150 resources) + sync visibility on the Integrations hub + orphaned-run reaper.** The user's real AWS account (siemba/calsaws, static keys) verified → `degraded` (working as designed: probes found `ecr:DescribeRepositories` + `route53:ListResourceRecordSets` denied for `readonly-anmol`) and the onboarding full sync ran live: **status `partial` — 150 resources, 27 edges, 86 signals, 15/17 scopes OK** (ECR + Route53 skipped = the two denials). Then closed the visibility gap that made the user ask "where can I see the sync?": **(1) API** — `ConnectionDto` gains `syncing` (an in-flight queued/running run exists) + `lastSync` (`{status, finishedAt, resources, edges, scopesFailed}` from the latest finished `sync_runs` row) via a new org-scoped `attachSyncInfo` on list/get. **(2) Hub UI** — each connection row now shows a live **"Syncing - pulling the latest data…"** line (polls `router.refresh()` every 4s while in flight, refresh icon spins), then **"Last synced Xm ago · N resources"** (+ amber "· N scopes skipped" when partial, red "Last sync failed" when failed), and degraded connections list their exact **missing IAM actions** (docs/06 §8 finally surfaced in UI). **(3) Reaper (bug found by the feature):** two zombie runs (a Bitbucket `running` since 2026-07-04 — API restart killed the in-process worker mid-run — and a GitHub `queued` from 07-02 with its in-memory job lost) rendered "Syncing…" as a permanent lie **and blocked all future runs via uq_sync_inflight (BR-SYNC-1)**. `attachSyncInfo` now self-heals org-scoped: queued >15min or running >60min → finalized `failed` with `scope_result.reaped` provenance (a cross-org boot sweep is impossible under RLS by design — R8). Verified live in the browser: AWS card shows "Last synced 4m ago · 150 resources · 2 scopes skipped" + the missing-perms line; Bitbucket's phantom "Syncing…" flipped to an honest "Last sync failed" and is re-syncable. Also fixed the session-opening **no-CSS bug**: two concurrent `pnpm dev` trees (one orphaned from a dead terminal) corrupted `apps/web/.next` → stylesheet 404; killed both, cleared cache, single clean restart. Gates: typecheck/lint/prettier/tests green (api 23 unit + web). Next: grant the 2 IAM actions → re-verify → full `connected` sync; Phase-2 toxic-combo rules now unblocked by live AWS data. |
| 2026-07-05 | architect | **☁️ AWS connect via static access keys (I1 goes live-ready) + dev hot-reload + per-page skeletons + Explore de-noise.** Several fixes. **(1) AWS static-key auth:** the connector was AssumeRole-only (config=roleArn, secret=External ID) and the Integrations AWS tile was a stub with no fields — so a user with static IAM access keys (and local dev, where Atlas has no AWS identity to assume from) couldn't connect. Added a `keys` auth mode end to end alongside `role`: `config` infers the mode (roleArn present → role, else keys); `StsStaticCredentialResolver` validates keys + gets the account via STS GetCallerIdentity → same {credentials, accountId} the crawl uses; `AwsTempCredentials.sessionToken` now optional; client-config omits empty token; `resolveCredentials()` branches for verify + crawl (discoverers unchanged). Web AWS tile now has a real form (regions + Access Key ID + Secret Access Key) → create `{authMode:'keys',regions}` + verify `{accessKeyId,secretAccessKey}` (encrypted via secrets broker, same path as Bitbucket); setup steps rewritten for the IAM-user + access-key flow. **69 connector-aws tests** (+9). **Verified end to end with dummy keys against real STS** → clean "AWS rejected these access keys" verdict (whole chain works; real keys will connect). All 18 aws.* node kinds seeded → crawl persists. **(2) Dev hot-reload:** shared packages had no watch task, so `turbo run dev` only watched the apps — package edits ran stale. Added `dev: tsc --watch` to all 9 packages + `dev dependsOn ^build` + `--concurrency=20`; API keeps real tsc + node --watch (NestJS metadata), which restarts on symlinked package dist change (verified). One `pnpm dev` now hot-reloads both sides. **(3) Per-page skeleton loaders** for every app screen (dashboard/settings/integrations/map/ask/explore detail+impact+edge) mirroring each layout — instant nav, no blank-wait hang; also parallelized `graph.service.summary()` across 3 org-scoped connections (~2.9s→~1.4s). **(4) Explore de-noise:** external.package + security.vulnerability now excluded from the estate browse/facets/map (830→95 real nodes); still reachable via explicit ?kind + Insights. |
| 2026-07-05 | architect | **🔐 Security & Vulnerability Intelligence — Phase 1 BUILT & verified live (dependency intelligence).** Shipped the first real security capability end-to-end on the actual Siemba Bitbucket graph. **(1) Dependency graph** — Bitbucket connector now fetches dependency **manifests** (`client.content()` per repo default branch): new `parsers/manifest.ts` (npm/pypi/go/maven incl. `pom.xml`, `groupId:artifactId`), crawl emits `external.package` nodes + `DEPENDS_ON_PKG` edges (cross-provider URN `external:<eco>:package:<name>`, shared w/ GitHub). **(2) OSV enrichment** — a **post-crawl stage** (`packages/ingest/src/osv.ts` OSV.dev client + `osv-enrichment.ts` `runOsvEnrichment`, wired into `createSyncHandler` after `runStagedSync`, best-effort so an OSV outage never fails a sync) turns `package@version` → `security.vulnerability` nodes (`external:vuln:<id>`, migration `0022`) + observed `AFFECTS` edges, each with a provenance row. **(3) Findings** in `graph.service.summary()`: **Vulnerabilities** (severity-ranked), **Blast radius** (a vulnerable package many repos share — one upgrade fixes N), **Dependency sprawl** (one package pinned to many versions) — all cited, auto-flowing into Insights + Ask AI; guidance packs for `vulnerabilities`/`dependency sprawl` in `knowledge.ts`. **(4) Schema fix** — `edges` has no attributes column by design; `persistEdge` now stores edge metadata (the DEPENDS_ON_PKG `version`) in `provenance.evidence` (+ re-points `provenance_id` on conflict so re-syncs refresh), and the sprawl query reads `pv.evidence->>'version'` (a query using `edges.attributes` would 500 the dashboard — caught + fixed before commit). **(5) On-demand CLI** `apps/api/scripts/sync.ts` (`pnpm --filter @atlas/api run sync -- --connection <id> | --org <id> | --all [--provider bitbucket]`) — runs the **exact production handler** (crawl→OSV→infer) for **any company** from the terminal; privileged pool for cross-org discovery, app role + RLS for the scoped sync. **Verified live:** ~391 packages, 681 DEPENDS_ON_PKG, **138 vulnerabilities (7 critical, 33 high, 59 medium)**; top blast radius **axios — 26 vulns across 7 repos**, then golang.org/x/crypto (24), dompurify (17), nodemailer (8×2 repos). Tests green (OSV client 2, connector 23 incl. manifest→pkg→edge, ingest, ai 76, api 23); typecheck + lint + prettier clean. Docs: `docs/plans/security-vulnerabilities.md` marked Phase-1-built. **Phase 2 (the "exposed AND vulnerable" toxic combination + cloud-posture rules) waits on the AWS role.** |
| 2026-07-04 | architect | **Ask Atlas chat redesign + Insights hub (proactive intelligence).** Big UX + product session. **Chat redesign** (per user design feedback): inline **superscript citations** tied to each claim (Perplexity-style, threaded the citation marker to the client) replacing the disconnected `[1][2]` chip row; a **quiet trust line** (dot + label + source count) replacing the loud confidence pill; **less green** (dark user bubbles — green now only the AI-sphere accent + the tiny observed dot); **markdown** (`**bold**`/`code`) rendering; **typewriter** animation fixed for short/smalltalk replies (a `live` flag — instant replies had `streaming=false` at mount so never animated); reopened-chat **loading skeleton**; AI-mark **size-pulse** (scale keyframe, not opacity) while thinking; smalltalk (`hi`/`thanks`) → **friendly replies** not honest-absence; deep-linkable `/ask/:chatId` URLs (replaceState on create so no remount); sidebar polish (collapse toggle, no per-row icons, horizontal separator, highlight-in-place not pinned). **Insights hub** (new): proactive counterpart to Ask Atlas — `GET /insights` returns the graph's grounded findings enriched with the P2 knowledge pack's `guidanceFor` (why/fix/pillar/source); new `/insights` page + nav (Lightbulb) renders **actionable cards** (finding + why-it-matters + how-to-fix + source + 'Ask Atlas' CTA → advisory thread). Provider-agnostic (grows with AWS). Verified live vs Siemba (2 cards: 56 repos no CI/CD → add pipeline; 14 stale → re-sync). Confirmed inline citations work across all AI answer paths (N/E/A markers). All typecheck + lint + 76 ai tests green; pushed. |
| 2026-07-04 | architect | **KE-P2 advisory answers + knowledge packs (provider-agnostic; live on Bitbucket).** Shipped the "how do I optimise/secure/fix this" capability with the **fact/advice trust model** that refines AE-1. **Knowledge pack** (`packages/ai/src/knowledge.ts`, `KNOWLEDGE_PACK_VERSION`): `guidanceFor(category)` → `{why, fix, pillar, source}` keyed by the finding category the graph already emits (Code hygiene, Blast radius, Source health, Freshness, Cross-boundary) — provider-agnostic, with AWS security/encryption/IAM/cost categories **seeded ready** (no findings until the connector lands). **`advisory` intent** (planner rules for "how do I improve/optimise/secure/harden/fix", "what should I fix/prioritise", "recommendations", "review my …") placed before the estate "needs attention" rule. **Advisory path** in `answer.ts`: gather grounded findings (`estateOverview`, +`category` re-added to the port type) → `buildAdvisoryContext` renders FINDINGS as cited facts (`A`-markers) + GUIDANCE as clearly-separated reference → narrate with a dedicated **`ADVISORY_SYSTEM`** prompt that PERMITS best-practice knowledge but enforces "facts cited, advice labelled, never advice-as-fact, no invented problems". New **`advisory` OverallConfidence tier** threaded through `narrate`/stream (system prompt now a `Prepared` field) + web `ConfidenceBadge` ("recommendation", primary). **68 @atlas/ai tests** (advisory eval: findings→cited recommendation @ advisory tier, no-findings→no invented problems; planner advisory classification). Typecheck+lint clean. **Verified LIVE** against the real Siemba Bitbucket estate: "how do I improve my setup" → "56 repositories have no CI/CD pipeline [A1] → Recommendation: implement CI/CD to automate builds/tests…", "14 resources went stale [A2] → Recommendation: re-sync…", confidence tier `advisory`, both facts cited (fixed a prompt-adherence nit so gpt-4o-mini emits bracketed `[A1]` not "Finding A1"). `docs/10` updated. **EPIC KE-P2 (advisory slice) ✅.** Remaining P2 (deferred to AWS): per-resource `describe_config`/`find_by_condition` + the AWS Well-Architected rule library. |
| 2026-07-04 | architect | **KE-P1 finished — WebSocket transport + cancellation + eval set + docs (steps 7-9).** Completed the agentic-loop epic. **(7) WebSocket transport** (DD-P1-5): added `@fastify/websocket`; `ask-socket.ts` registers `GET /ai/ws` on the same Fastify server (via `app.init()`→`registerAskSocket`→`listen` in main.ts). Protocol = first-frame `{t:auth,token,orgId}` (never token-in-URL) verified by the SAME `SupabaseJwtVerifier` + `MembershipService` membership check as the guards (R8), origin pinned to `WEB_ORIGIN`, ping/pong heartbeat, `{t:ask}`/`{t:cancel}`. **Cancellation threaded** end-to-end: added `signal?` to `CompleteRequest` (OpenRouter→fetch, Claude→SDK) and through `retrievalLoop`/`collectLoop`/`answerQuestionStream`/`narrate`/`AiService.askStream`; a `cancel` frame or disconnect aborts the tool-loop + LLM (stops cost), and `askStream` now persists the partial answer in a `finally` so a stopped turn isn't left dangling. **Client** (`browser-api.streamAskWS`): opens the socket, auths, streams the same `AskEvent`s, `signal`→`cancel`; **auto-falls back to SSE** if the socket can't reach `ready` (proxies/WS-blocked nets). `ask-chat` now uses WS + shows a **Stop** button while streaming (abort = clean stop, no error). **(8) Eval set**: +4 golden/adversarial tests — estate/aggregate answered from the computed snapshot (cited to `/summary`, `observed`), a fabricated uncited aggregate CAUGHT by L5, the agentic route grounded+cited, and `retrieval_step` streamed. **61 @atlas/ai tests pass.** **(9) Docs**: `docs/08` §10.2 gains the `retrieval_step` event + the WebSocket section, OQ-API-3 resolved (SSE+WS); `docs/10` agentic-loop note (prior). Verified: WS boots on :4290, bad token → `{type:error}` + close 1008; api/web typecheck + lint clean. **EPIC KE-P1 ✅ COMPLETE.** Next: P2 AWS knowledge pack + advisory ("how do I optimise/secure") once the AWS role lands. |
| 2026-07-04 | architect | **KE-P1 agentic Graph-RAG loop — core engine built + verified live.** Turned Ask AI's fixed single-shot planner into a bounded tool-calling retrieval loop (the real engine from `ai-knowledge-engine-p1-design.md`). **(1) Tool-turn message model** (DD-P1-1): extended `ChatMessage` to a union (`user`/`assistant`+`toolCalls`/`tool`) so the loop feeds results back; OpenRouter maps to OpenAI `tool_calls`/`role:tool`, Claude to Anthropic `tool_use`/`tool_result` (+ accurate stop_reason). **(2) Tool registry** (`tools.ts`): 6 read-only, org-scoped, input-clamped tools over the RetrievalPort (`search`/`get_node`/`get_neighbors`/`traverse`/`timeline`/`estate_overview`), each returning a model summary + citable facts. **(3) Loop** (`loop.ts`): `retrievalLoop` async-generator (yields live `RetrievalStep`s, returns accumulated context) + `ContextAccumulator` (multi-hop generalisation of buildContext — dedups nodes/edges by id so citation binding stays deterministic across hops; renders N/E/A markers). Bounded MAX_HOPS=5/MAX_TOOL_CALLS=12/node-budget=80; dedups repeated calls; recovers from tool errors. **(4) Hybrid router** in `answer.ts`: `lookup`/`architecture` → agentic loop (real providers only); canonical intents (blast/deps/deploy/culprit/timeline/estate) → fast deterministic path unchanged; mock provider always fast (dev/CI). The grounding-gate→narrate→cite→confidence back-half is untouched — trust guarantees intact. **(5) Show-your-work**: new `retrieval_step` SSE event (API forwards generically) + `ask-chat.tsx` renders a live checklist ("Searched the graph ✓ Read a node ✓…"). **57 @atlas/ai tests pass** (5 new deterministic loop tests via scripted mock: tool call→cited context, cross-hop node citations, dedup guard, not-grounded, tool-error recovery); api/web typecheck + lint clean. **Verified LIVE** against the real Siemba graph with gpt-4o-mini: "tell me about the API Backend repository" → the model autonomously planned `search → get_node → get_neighbors` over 60 nodes → a grounded 50-citation answer, with the trace streaming. `docs/10` updated (agentic-loop note). **Remaining P1:** WebSocket transport (step 7), golden+adversarial eval set (step 8), docs/08 SSE (step 9). Not committed yet. |
| 2026-07-04 | architect | **AI Knowledge Engine game plan + P0 estate slice (aggregate questions now work) + Ask AI SSE fix + brand logos.** Big session across debugging, design, and build. **(1) Fixed the Ask AI "stream interrupted" bug** — root cause was the SSE handler's `reply.hijack()` bypassing Fastify's CORS hook, so the streamed response shipped **without `Access-Control-Allow-Origin`** → the browser blocked the cross-origin body ("Failed to fetch"), which Chrome surfaced as a 503. Verified server-side (DIAG: hijack ok, writeHead 200, 7-8s stream) vs client 503, then reproduced with a raw fetch. Fix: set the CORS header + `Vary: Origin` on the hijacked response (inject `ENV`/`WEB_ORIGIN` into `AiController`). Also hardened `getClientToken` (refresh expired Supabase token) + fail-soft auth middleware + web `.env.local` for `NEXT_PUBLIC_*`. **(2) AI Knowledge Engine plan** (user: "complete knowledge base… answer anything about AWS/Bitbucket config + optimisation, RAG inside the system, production-grade"): wrote `docs/plans/ai-knowledge-engine.md` (agentic Graph-RAG; the **fact/advice trust model** resolving the AE-1 tension — facts graph-only+cited, expertise = labelled advice anchored to facts; provider knowledge packs; roadmap P0→P4) + `docs/plans/ai-knowledge-engine-p1-design.md` (implementation-ready agentic tool-loop: tool-turn message model DD-P1-1, hybrid router, bounded loop, tool registry, `ContextAccumulator`, budgets, evals, **+ WebSocket transport §9.1/step7 for the conversation — live cancel/interrupt, smoother progress**). **(3) Built P0 estate slice** — new `estate` planner intent (regex rules for aggregate/ranking/overview questions) + `estateOverview` RetrievalPort method (reuses `GraphService.summary` → Ask AI and dashboard agree by construction) + a third **computed** citation kind (`A`-markers) for aggregate facts. So "who are top contributors", "how many repos", "what needs attention" now route to a grounded, cited org snapshot instead of matching one stray node. **Verified end-to-end against the real Siemba graph** (probe: top contributors Mohit Mishra=87, Sahil Saleem=35, Anmol Bhardwaj=32; most active repos API Backend=49…; pipeline coverage 5/61; intent=estate, grounded, 6 computed cites). **51 `@atlas/ai` tests pass** (new estate + planner cases); api/web typecheck + lint clean; `docs/10` §4.2 updated with the estate intent (cardinal rule 1). **(4) Brand:** wired the two logos (geometric globe = Atlas, green gradient sphere = Atlas AI) via `components/brand.tsx` into sidebar/login/favicon + all Ask-AI surfaces; globe spins (5s, motion-safe). Not yet committed. |
| 2026-07-04 | architect | **BYO-LLM (3 providers) + Ask AI polish + auto-refresh scheduler.** Made the AI genuinely usable on the user's own model + sharpened the Ask experience. **BYO-LLM:** an OpenAI-compatible `OpenRouterProvider` (fetch, streaming + tool-calls, no SDK dep; 3 unit tests) reused for **OpenRouter *and* OpenAI** (endpoint override); **Anthropic** via ClaudeProvider — so **3 selectable providers**. Per-org config: migration `0019 org_llm_config` (+ `0020` widening the provider CHECK to openai), key stored via the **encrypted Secrets Broker** (never plaintext), `resolveProvider()` picks the org's model per-request else the env default. **`setLlmConfig` runs a live probe** (tiny completion) before storing — bad key/model → 422 with the provider's real error, nothing saved. Endpoints `GET/PUT/DELETE /ai/settings` (GET Member, mutate Admin); `ConnectionsModule` exports `SECRET_BROKER`, `AiModule` imports it. **Settings UI:** provider tabs with **brand icons in their colors** (OpenAI mono, Anthropic coral, OpenRouter fork), a **model dropdown with cost-tagged optgroups** (Free/Low-cost/Premium — curated from OpenRouter's live catalogue, 340 models/26 free), per-provider key link, Test&save with live status. Fixed **"Failed to fetch"** = CORS didn't allow `PUT` (added it). **Ask AI polish:** data-aware suggested questions from `/summary` (code orgs → repos-without-CI / top-contributors / most-active-repo *by name*); **conversation history sidebar** (`GET /ai/conversations`, titled by first question, reopen persisted+cited turns, New chat). Citations already deep-link to `/explore/:id`. **Auto-refresh:** in-process scheduler (dev slice of the deferred Scheduler) — migration `0021 app_due_syncs` (SECURITY DEFINER, cross-org) finds stale real sources, `SyncSchedulerBootstrap` triggers per-org re-sync on `SYNC_INTERVAL_MINUTES` cadence (needs durable secrets; skips demo/uncredentialed; BR-SYNC-1). Built + verified (finds Bitbucket, skips demo AWS, boots "Auto-refresh on") but **turned OFF per user (`=0`) — manual Fetch latest for now**; flip on anytime. Also captured the **Security & Vulnerability Intelligence** feature plan (`docs/plans/`, board pointer, memory). All gates green throughout; commits `b33729c`…`c8a094b` pushed. |
| 2026-07-03 | architect | **Consumer-polish avalanche + durable secrets + live data correctness (P-phase, ~20 commits).** Long working session hardening the real-data experience against the live Siemba Bitbucket workspace. **Secrets (the real fix, not a patch):** built a **durable encrypted Secrets Broker** — migration `0018 connection_secrets` (org-scoped RLS, `atlas_app` grants) + `DbSecretBroker` (AES-256-GCM at rest, key from `SECRET_ENCRYPTION_KEY` **never in the DB**, ref `db:<org>:<uuid>` so `get()` sets the org GUC, fails closed on wrong/tampered key); `connections.module` uses it when the key is set, else the in-memory dev broker; docs/13 §7.2 documents the three impls (DD-1a). Migrated the Bitbucket token in (encrypted at rest, 0 plaintext leaks, round-trips) → **"Fetch latest" now works across restarts, no reconnect.** **Dashboard/UX:** **"Fetch latest" button + `POST /connections/:id/sync`** (Admin; graceful "reconnect" when creds unresolvable) + per-source "Sync now"; fixed `last_synced_at` (was never written). **Recent activity** rebuilt human + PR-centric (`recentActivity()`: one line per PR "opened in <repo> by <author>", ordered by real `updatedOn` not ingest time, provider brand logos) — dropped the cryptic raw-edge feed. **Needs attention** + **Recent activity** wrapped in Cards; **Ask Atlas** hero polished (search-style input, graph-relevant examples). Brighter **severity** colors (vivid red/amber/sky). **Explore, product-grade:** shows the **estate only** (repos/services/datastores/cloud — PRs, users/teams, pipelines hidden from list + kind filter); new **Type (category) + Source (provider)** facet filters (`/overview` returns `byProvider`+`byCategory`, estate-scoped) replacing raw kind strings; whole estate on one page (limit 100). Consistent **map lane chips** (Account no longer a heavy solid fill), filter **casing** (Any kind/status/confidence), and an **em-dash→hyphen** sweep (212 across 81 files). **Map:** shows **open PRs only** (166→10) + a **"N open PR" badge** on repos so in-flight work is spottable without expanding all 60. **Data correctness:** org renamed **Acme Verify→Siemba**; removed **Azure+GCP demo** connections (kept AWS demo, surfaced in Explore); **PR crawl coverage fix** — merged crawl now windows/sorts by `updated_on` (was `created_on`), so PRs merged recently but opened weeks ago are captured (found July-3 PRs Bitbucket had but the graph missed). **Dev workflow (finally simple):** `pnpm dev` auto-sources `.env` **and** `turbo.json envMode=loose` (Turbo 2.x strict mode was stripping `DATABASE_URL` from tasks → the recurring `DATABASE_URL is required` boot crash). **AWS prep:** handed the user the verified least-privilege IAM policy + trust-policy template for their DevOps (read-only, no data-plane, External-ID). All gates green throughout; ~20 commits pushed (`3ba4a04`…`2b196e2`). |
| 2026-07-03 | architect | **"Fetch latest info" — manual re-sync button + endpoint.** New `POST /connections/:id/sync` (Admin) enqueues a manual full sync, reusing the onboarding enqueue path (BR-SYNC-1: one in-flight run/conn via `uq_sync_inflight`). It guards that the source is usable **and its credentials are still resolvable** — the dev in-memory Secrets Broker can leave a dangling `secret_ref` after an API restart (or when seeded by a one-off script), so we return a clear *"reconnect, then sync"* error up front rather than enqueueing a sync that fails auth mid-run. Also fixed a latent gap: `connections.last_synced_at` was **read but never written** — the sync runner now stamps it on a succeeded/partial run so the dashboard's "synced N ago" is real. Web: a **"Fetch latest"** button on the dashboard header (Admin/Owner) re-syncs all connected sources, plus a per-source **"Sync now"** in the Integrations hub; both surface progress/reconnect hints inline (no toast dep added). Route verified live (401 auth-guard, not 404); typecheck+lint+tests green; `@atlas/ingest` rebuilt. Committed `a7ffa01`. **Caveat for today:** the current Bitbucket connection's token was seeded by a script (separate process), so the button will say *"reconnect to refresh"* until the user reconnects Bitbucket once via the UI (which also rotates the exposed token) — after that it re-syncs on demand for the life of the API process. Durable secrets (AWS Secrets Manager impl) remain the real fix, deferred to deploy. |
| 2026-07-03 | architect | **Real Bitbucket data now flows end-to-end — 30-day PR insights VERIFIED live; the "hang" was remote-DB latency, not a bug.** Ran the full merged-PR sync to completion against the real Siemba workspace and confirmed the leaderboards populate. Root-caused the earlier "sync hangs on the first repo": it was never hung — the DB is remote Supabase behind pgbouncer, so every record write is a ~0.5s network round-trip; one 60-record repo took **33.6s** and my earlier polls simply gave up before it finished (0 locks, 0 failed scopes). Fixes/changes: (1) switched the merged-PR window from 90→**30 days** (per request) and lowered the per-repo cap 150→**40** so the sync finishes in minutes over the remote DB — leaderboards need ranking, not full history (note: the 1–2 busiest repos' counts are now floors, e.g. portal capped at 40; offered a higher-cap re-sync for exact totals). (2) Full sync **succeeded — 62/62 scopes, 0 failed**: 60 repos · 4 projects · 14 users · 21 pipelines · **166 PRs (155 merged)** · 388 edges. (3) Verified the exact `/summary` insight queries against the data: **Top contributors 30d** = Mohit 90 · Anmol 32 · Sahil 32; **Most active repos 30d** = API Backend 51 · portal 40 · GPT-Engine 30 · reports-gen 11 · finding-hub 9 — matches Bitbucket. Gates green (connector 17 tests, api+web typecheck clean). **Committed + pushed** `3ba4a04`. Temp debug scripts deleted; `.gitignore` now ignores `*.tmp.js`. **Next:** "fetch latest info" refresh button (mapping the connections/sync/secrets surface now — note the dev Secrets Broker is in-memory and was populated by a one-off script, so the API worker can't re-sync without the token being re-supplied). |
| 2026-07-03 | architect | **Insights = 90-day PR window (created-date) + merged-PR crawl; dev hot-reload; onboarding connectors.** Iterated on the code insights with the user (Siemba data): (1) both leaderboards now count **PRs *raised* in the last 90 days** (by `created_on`) — connector records `createdOn` + crawls **merged** PRs raised in the window (paginated, capped 150/repo). Top contributors = authors in 90d; Most active repos = total PRs (open+merged) in 90d; caption states the window; empty-state guides a reconnect (current graph has only years-old open PRs + no `createdOn`, so it's blank until the next sync). (2) Removed the **Busiest projects** widget (per request). (3) **Dev workflow fixed** — repo had only build/start, so nothing hot-reloaded and my `nohup &` servers kept getting signal-killed; added an api **watch** `dev` (`tsc --watch` + `node --watch`, emits Nest DI metadata, no new deps), a turbo persistent `dev` task, root **`pnpm dev`**, and now run both servers as harness-managed background tasks (stable). (4) **Onboarding** "connect a real source" now offers **Bitbucket / Azure / GCP** tabs (+ AWS/GitHub), brand-logo'd, shared with the Integrations hub. Gates green (19). **Blocker:** the PR insights only populate after the user reconnects Bitbucket (rotated token) to re-sync merged PRs. |
| 2026-07-03 | architect | **Insights sharpened — Most active repos + real contribution via merged-PR crawl; Explore brand logos.** User flagged "Top contributors by open PRs" as a weak signal (open PRs = in-flight, not contribution — and we only crawled OPEN). Fixes: (1) **Bitbucket connector** now also crawls recently **merged** PRs (bounded — most-recent page per repo, single request, no pagination) so contribution reflects shipped work; open-PR inventory count filters `state=OPEN` to stay accurate. Takes effect on next sync. (2) New **"Most active repositories"** insight (repos by open PRs — works on existing data); Top-contributors relabelled "by PRs raised". Insights now 4 cards (contributors · active repos · busiest projects · coverage). Also: **Explore rows show real provider logos** (Bitbucket mark for bitbucket rows, aws service icons, etc.) via a shared **`kind-visual`** module the map + Explore both import (deduped the icon/colour maps). Gates green (19). Note: `atlas.service` nodes explained to user as the inferred logical-service layer (R4) — kept. |
| 2026-07-03 | architect | **Code insights on the dashboard — leaderboards + coverage.** Building on the code-inventory work: `/summary` now returns an **insights** block (top contributors by open PRs, busiest projects by repo count, pipeline-coverage %), all graph-derived and click-through to the filtered Explore. The dashboard renders an **Insights** section (two ranked leaderboards + a coverage stat) whenever a code host is connected. Verified live against Siemba: **ROAR 26 · Provapt 17 · R&D 16** repos; top contributors Chandra B (3 PRs) et al; **8% pipeline coverage (5/60)**. Gates green (19). |
| 2026-07-03 | architect | **Dashboard now meaningful for code (not just infra) + map fit/relationships fixes.** With a real code-heavy org connected, the Overview was quietly broken — a Bitbucket-only estate showed all-zero Services/Datastores/Clouds and no findings. `GET /summary` now also returns **code inventory** (repositories, projects, pipelines, contributors, open PRs) and **code-hygiene findings** (repos with no CI/CD pipeline, projects with no repos); the dashboard shows an **Infrastructure** row and a **Code** row, each only when that side is connected. Verified live: **60 repos · 4 projects · 21 pipelines · 14 contributors** + a cited *"55 repositories have no CI/CD pipeline"* finding. Also fixed the map's **blank first paint** (collapse default applies on the first render; fitView runs after a double rAF so it reads the fresh store, not a stale huge box) and added a **Connections** section to the node detail panel — a PR now reads *"Raised by <author>"* / *"In <repo>"*. Gates green (19). |
| 2026-07-02 | architect | **Map scales now — collapsible drill-down + per-type colours.** The real 100+-node Bitbucket workspace exposed that the flat lane-tree doesn't scale (60 repos × pipelines/PRs + members = an unreadable wall). Fixed on the infra map: (1) **collapse-to-containers by default** — every node that contains others (project→repo→pipeline/PR, PR→author via CONTAINS/OWNED_BY) starts folded with a **⊕N child-count** toggle; click to drill in, ⊖ to fold; hidden set is a cycle-safe BFS of descendants. (2) **distinct icon colour per node type** (all code kinds had been one slate colour): project=indigo · repo=blue · pipeline=orange · PR=green · user=pink · package=slate · service=emerald — types read at a glance (tried a left-border accent, removed per user feedback; colour lives in the icon chip). (3) **drop isolated nodes** (no edges) — the disconnected workspace members that were just floating boxes; PR authors stay and surface when you expand their PR. Verified in-browser: Provapt project collapses to ⊕17 and expands to its repos on click. Gates green (19). |
| 2026-07-02 | architect | **Bitbucket connector — LIVE (SDK realization #3), verified against a real workspace.** Built `@atlas/connector-bitbucket` implementing the frozen Connector contract (docs/07b), read-only (P2): Basic auth (Atlassian email + scoped API token from the Secrets Broker), REST client for api.bitbucket.org/2.0 with body-based (`values`/`next`) pagination + bounded 429/5xx backoff, `verify()` probing account/workspace/repo reads (reports missing scopes), and a crawl walking projects → members → repos → each repo's deployment environments (pipelines) + open PRs into the seeded `bitbucket.*` kinds (URN scheme matches migration 0017). Registered alongside aws/github; **17 unit tests** (urn, pure modules, client, verify, full crawl round-trip). Added the **connect+verify UI** (workspace + email + token → `POST /connections` + `/verify`; token → broker, never the DB) and an **in-process dev sync worker** (`SyncWorkerBootstrap`) so connect→verify→sync→graph runs locally (deploy still uses a separate worker). Also updated the guide to **scoped API tokens** (Atlassian deprecated App passwords). **Live-verified:** connected to a real Siemba workspace → auto-sync crawled **109 nodes** (60 repos · 21 pipelines · 14 members · 10 PRs · 4 projects) + 98 edges, 0 scope failures — real API payloads matched the connector's assumptions. Fixed **two bugs found in the process:** (1) `provider` now = URN prefix (see below); (2) `purgeGraphData` nulls `provenance.raw_snapshot_id` before deleting `raw_snapshots` (RESTRICT FK) — disconnect 500'd for any connection with real persisted snapshots (demo used in-memory snapshots so never hit it). Demo GitHub/Bitbucket connections purged for a clean real-Bitbucket view. **Note:** the test API token was entered by the user via the UI (I don't enter credentials into fields) and is to be rotated now that testing is done. |
| 2026-07-02 | architect | **Data-correctness fix (P3) — node `provider` = URN prefix + map group explainer.** Closed the follow-up from the Overview work: `persistNode` set a node's `provider` from `connection.provider` and never updated it on conflict, so a node one connector *references* but another *owns* (the multi-cloud demo's cross-cloud datastores) was stuck with the first writer's provider — **Azure/GCP/GitHub/external nodes were all mislabeled `aws`** (aws=43). A node's provider **is** its URN prefix (`<provider>:…`, docs/05, the canonical identity), so `persistNode` now derives it from the URN and adds `provider = EXCLUDED.provider` to the `ON CONFLICT DO UPDATE` (re-syncs self-heal). For a well-behaved connector this is identical to `connection.provider`; only cross-referenced nodes change. Re-seeded → correct: **aws=25, github=12, bitbucket=6, atlas=3, external=2, azure=2, gcp=2** (52 total). The dashboard/map already URN-derive so they were right; now the column agrees. Also added a **map group-by explainer** — a one-line caption that updates on selection to say what the lanes mean (environment = *where it runs* · cloud = *who runs it* · account = *billing/isolation boundary*). Gates green (17); ingest+api rebuilt, re-seeded, verified. |
| 2026-07-02 | architect | **Overview redesign → consumer-first (inventory · trust · cited findings · activity) + tag/border polish.** Reworked Home from graph-internals (node/edge/certainty counts — QA telemetry) to a consumer's real questions (P1). New **`GET /summary`**: human **inventory** (services/datastores via `node_kinds.category`; environments via `inferEnvironment`; **clouds/accounts + cross-boundary derived from the URN prefix** — robust vs the `provider` column, consistent with the map's `providerOf`), **trust pulse** (source health + freshness), graph-derived **cited findings** (source-health · cross-boundary · single-point-of-failure blast-radius · stale-resources — severity-ranked, each links to its evidence, P3/P4), and a **recent-activity** change feed (reuses `timeline`). **Dashboard** rebuilt: **Ask-Atlas hero** (prefills `/ask?q=` → `AskChat` one-shot auto-ask), at-a-glance stats, **Needs attention** (calm empty state), Recent activity, map preview. `docs/09 §5.2` updated (**OQ-FE-5 resolved**). **Same-session UI polish (user feedback):** (1) **no coloured borders on badges/chips EVER** — env/cloud/cross-boundary chips + role tags → transparent border + tinted fill (shadcn filled-badge pattern); (2) **role tags consistent + visible** (Owner/Admin/Member all filled — 12%-opacity palette tints were invisible on white → bumped); (3) **severity taxonomy + `SeverityBadge`** (reused by findings); (4) integrations **"Add another" → dark** primary button; (5) Explore **"kind" filter → proper `<select>`** populated from the graph's kinds. All gates green (17); API rebuilt + web restarted; verified in-browser (Overview shows 3 clouds / 4 accounts + 2 findings, role badges consistent, chips borderless, kind dropdown 28 opts). **Known follow-up:** demo seed sets `provider='aws'` on Azure/GCP nodes (URN prefix is correct) — a latent P3 data-correctness bug in `demo-estate`; summary/map already URN-derive around it, but the column should be fixed. See [[feedback-shadcn-ui-workflow]], [[project-consumer-vision]]. |
| 2026-07-02 | architect | **Categorical tag taxonomy (colored role/status/env/provider tags) + Members panel reliability.** Per user asks (proper enums, reuse, production-grade, add color+icons). New **`lib/taxonomy.ts`** — the single source of truth for how enum-typed things render: `ROLE_META` (Owner/Admin/Member, privilege-**ranked** + icons Crown/Shield/User + hues amber/blue/neutral), `ENV_STYLE` (semantic env hues + dot), `PROVIDER_META` (brand hex + real brand-logo key). `components/tags.tsx` `RoleBadge` reuses it; members are now **privilege-sorted** (`byRole`). Extended `certainty.tsx`'s `StatusBadge` enum (invited/accepted/expired/revoked) so invitation **and** membership states share one component; **Pending** renders amber. Map **group-by chips** now read at a glance — environments carry a semantic hue + dot, clouds carry their **brand hue + real logo** (`GroupChip`), accounts stay neutral. Also: lightened the success-green light token (`142 72% 29%` → `142 52% 43%`) and made status/freshness labels `capitalize` (Connected/Fresh/…). **Members reliability fix:** the client `OrgPanel` raced Supabase session hydration on fresh nav → intermittent "No members loaded" — now **server-fetched** in the settings page (reliable `shell.token`) and hydrated as `initialMembers`/`initialInvites`; client re-fetches only after an invite. All gates green (17); verified in-browser (role badges, colored chips w/ logos, members reliable). Design-language note: hue is now sanctioned for **categorical identity** in addition to status — see [[feedback-shadcn-ui-workflow]]. **Next: Overview redesign** (approved — lead with Needs-attention + At-a-glance). |
| 2026-07-02 | architect | **Azure/GCP/Bitbucket connect flows + richer multi-cloud demo (members + activity) + GitHub octocat.** Made the non-AWS/GitHub providers first-class so a consumer can add them and see data. **Backend:** migration **0017** widens `connections.provider` CHECK to `aws/github/azure/gcp/bitbucket/gitlab/datadog` + seeds 5 Bitbucket node kinds; `ConnectionProvider` + the connections DTO enum widened to match. **Connect UX:** `AzureSetup` (Entra service-principal, built-in Reader role) / `GcpSetup` (service account, `roles/viewer`) / `BitbucketSetup` (read-scope App password) guides, each read-only-by-construction; the Integrations hub dispatches per-provider setup and the tiles flip to **available**. **Demo estate:** now seeds **5 provider connections** (all "… (demo)"), **6 members** (Priya/Admin, Diego·Mei·Lena/Member, Sam/Owner + the owner) via users-ON-CONFLICT-email + memberships, and **8 activity events** (append-only `audit_events`, spread over time) — so Settings shows real members + a populated Activity log. **Map:** Bitbucket kinds render real logos; **GitHub octocat** mark replaces the wordmark everywhere (regenerated `cloud-icons-data.ts`, 19 icons). Also fixed Ask AI input pinned to the content bottom (`h-[calc(100dvh-7rem)]` flex-col, non-sticky form). **Verified in-browser:** hub shows 5 connected providers, Settings renders all 6 members + activity, map shows the Bitbucket lane. Investigated a "No members loaded" report → root-caused as a **stale render** (panel mounted before the seed; the API returns 200/6 correctly) — no code change. All gates green (17); committed. Live Azure/GCP/Bitbucket crawlers stay creds-gated; vocabulary + connect flow + demo data proven. See [[project-consumer-vision]]. |
| 2026-07-02 | architect | **Multi-account / multi-cloud — the enterprise foundation (system + demo + map).** **System:** migration 0016 seeds Azure (12) + GCP (11) node kinds (URN `azure:<region>:<subscription>:…` / `gcp:<region>:<project>:…`); new **R9 `cross_boundary_connects`** stitches a compute config reference to a datastore ANYWHERE in the org (any account/cloud), flagging crossCloud/crossAccount, partitioned with R3. **Demo:** enriched to a real multi-cloud estate — 2nd AWS account (999988887777) + Azure + GCP + cross-boundary env refs → R9 produces 3 cross-cloud+cross-account links; 46 nodes across 4 providers (live-verified). **Map:** a **Group-by control** (Environment / Cloud / Account) re-lanes the graph (`buildLayout` generalized to a `Grouping`); Azure/GCP category icons; cross-boundary edges span lanes. Verified in-browser. Also fixed the shadcn **badge** (rounded-full pill, font-medium, no shadow, transparent-border status — official variants strictly, per user). Live Azure/GCP crawlers remain creds-gated; model+linker+viz proven. See [[project-consumer-vision]]. |
| 2026-07-02 | architect | **Map polish — real provider icons + motion + colored categories.** (1) **Real logos:** map nodes render the actual AWS service icons (EC2/ECS/RDS/DynamoDB/Lambda/S3/VPC/ELB/Route53/API-GW/IAM/ElastiCache) + GitHub mark; Integrations tiles render real brand logos (AWS/GitHub/Azure/GCP/Bitbucket/GitLab/Datadog). Offline+lean: generated `cloud-icons-data.ts` (19 icons, 39KB, from `@iconify-json/logos` — CC0) + a tiny `<CloudIcon>` SVG renderer; `@iconify-json/logos` is a devDep (regen only). (2) **Motion:** flow edges (CONNECTS_TO/ROUTES_TO/DEPLOYS_TO/STORES_IN/DEPENDS_ON) animate; node hover-lift + fade/zoom entrance. (3) **Category colors** for fallback glyphs (compute/data/network/security/…). All gates green; prod build 5/5. **Next: multi-account / multi-cloud grouping** (map lanes by account→environment) + demo enrichment (2nd account + mock Azure/GCP) — see [[project-consumer-vision]]. |
| 2026-07-02 | architect | **Integrations hub (`/integrations`) — the "add your accounts" home.** Per the user's vision (one page with provider options). A tile per provider: **AWS + GitHub live**; **Azure, Google Cloud, Bitbucket, GitLab, Datadog** shown as "Soon" (the visible roadmap). Each live tile lists its connected accounts with status badges + a **disconnect** control (purges that source's graph — the earlier purge-on-delete), and a **Connect** flow: a side Sheet with the provider's read-only setup steps (shared `provider-setup.tsx`, reused from onboarding) + a "connection name" form → `POST /connections`. New `createConnection`/`deleteConnection` browser-api calls; sidebar nav entry. **Verified in-browser:** tiles render, Connect GitHub → sheet → created "GitHub — production" (pending) → shows under the tile. Onboarding de-duped to use the shared setup. All gates green (17); prod build 5/5 (`/integrations`). |
| 2026-07-02 | architect | **Visual infrastructure map (`/map`) — the consumer-facing picture.** Per the user's vision (see the graph, not tables; understand environments; simple for consumers), built an interactive map. **Backend:** `GET /graph` (bounded nodes+edges, each node carrying a derived `environment` + `accountRef`) + `inferEnvironment` (tag/attribute → naming); demo seed enriched with a staging environment in us-west-2 (prod stays us-east-1) so env + region separation is visible. **Frontend:** `/map` (React Flow + dagre) — resources as icon nodes, connections as edges (solid=observed, dashed=inferred), framed into **environment lanes** (Production / Staging / Code & shared), auto-laid-out; pan/zoom/minimap, environment filter chips, click a resource → side panel (env/region/account + Details/Impact links). Mono theme + certainty language throughout. New sidebar nav entry. Runs entirely on the sample data — no creds. All gates green (17); prod build 6/6 (`/map` route). Servers live on :4290/:4291. Next iteration: Integrations hub page (provider tiles). |
| 2026-07-02 | architect | **In-repo ledger burn-down — purge-on-delete + AWS additive modules + GitHub webhook route.** Three deferred items closed (no infra needed): (1) **Connection purge-on-delete** — `disconnect` now purges the source's graph footprint (nodes cascade edges via composite FK; sweep signals/snapshots/orphaned derived nodes/unreferenced provenance), keeping the soft-deleted row; org-scoped tx; 2 live-PG tests. (2) **AWS additive service modules** — DynamoDB / ElastiCache (PROTECTS + endpoint signal) / API Gateway (ROUTES_TO→lambda from integration URIs), pure + 6 golden tests (`services/additive.ts`); IAM-policy left as an R8 signal not a browsable node (NG6). (3) **GitHub webhook ingress** — `POST /webhooks/github/:connectionId` (rawBody HMAC verify → event map → `app_connection_org` SECURITY DEFINER resolver [migration 0015] → org-scoped incremental sync_run), fail-closed without a secret; `GITHUB_WEBHOOK_SECRET` in `@atlas/config`; 4 live-PG tests; route 401s without a valid signature. All gates green; migrations 0015 applied. Remaining big items are genuinely infra/creds-bound (live connector verify, load/DR, worker process, OpenSearch). |
| 2026-07-02 | architect | **P2 cont. — audit read path + Settings Activity log + API security headers.** Completed the audit feature end-to-end (it was write-only): `AuditService.list` (org-scoped, newest-first, actor email joined from `users`) → `GET /audit` (Admin+, `AuditModule`) → a Settings **Activity log** card (admin-gated via `shell.role`, human-labeled actions + relative time, `EmptyState` when empty). "Trust is visible" (docs/13 §8). Added baseline **security headers** on every API response (`x-content-type-options: nosniff`, `x-frame-options: DENY`, `referrer-policy: no-referrer`) via the same Fastify onRequest hook (dependency-free; HSTS at the TLS edge in deploy). 5th live-PG audit test (list ordering + actor join + limit clamp). `/audit` mapped + 401-guarded; headers verified live; gates green (17); prod web build 5/5. |
| 2026-07-02 | architect | **P2 hardening — first increment (observability + audit log + CI).** Not the whole epic (load/DR/mutation/adversarial-QA are infra-bound, still ledgered) — the in-repo, testable slice. (1) **Observability** (docs/02 §9.4): request **correlation id** via Fastify `genReqId` (reuses inbound `x-request-id` or generates; echoed in the response header through an `onRequest` hook), a structured `LoggingInterceptor` emitting one JSON access line per request (`requestId/method/url/status/durationMs`, no secrets/bodies), and the error filter now reuses `req.id` so the `requestId` the client sees matches the logs. (2) **Audit log** (docs/13 §8): migration **0014** `audit_events` — **append-only** (atlas_app granted INSERT+SELECT, UPDATE/DELETE REVOKEd → tamper-evident) + org-scoped RLS; `AuditService` (global via CoreModule, **best-effort** so it never breaks the audited op) wired into the security-relevant mutations at the controller boundary (`org.create`, `member.role_change`, `member.remove`, `invitation.create`/`revoke`, `connection.verify`/`disconnect`, `demo.seed`), each carrying actor + correlation id. `AuditEventRow` in `@atlas/db`. **4 live-PG tests** (record+read, tenant-isolation, append-only rejects UPDATE/DELETE with permission-denied, best-effort swallows a bad write). (3) **CI**: new `web-build` job (production `next build` — catches build/NODE_ENV regressions); the audit test runs in the existing `integration` job. Migration 0014 applied to live DB. All gates green (17); API boots clean. **Next: remaining P2 is infra-bound** (load/perf, DR drill, mutation tests, adversarial QA agent + E2E/contract/AI-eval CI, security headers). |
| 2026-07-02 | architect | **P1.3 DONE — state polish converged on the pattern library.** Extended `EmptyState` with a `tone` (default/danger) + added `ErrorState` (the one "couldn't load/compute" surface), then replaced bespoke inline states across: Explore nodes-list empty (`SearchX`), Explore load-error, impact-panel compute-error (bare, in-card), settings connected-sources empty (`Plug`). Added route-specific `explore/loading.tsx` (table-shaped skeleton, matches the page vs the generic app skeleton). **Color normalize:** the mono theme defines shadcn's `--destructive` as *grayscale*, so login/create-org form errors rendered gray (easy to miss) — switched all error text/badges to the semantic **`danger`** (red) token (login, create-org, Ask chat inline error). Honest-absence (impact "truthful empty result"; Ask `insufficient`) kept + consistent. Gates green; prod build 5/5 static, 11 routes. |
| 2026-07-02 | architect | **`next build` fixed (root-caused).** The repo-wide `next build` failure (`<Html> should not be imported…` on `/404`,`/500`) was NOT version drift or our code — `.env` sets `NODE_ENV=development`, and sourcing it before `next build` made React use its **dev** SSG renderer → null dispatcher → `useContext` null on prerender → Next fell back to the pages-router error page → the misleading `<Html>` error. Proved with a zero-hook component. Fix: `apps/web` `build` script → `NODE_ENV=production next build` (overrides the ambient dev value). Build now green (11 routes) with the dev `.env` still sourced. Bisected via minimal-app build + version pins (reverted — they weren't the cause). See [[reference-local-dev-runbook]]. |
| 2026-07-02 | architect | **P1.2 DONE — onboarding wizard + gated `POST /demo/seed` + a reusable UI system.** Empty graph now lands on a real first-run experience instead of a "go to settings" stub. (1) **Shared seeder** — extracted the Shopyard estate + seeding into `@atlas/ingest` `demo-estate.ts` (`seedDemoData`, org-scoped via `withOrgScope`/atlas_app — no admin needed, so the API can call it directly); the CLI `seed:demo` script and the new API endpoint both call the *same* function (DRY). Verified live: 36 nodes, 29 observed + 12 inferred edges, sync succeeded. (2) **API** — `DemoModule`: `POST /demo/seed` (Admin-only, `TenantScopeGuard`), gated to empty orgs (409 if a real source is connected), idempotent by URN. API boots with the module; route mapped + 401-guarded. (3) **Frontend** — `Onboarding` component: primary **"Load sample data"** (client → `/demo/seed` → `router.refresh`, admin-gated via new `shell.role`) + AWS/GitHub tabbed setup instructions with copy-ready least-privilege IAM policy / App read-scopes. Wired into the dashboard empty state. (4) **Repeatable UI system** (user asked for structured, reusable UI): extracted `patterns/EmptyState`, `patterns/Steps`, `ui/CodeBlock` primitives + pulled shadcn `tabs` via CLI — onboarding composes the system. (5) Added app-router `not-found.tsx` + `global-error.tsx` (designed error states, docs/09 §7). All `pnpm run check` gates green (17). **Env issue found:** `next build` is broken repo-wide (next 15.5.19 + react 19.2.7 drift vs the 15.1.x green builds used → pages-router error-page prerender throws `<Html>`); affects the committed baseline, not P1.2. Servers restarted (API :4290, web :4291). See [[reference-local-dev-runbook]]. **Next: P1.3 (state polish).** |
| 2026-07-02 | architect | **Phase P started (plan: seed→P1→P2, skip live-creds). Demo data + mono UI overhaul + P1.1.** (1) **Demo seed** — `packages/ingest/scripts/seed-demo.ts` drives the real MockConnector→runStagedSync→runInference to populate an org with a "Shopyard" estate (36 nodes, 29 observed + 12 inferred edges, atlas.service nodes); idempotent; `SEED_EMAIL`/`SEED_ORG_ID` (seeded *Acme Verify*). (2) **UI** — shadcn/ui, **monochrome black&white** theme (user directive) + semantic **status colors only** (success/warning/danger); replaced top-bar with a **collapsible sidebar** in an **`(app)` route group** (persistent `layout.tsx` + `loading.tsx` → instant nav); dashboard→`/dashboard`, `/` redirects/onboarding; added **`/settings`**; `requireShell()` React-`cache()`d. **shadcn CLI wired** (`apps/web/components.json`); badge re-pulled from registry; see [[feedback-shadcn-ui-workflow]]. (3) **P1.1 ✅** — `GET /overview` + rich dashboard (counts/kinds/certainty-mix/sources). Run notes: [[reference-local-dev-runbook]]. shadcn MCP init'd → `apps/web/.mcp.json` (move to repo root + restart to load). **Next: P1.2 onboarding wizard.** |
| 2026-07-01 | architect | **UI refresh — shadcn light theme + collapsible sidebar (design pass over G4).** Adopted the full shadcn/ui token system in **light mode** (background/card/primary/muted/accent/destructive/border/ring/sidebar), with the **certainty visual language** kept as first-class colors (observed/inferred-high/inferred-low/stale, now light-tuned with tinted badges). Vendored the canonical shadcn components (button, card, badge, input, separator, sheet, tooltip, dropdown-menu, skeleton, and the **collapsible sidebar** + `use-mobile`). Replaced the top-bar `AppShell` with a real **sidebar layout**: `AppSidebar` (brand + nav w/ active state + user dropdown w/ sign-out) beside a `SidebarInset` with a sticky header (sidebar toggle + ⌘K search box). Restyled login + onboarding + filters as shadcn blocks; migrated every page off the old dark tokens. Deps added: cva, @radix-ui/{slot,separator,dialog,tooltip,dropdown-menu}, tailwindcss-animate. `pnpm run check` green (17); `next build` green; CI green. Also fixed the local-dev 500 (stale 19h servers + `.next` deleted mid-build) — see [[reference-local-dev-runbook]]. |
| 2026-07-01 | architect | **G4.4 DONE — Ask AI surface + ⌘K; Sprint G4 & EPIC G COMPLETE.** **`/ask`** is a client chat that streams the G3.5 SSE endpoint (`streamAsk` parses SSE frames off a `fetch` body reader — EventSource is GET-only, and the endpoint is POST). Tokens append live; **citations** stream in as numbered chips linking to `/explore/[id]` / `/explore/edge/[id]` (P4 end to end — the AI's sources are the same nodes/edges Explore shows); a **ConfidenceBadge** tiers each answer and caveats render inline. The **four designed states** are all realized (docs/09 §7): *empty* (example prompts you can click), *streaming* (typing dots → caret), *answered* (cited + tiered), and **honest-absence** (`confidence: insufficient` → a muted "I don't know", never a fabrication — US-11, the AI's grounding gate surfaced in the UI). **⌘K command palette** (`CommandPalette` + a top-bar `CommandTrigger`, connected by a `window` custom event): keyboard-first (⌘K/Ctrl-K to open, ↑↓ to move, ↵ to open, Esc to close), debounced `/search`, results navigate into Explore — the *same* hybrid search seam the AI uses for entity resolution (docs/11 DD-1). Interactive client calls use the public Supabase access token (RLS + our GUC isolation still enforce access; nothing privileged is exposed). Needed `orgId` threaded through `AppShell`. Clean `next build` green (all routes: /ask, /explore*, /login, /); `pnpm run check` green (17 tasks). **EPIC G done — graph is queryable + explorable + answerable, cited and confidence-tiered end to end. Next: Phase P (P1 onboarding/UX → P2 hardening) → GA.** |
| 2026-07-01 | architect | **G4.3 DONE — impact analysis (blast-radius + dependencies), list-first with why-chains.** New route **`/explore/[id]/impact`** renders both G2 recursive-CTE traversals side by side: **Blast radius** (downstream — what breaks if this resource changes/fails, US-4) and **Dependencies** (upstream — what it needs). The reusable `ImpactPanel` is **list-first for a11y** (docs/09 §5.4 — the list is the source of truth; the WebGL graph canvas is an *optional* enhancement, deferred to the ledger): impacted nodes are grouped by **hop distance**, each showing its **pathConfidence** (the weakest edge on the path — P3, weakest-link) and an expandable **why-chain** built from native `<details>` (no client JS) that lists the edges back to the root, each linking to its `/explore/edge/[id]` evidence page (P4 provenance end to end). Truncation and traversal warnings are surfaced in a banner (never silently dropped, A21); a zero result reads as "a truthful empty result, not a failure" (honest-absence, consistent with the AI layer). Added an "Impact analysis" action to the node-detail header. `next build` green (8 routes); `pnpm run check` green (17 tasks). **Next: G4.4** (Ask AI surface + ⌘K → Sprint G4 & Phase G complete). |
| 2026-07-01 | architect | **G4.2 DONE — Explore surface (nodes list + node detail + provenance + edge "why?").** All server-rendered (secrets stay server-side, NFR-22). Added `requireShell()` — a session+org guard for org-scoped pages (no session → /login; no org → home create-org) — and `graph-types.ts` (web mirror of the read-API shapes). **`/explore`**: a GET-form filter bar (q · kind · status · confidence) so the URL is the source of truth (shareable, back-button, no-JS), a nodes table showing **certainty (ConfidenceBadge) + freshness (FreshnessTag) per row**, and keyset "Next page" carrying filters. **`/explore/[id]`**: node detail — header with confidence+freshness, attributes, a **Provenance** card (source / observed / sync-run / raw-snapshot, or an honest "derived, not directly observed" when none), and **Connections** split into incoming/outgoing with each edge's type + confidence + origin. **`/explore/edge/[id]`**: the **"why?"** provenance page (P4) — origin (observed vs inferred), the rule that derived it, the evidence JSON, and the raw source. Cross-tenant or missing ids render a graceful not-found (404-as-not-found, R8 — never leaks existence). `next build` green (7 routes); `pnpm run check` green (17 tasks). **Next: G4.3** (blast-radius panel + neighbor subgraph, list-first for a11y). |
| 2026-07-01 | architect | **G4.1 DONE — frontend foundation; login now lands on a real UI.** (Answers the user's "why no proper UI after login" — before this the web app was only the F1.5 login shell; the Explore UI is Sprint G4, now started.) Stood up the design system per docs/09 §3: Tailwind v3 + PostCSS, dark theme, and the **certainty visual language** as CSS-var tokens (`--observed`/`--inferred-high`/`--inferred-low`/`--stale`/`--danger` etc.) so trust is legible everywhere (P3/P4/trust-visible). Hand-vendored shadcn-style primitives (`Card`, `Badge`) + `cn()` (clsx+tailwind-merge); reusable certainty components `ConfidenceBadge`/`FreshnessTag`/`CitationLink`. `lib/api.ts`: `getSession` (Supabase server session → token/email) + `apiGet` (attaches `Authorization: Bearer` + `X-Atlas-Org`). `AppShell` (sticky TopBar: brand + org chip + nav Dashboard/Explore/Ask/Settings w/ lucide icons + email + sign-out; bounded content). Server-rendered `Dashboard` (docs/09 §5.2): connections list + recent-nodes preview (both with empty states). Rewrote `page.tsx`: no session → **Landing** (hero + Sign in); session but no membership → **create-org card**; authed+org → **AppShell + Dashboard**. `next build` green (5 routes); `pnpm run check` green (17 tasks). **Next: G4.2** (Explore nodes list + detail + provenance drawer + edges). |
| 2026-07-01 | architect | **G3.5 DONE — SSE /ai endpoints + conversation persistence + API wiring; Sprint G3 (AI engine) COMPLETE.** Added `answerQuestionStream` (SSE-shaped events: retrieval→token→citation→confidence→done). `AiModule` in the API: **`GraphRetrievalPort`** adapts the engine's RetrievalPort over the G2 GraphService + SearchProvider (the only seam between the NestJS-free `@atlas/ai` and the API; org-scoped, AE-7); the **LLM provider** is Claude when `ANTHROPIC_API_KEY` is set, else a dev `MockLLMProvider` (so retrieval/grounding/citation all work in dev/CI without a key). Migration **0013** adds `ai_conversations` + `ai_messages` (org-scoped RLS, R8). Endpoints (Member+): `POST /ai/conversations`, `GET /ai/conversations/:id`, `POST /ai/conversations/:id/messages` (manual SSE over the raw reply — bypasses the JSON envelope). `askStream` persists the user + assistant turns (assistant carries citations + overall confidence, so the transcript stays cited/tiered). 3 live-PG service tests (grounded+cited+persisted, honest-absence stream, cross-org 404); API boots clean with AiModule (/health 200). **G3 exit met** (canonical cited answers, honest-absence, eval green). **Next: G4 (Explore UI).** |
| 2026-07-01 | architect | **G3.4 DONE — answer pipeline (narrator + citation engine + confidence + eval).** `answerQuestion` orchestrates plan→retrieve→gate→narrate→deterministic post-process. If the gate fails it returns honest-absence **without ever invoking the LLM** (L1 — a wrong-context model can't fabricate). Narration streams from the LLMProvider at low temperature (L3). **Citation engine** (DD-5/L4): parses `[N#]/[E#]` markers, binds them to real node/edge ids + `provenanceUrl`, dedup + numbered by appearance; markers with no matching context fact are **dropped** (the model can't invent a source). **Uncited-claim detector** (L5): flags factual sentences (≥6 words) carrying no marker, excusing hedge/absence sentences — a hallucination is surfaced, not hidden. **Confidence scorer** (§5): overall = weakest cited tier (weakest-link, P3); caveats from the context's FRESHNESS notes. **AI eval set** (L7, docs/14 §11) over the MockLLMProvider — canonical (blast-radius answered, cited, `inferred-high`), honest-absence (unresolved entity + out-of-scope → refuse, `insufficient`, 0 citations), adversarial (a tempted fabrication is BLOCKED by the gate or CAUGHT by L5): escaped-hallucination rate 0 (<1% bar). 10 unit/eval tests; 37 in `@atlas/ai`. Gates green. **Next: G3.5** (SSE /ai endpoints + within-session memory + wire RetrievalPort adapter + ClaudeProvider into the API). |
| 2026-07-01 | architect | **G3.3 DONE — retrieval orchestration + context builder + grounding gate.** `orchestrate` executes the typed plan against the bounded, org-scoped RetrievalPort (retrieval-first — no LLM yet): blast_radius/dependents → inbound blastRadius; deploy_mapping → inbound DEPLOYS_TO edges; culprit → inbound CHANGED_BY + timeline window; timeline → window query; lookup/architecture → both-direction edges. `buildContext` (DD-3, the top hallucination defense): assembles the compact CONTEXT block where **every fact carries a stable `N#`/`E#` marker + `cite:<id>` + confidence + evidence**, with a FRESHNESS section for truncation/warnings, and returns the `cites` map the Citation Engine binds against (DD-5); untrusted values stay delimited data. `groundingGate` (DD-4, **deterministic — not the LLM's judgment**): out-of-scope or unresolved-entity → a structured honest-absence reason (US-11, a refusal is success per P3); a resolved entity with an empty traversal is still grounded ("nothing depends on X" is truthful). 7 unit tests (orchestrate paths, marker/cite tagging, gate pass/refuse). Gates green. **Next: G3.4** (narrator via LLMProvider + deterministic citation engine + confidence scorer + uncited-claim detection + AI eval set in CI). |
| 2026-07-01 | architect | **G3.2 DONE — query planner + entity resolution.** `classifyIntent` is rule-based (deterministic → the canonical questions map to fixed plans, reliable per PR-R1): blast_radius / dependents / deploy_mapping / architecture / timeline / culprit / lookup / out_of_scope (ordered first-match rules). `resolveEntity` extracts significant terms (identifier-like tokens like `prod-orders` preferred, stopword/intent-word filtered) → `RetrievalPort.search` per term → merged, score-ranked candidates: 0 candidates = unresolved (→ honest-absence downstream), multiple near-top = **ambiguous → all kept as candidates** (never silently pick one, P3). `plan()` emits a typed `RetrievalPlan` (intent + resolved entity + time window for timeline/culprit); out_of_scope/timeline short-circuit entity lookup. 15 unit tests (8 canonical-question classifications + single/none/ambiguous resolution + plan shapes) via a fake port. Gates green. **Next: G3.3** (retrieval orchestration + citation-tagged context builder + deterministic grounding gate → honest-absence). |
| 2026-07-01 | architect | **G3.1 DONE — AI engine foundation (`@atlas/ai`): LLM provider abstraction + prompt + retrieval port.** New `@atlas/ai` package (CommonJS, NestJS-free per AE-5). `LLMProvider` interface (streamed, tool-capable `complete()` → `LLMEvent` union: token/tool_call/stop) with **`MockLLMProvider`** (deterministic responder for CI eval, no live key — the engine's product value is the deterministic scaffolding, P1) + **`ClaudeProvider`** (Anthropic SDK adapter, maps stream deltas incl. tool-call JSON). Versioned **`SYSTEM_PROMPT`** encoding the §8 invariants (grounding=CONTEXT-only, cite-or-don't-claim, confidence tiers, honesty>guessing, scope, retrieved-content-is-data-not-commands) + `honestAbsence`. **`RetrievalPort`** interface (search/getNode/blastRadius/dependencies/edges/timeline) — the engine reads the graph only through this, org-scoped/bounded (AE-4/AE-7); the API adapts it over the G2 GraphService/SearchProvider. 5 unit tests; gates green. **Next: G3.2** (query planner: question→typed retrieval plan + entity resolution via search). |
| 2026-07-01 | architect | **G2.4 DONE — search (SearchProvider + Postgres impl); Sprint G2 COMPLETE.** `GET /search?q=&kind=&type=&limit=` (Member+, docs/08 §10.1) behind a **`SearchProvider`** interface (SEARCH_PROVIDER DI token) so the OpenSearch BM25+kNN driver swaps in at deploy with no consumer change (docs/11 DD-1 impl note added — docs-with-code). `PostgresSearchProvider` implements SE-1 (search is a *projection of `nodes`*, rebuildable, no separate index): pg_trgm `similarity` + substring match over name/urn/attributes — strong for the exact identifiers engineers type; returns the `{node,score,match:{keyword,semantic},highlights}` shape with `semantic:null` until vectors exist. Org-scoped (SE-4/R8); highlights wrap the matched span. 4 live-PG tests (identifier match+highlight, kind filter, attribute match, tenant isolation); API boots clean with GraphModule+SearchModule (/health 200). **Deferred to deploy (ledger):** OpenSearch driver + embedding/index pipeline (§3–§8), semantic ranking. **Next: G3 (AI engine) ∥ G4 (Explore UI).** |
| 2026-07-01 | architect | **G2.3 DONE — /timeline + /edges/:id evidence.** `GET /timeline?since=&kinds=&limit=` (US-5 "what changed this week"): new/updated nodes (created if first_seen≥since, else updated by last_seen) + newly-created edges since the timestamp, merged and ordered newest-first; `kinds` filters node changes; future `since` → empty. `GET /edges/:id`: full edge detail — from/to node summaries, `evidence`, `rule` (`key@version`), `provenance` (source/observedAt) + raw-snapshot ref for P4 click-through. Both Member+ and org-scoped (cross-tenant/malformed → 404). `TimelineQuerySchema` (z.coerce.date). 9 live-PG GraphService tests (added edge-detail + tenant-isolation, timeline window + kind filter + future-since). Gates green. **Next: G2.4** (search: SearchProvider abstraction + Postgres hybrid; OpenSearch driver deferred to deploy). |
| 2026-07-01 | architect | **G2.2 DONE — blast-radius + dependencies traversals (the signature endpoints).** `GET /nodes/:id/blast-radius` (inbound impact closure — "what breaks if this is deleted", US-4) and `/nodes/:id/dependencies` (outbound — "what this depends on", US-9), sharing a bounded recursive-CTE `traverse`. Walks the impact-bearing edge set (CONNECTS_TO/DEPENDS_ON/ROUTES_TO/STORES_IN/DEPLOYS_TO, overridable via `edgeTypes`); **cycle-guarded** (edge-path array `<> ALL`), depth ≤6 + nodeBudget ≤500 **clamped with `warnings`** (never unbounded, A21). Each impacted node returns `distance`, the **`via` why-chain** (each edge with type/confidence/evidence/rule/provenanceId, for cited AI answers, P4) and **`pathConfidence` = the weakest edge on the path** (docs/05 §8); `minConfidence` prunes low-confidence paths (the P3 high-confidence-only view). 7 live-PG `GraphService` tests: 2-hop chain, weakest-edge pathConfidence, minConfidence exclusion, outbound deps, depth-clamp warning. Gates green. **Next: G2.3** (/timeline + /edges/:id evidence). |
| 2026-07-01 | architect | **G2.1 DONE — graph read API (nodes list/detail/edges/neighbors + cursor pagination).** New `GraphModule` in the API (Member+ via TenantScopeGuard/RolesGuard): `GET /nodes` (filters: kind/region/status/confidence/`q` name-ILIKE; **keyset cursor** over (last_seen desc, id desc), opaque base64url, stable under concurrent crawl writes — DD-3), `GET /nodes/:id` (detail + latest raw-snapshot provenance for P4 click-through), `GET /nodes/:id/edges` (in/out/both + type/confidence filters), `GET /nodes/:id/neighbors` (depth-1 subgraph, node-budgeted). All org-scoped via withOrgScope (RLS; malformed/cross-tenant id → 404). Fixed `parseBody` to infer the schema OUTPUT type so `.default()`/`z.coerce` query schemas type correctly; added `invalid_cursor` error code. 6 live-PG `GraphService` tests (list+filter+paginate, detail+provenance, 404s, edge direction, neighbor subgraph, tenant isolation); API vitest gains DB timeouts. Gates green. **Next: G2.2** (blast-radius/dependencies recursive-CTE traversals with why-chains + pathConfidence). |
| 2026-07-01 | architect | **G1.4 DONE — determinism + precision + infer-stage wiring; Sprint G1 (inference) COMPLETE.** Pure `evaluateAll` (mirrors the engine's dependency-ordered fold, no DB) drives the quality tests. **Determinism golden file**: the docs/05 §9 worked example reproduces an exact edge set (CONNECTS_TO ecs→rds via R2, CONNECTS_TO lambda→rds via R3, DEPLOYS_TO repo→ecs via R1, IMPLEMENTS + RUNS + the derived atlas.service via R4, OWNED_BY svc→team via R5, CHANGED_BY svc→pr via R6); re-run yields an identical plan (IE-1/A19); every edge is tiered + carries evidence (P4/DD-4). **Precision-sampling harness (≥95%)**: labeled fixtures incl. negatives (R2 wrong-port, R8 wildcard-IAM) → **100% precision, zero false positives**. **Infer stage wired**: the sync worker runs `runInference(ALL_RULES)` after every non-failed sync (docs/05 §6.1); an inference error is logged but never fails the sync (P7, converges next run). Proven sync→infer end-to-end (a mock sync emitting a workflow-deploy signal yields a persisted DEPLOYS_TO). `@atlas/ingest` now depends on `@atlas/inference`; API boots clean with the transitive dep. 25 inference tests + 12 ingest tests. **G1 exit criterion MET.** **Next: G2** (read API + traversals/blast-radius + OpenSearch hybrid search). |
| 2026-07-01 | architect | **G1.3 DONE — R2/R3/R8 data-dependency edges (all 8 MVP rules now live).** **R3 config_ref_connects** (inferred-high): a Lambda/ECS env-var value referencing a crawled RDS endpoint host / S3 bucket / DynamoDB table → CONNECTS_TO (rds) or STORES_IN (s3/ddb); precise matching (exact / `s3://` / ARN-suffix) to avoid substring noise; evidence stores the env KEY + matched resource, never the raw value (may hold secrets). **R2 sg_correlation_connects** (inferred-high): resolves SG reachability — a compute node's SG allowed (ingress) into a datastore's SG on the engine port (RDS `endpointPort`/engine-derived) with a shared VPC → CONNECTS_TO; built from PROTECTS observed edges + `aws.sg.rules` signals + groupRef→SG-node mapping. **R8 iam_access_connects** (inferred-LOW, P3): observed ASSUMES_ROLE + an `aws.iam.policy_statements` Allow with a SPECIFIC resource ARN (wildcards ignored) → CONNECTS_TO/STORES_IN; low because permission ≠ use. Shared `refs.ts` (engine→port map, ARN→node resolution). 6 pure tests; ALL_RULES now R1–R8; existing live-DB integration unaffected (22 inference tests total). Gates green. **Next: G1.4** (determinism golden files + precision-sampling ≥95% + wire the infer stage post-sync). |
| 2026-07-01 | architect | **G1.2 DONE — R4 (atlas.service) + R5-propagation + R6 (CHANGED_BY).** Extended the engine so rules emit **derived nodes** (upserted first → their edges resolve in-scope) and read EARLIER rules' inferred edges in one dependency-ordered pass (R1→R4→R5/R6, accumulator folds each rule's output back into the input). **R4 service_derivation**: from each high-confidence `DEPLOYS_TO(repo→runtime)`, derive an `atlas.service` node (`atlas:<org-slug>:service:<repo-name>`) + `IMPLEMENTS`(repo→svc) + `RUNS`(runtime→svc); high-only (DD-1: a service is only as trustworthy as its deploy link); one service node per repo with RUNS×N for multiple runtimes. **R5 ownership_propagation**: observed repo `OWNED_BY` + `IMPLEMENTS` → service `OWNED_BY` (inferred-high). **R6 pr_changes_service**: a merged-PR `github.pr.files` signal + repo `IMPLEMENTS` → service `CHANGED_BY` pr (high if repo implements exactly one service, low for monorepo — honest culprit attribution, P3/US-6). `nodes.connection_id` made **nullable** (migration 0012 + `docs/04` §5.3) since derived nodes have no single connection; connection-scoped stale-reconcile naturally skips them. 16 inference tests (12 pure + 4 live-DB, incl. the full **R1→R4→R5→R6 worked-example** from docs/05 §9 reproduced end-to-end); ingest still green. Gates green. **Next: G1.3** (R2 SG-correlation, R3 env-refs, R8 IAM-access → CONNECTS_TO/STORES_IN). |
| 2026-07-01 | architect | **G1.1 DONE — inference engine core + signals persistence + R1 (Phase G started).** New `signals` table (migration 0010, org-scoped RLS; `docs/04` §5.4b added — docs-with-code) persisted during sync: the runner now calls `extractSignals`→upsert by (org, subject_urn, kind) (+`signals` stat). New **`@atlas/inference`** package: pure versioned `Rule` contract (deterministic, DD-2/P9) → candidate edges; the engine loads nodes+signals+observed-edges, resolves URNs→ids, **upserts inferred edges with provenance (rule id + evidence, P4)**, and **retires (status='retired', not delete — P4) any active inferred edge a rule no longer produces** → **convergent (IE-4: a no-change re-run writes nothing)**. `inference_rules` seeded (migration 0011: R1–R8 metadata, keyed for edge FK). **R1 `repo_deploys_to_runtime` → DEPLOYS_TO**: reads `github.workflow.deploy` signals, resolves targets vs AWS runtime nodes — exact ARN or a unique cluster/service (or function) name → `inferred-high`; multiple candidates → several `inferred-low`, never one wrong high (P3). 11 tests: 4 pure R1 tiering + 3 live-DB engine (derive+provenance, converge zero-churn, retire-on-evidence-loss); ingest still green (11). Gates green. **Next: G1.2** (R4 atlas.service + IMPLEMENTS/RUNS, R6 CHANGED_BY, R5 ownership propagation). |
| 2026-07-01 | architect | **I2.4 DONE — GitHub crawler CODE-COMPLETE (live client + crawl + webhook + wiring); Phase I complete.** `FetchGithubClient`: bearer install-token, `Link` pagination (+ `itemsKey` for `/installation/repositories`), header-aware rate-limit/retry (primary `x-ratelimit-reset` wait, secondary `retry-after`, 5xx backoff — all bounded, injectable sleep/clock). `crawlRepo` fetches repo detail + CODEOWNERS/manifests/workflows + recent PRs and yields repo/workflow/PR nodes **plus** the edge-target team/user/package nodes so OWNED_BY/DEPENDS_ON_PKG resolve in-scope; `plan()` = one scope per installed repo (bounded, resumable); one installation token+client cached per run (mirrors AWS AssumeRole). **Webhook HMAC verify** (timing-safe sha256, GHR-8/docs/13 §5) + event→descriptor mapping. **Wired into API:** real `GithubConnector` registered for "github" (factory injects App token provider + Secrets-Broker accessor); API boots clean with BOTH connectors (DI verified, /health 200); enqueue-on-verify already generic. 42 connector-github unit tests (webhook, client pagination + rate-limit via fetch mock, crawl + connector plan/discover/fetchDetail via fake client). **Open:** live-install verification (needs App creds) + webhook API route/worker (deploy). **Next: G1 (inference engine + edges)** — consumes the emitted signals to derive DEPLOYS_TO/CONNECTS_TO/etc. (docs/05 R1–R8). |
| 2026-07-01 | architect | **I2.3 DONE — GitHub pure parsers + entity modules (golden fixtures).** Pure parsers: **CODEOWNERS** (rules, comment/blank skip, `@org/team` vs `@user` vs email classification, distinct-owner dedup), **dependency manifests** (npm `package.json` deps+devDeps, Python `requirements.txt`, Go `go.mod` require block — direct deps only, other ecosystems additive, GHR-7 fail-soft), **workflow deploys** (`yaml`-parsed: AWS actions, ECS `with: {cluster,service}`, `run:` `--cluster/--service/--function-name` flags, ARNs anywhere). 6 entity modules dispatched by kind: **repository** (node + OWNED_BY from CODEOWNERS + DEPENDS_ON_PKG from manifests), **pull_request** (node + changed-files signal for R6), **workflow** (node + CONTAINS(repo→workflow) + deploy signal for R1), **team/user/package** (leaf nodes so the edges resolve). **DEPLOYS_TO stays inferred (R1/G1) — the connector emits deploy targets as signals, never a possibly-wrong observed edge (P3).** Connector now dispatches normalize/observedEdges/extractSignals; plan/discover/fetchDetail stubbed (I2.4). 31 unit tests (parsers + module golden fixtures). Gates green. **Next: I2.4** (live Octokit discoverers + rate-limit/retry + webhook HMAC ingress + wire into runner/registry/API). |
| 2026-07-01 | architect | **I2.2 DONE — GitHub App auth + verify/health + permission detection.** `GithubAppTokenProvider` signs a short-lived App JWT (RS256; iat backdated 60s for skew, exp +9min < GitHub's 10-min cap) using Node `createPrivateKey` (accepts the PKCS#1 key GitHub issues AND PKCS#8), then exchanges it for an **installation access token** (≤1h, in-memory, read-only — the GitHub analogue of AWS AssumeRole). App private key comes from the Secrets Broker (C1, never config). Permission detection is **direct** — the token response reports granted `permissions`, so verify compares to the required read set (metadata/contents/pull_requests/actions/members) and returns `degraded` with the missing `key:read` list (no probing needed, unlike AWS); malformed config/key or a failed exchange → `error`. `InstallationTokenProvider` interface keeps HTTP/JWT out of the connector for testability. 17 unit tests (config parse, permission logic, a real RS256 sign+verify round-trip via a generated keypair, verify decision matrix with a fake provider). Crawl stages stubbed (I2.3/I2.4). Gates green. **Next: I2.3** (pure parsers + normalize/extractSignals/observedEdges: workflows→deploy signals, CODEOWNERS→OWNED_BY, manifests→DEPENDS_ON_PKG, golden fixtures). |
| 2026-07-01 | architect | **I2.1 DONE — GitHub connector foundation (I2 started).** New `@atlas/connector-github` package (CommonJS, requireable by the CJS API). GitHub URN builders matching docs/05 §2.2: repo `github:<owner>/<repo>`, PR `…:pr:<n>`, workflow `…:workflow:<path>`, team `github:<owner>:team:<slug>`, user `github:user:<login>`, package `external:<ecosystem>:package:<name>` (I own the user/workflow/package schemes; grammar owned by docs/05). Case-significant keys (owner/repo/pkg name) preserved; login/ecosystem lowercased. `GITHUB_NODE_KINDS` catalog (5 github kinds + external.package). Migration **0009** seeds `external.package` (provider `external`, category `dependency`) — the DEPENDS_ON_PKG target (docs/07 §7.3); applied to live DB. 6 unit tests; gates green. **Next: I2.2** (GitHub App installation-token auth + verify/health + permission detection → degraded). |
| 2026-07-01 | architect | **I1.4 DONE — AWS crawler CODE-COMPLETE (live discoverers + resilience + wiring).** Added the live-crawl layer: a `Discoverer` interface + **14 discoverers** (EC2/VPC/Subnet/SG share one EC2 client; Lambda, ECS cluster/service/taskdef, ECR, ELB with target-group/health enrichment, Route53 manual record-paging, RDS, S3 multi-call config+tags, IAM inline-policy flattening) using SDK paginators + adaptive retry. Connector `discover`/`fetchDetail` collapse list+detail via a per-run payload cache; **one AssumeRole per run**. Resilience: `withRetry` (exp backoff + full jitter on throttling/transient; access-denied/not-found/fatal pass straight through) + `classifyAwsError` (docs/06 §7.3). Permission probes now **derived from discoverers** so verify does real detection. **Wired into the API:** ConnectorRegistry holds the real `AwsConnector` for "aws" (factory injects STS provider + Secrets-Broker accessor + probes); **enqueue-on-verify** creates an onboarding `sync_runs` row + `enqueueSync` (BR-SYNC-1 one-in-flight, 23505 swallowed); `JobQueue` = in-memory dev impl (BullMQ + worker process at deploy). **Incremental hash-diff** in the runner: unchanged content hash → skip re-snapshot/provenance + new `unchanged` stat (docs/06 §6). API boots clean (DI verified, /health 200); 54 connector-aws unit tests (aws-sdk-client-mock for EC2/Lambda/S3 + retry) + 11 ingest integration on live DB (incl. new incremental test). **Open: live-account verification** (needs a customer read-only role — no creds yet) + additive services (DynamoDB/ElastiCache/APIGW/IAM-policy). **Next: I2 (GitHub crawler).** |
| 2026-07-01 | architect | **I1.3 DONE — AWS service modules (pure normalize/observedEdges/extractSignals, golden fixtures).** Added a per-service `ServiceModule` layer the connector dispatches by node kind (additive per docs/06 §9). 14 modules — VPC, Subnet, SecurityGroup, EC2, Lambda, ECS cluster/service/taskdef, ECR, ELB, Route53, RDS, S3, IAM-role — covering **every observed edge type** in docs/05 §4: CONTAINS (vpc→subnet, cluster→service, subnet→ec2), PROTECTS (sg→ec2/lambda/rds/elb), ASSUMES_ROLE (ec2/lambda/taskdef→role), USES_IMAGE (taskdef→ecr, ECR-image parsing skips non-ECR), ROUTES_TO (elb→instance targets), TRIGGERS (s3→lambda). Signals emitted for inference: SG rules (R2), Lambda/ECS env (R3), RDS endpoint (R3), IAM policy statements (R8), ECS target-groups + Route53 alias (routing). Route53→ALB intentionally a **signal, not an observed edge** (alias DNS doesn't deterministically resolve to one ELB → P3, a missing edge beats a wrong one). Deterministic URNs via `awsUrn`; `plan()` fans region×service + global services once. Golden-fixture tests assert exact node URN/attrs + edge from/to/type + signal shape (100% observed-edge precision on fixtures). 44 unit tests. **Additive remaining (explicitly tracked, not dropped):** DynamoDB, ElastiCache, API Gateway, IAM-policy. **Next: I1.4** — live discover/fetchDetail (AWS SDK + pagination/retry/rate-limit), incremental hash-diff, and wiring into the runner/registry/API (enqueue-on-verify). |
| 2026-07-01 | architect | **I1.2 DONE — AWS credential provider + verify/health + permission detection.** `StsCredentialProvider` does `sts:AssumeRole` (Atlas's own identity → customer read-only role) with the per-connection **External ID** (confused-deputy defense, resolved from the Secrets Broker — C1, never in config) and `sessionName=atlas-{verify,health,sync}-<id>` so every action is attributable in the customer's CloudTrail; creds are ≤1h, in-memory only (A23/P8); account id parsed from the assumed-role ARN (URN scope). `parseAwsConfig` validates `roleArn` + a regions allow-list (A25). `verify`/`health` AssumeRole then probe each supported service → `AccessDenied` becomes a **`degraded`** connection listing the missing IAM actions (P3, FR-1.6, US-1); transient (non-deny) probe errors are NOT counted as missing; bad config / failed AssumeRole → `error` with actionable text. Permission-probe framework + `isAccessDenied` classification in place; concrete per-service probes attach in I1.3. 27 unit tests (config, verify decision matrix, STS via aws-sdk-client-mock). Crawl stages stubbed (throw, I1.3). Gates green. **Next: I1.3** (service discoverers + pure normalize/extractSignals/observedEdges, golden fixtures). |
| 2026-07-01 | architect | **I1.1 DONE — AWS connector foundation (Phase I started).** New `@atlas/connector-aws` package (CommonJS so the CJS NestJS API can `require` it). `awsUrn()` builds the deterministic, recomputable 5-segment URN (`aws:<region\|global>:<account>:<type>:<key>`) matching every documented pattern in docs/05 §2.2 — region in scope for multi-account safety, literal `global` for S3/Route53/IAM, natural key case-preserved (Lambda/DynamoDB names are case-significant). `AWS_NODE_KINDS` catalog = the 18 MVP kinds (docs/05 §3.1 / docs/06 §4) with URN type discriminator + region/global scope. Migration **0008_node_kinds_seed** seeds `node_kinds` global vocab (24 rows = 18 AWS + 5 GitHub + 1 `atlas.service`) — required before any node insert (FK `nodes.kind`). Idempotent (ON CONFLICT DO NOTHING). 9 unit tests; applied to live DB (counts verified). Gates green. **Next: I1.2** (STS AssumeRole + verify + degraded permission detection). |
| 2026-07-01 | architect | **Phase F COMPLETE — F2 (connector framework + secrets + queue) DONE.** F2.1 connections/sync_runs schema · F2.2 `@atlas/connector-sdk` (frozen Connector contract) · F2.3 graph persist schema (nodes/edges/provenance/raw_snapshots, composite-FK same-org edges, uq_edge NULLS NOT DISTINCT) · F2.4 `@atlas/ingest` staged sync runner + MockConnector (idempotent/resumable/BR-SYNC-2, exit proof) · F2.5 JobQueue (InMemory + BullMQ) + sync worker · F2.6 SecretBroker · F2.7 Supabase Storage snapshots (live round-trip) · F2.8 connection API (live create→verify→connected). New packages: `@atlas/connector-sdk`, `@atlas/ingest`. ~18 integration tests on live PG + Storage; CI green (check + integration jobs). **Next: I1 (AWS) ∥ I2 (GitHub) crawlers** implement the frozen SDK. |
| 2026-07-01 | architect | **F1.7 CI Postgres integration job DONE — F1 Foundation epic COMPLETE.** Added CI `integration` job (postgres:16 service → migrations as owner → grant atlas_app LOGIN via `setup-app-role.ts` → env-gated integration tests with two-role + RLS). Expanded `org-scope.test.ts`: organizations org-scope + fail-closed, invitations cross-org isolation, `app_user_memberships`/`app_invitation_by_token` resolvers, write-guard negative. 9 integration tests pass on live PG; skip in the no-DB job (21 unit tests green). Updated docs/14 ref + ci.yml header. **Next: F2** per docs/15. |
| 2026-07-01 | architect | **F1.6 Org / RBAC / memberships / invitations DONE — live-verified through the app.** Built `TenantScopeGuard` (active org + live role) + `@Roles`/`RolesGuard` (Owner>Admin>Member); orgs/members/invitations endpoints with BR-ORG-1 / BR-MEM-2/3 invariants; invitation accept by sha256-hashed capability token (never returned). Added `{data}`/`{error}` API envelopes (global interceptor + exception filter), zod validation, CORS. Migration 0005 (organizations org-scope RLS + `app_invitation_by_token` SECURITY DEFINER resolver). Synced docs/12 §5.2 + docs/08 §6. Frontend: create-org + org-management panel (members/invitations/invite). Verified live: real session → create org (Owner), invite (pending, hashed), members RLS-scoped read 200, last-Owner demote rejected (still Owner), cross-tenant 404. Unit tests RolesGuard+slug (api 15 tests). All gates green. |
| 2026-07-01 | architect | **F1.5 Supabase Auth DONE — live Google login verified end-to-end.** Rewrote `docs/12` §2–4 (v1.1, docs-before-code). Built NestJS auth (CoreModule env+atlas_app pool, `SupabaseJwtVerifier` via JWKS/ES256, `AuthGuard`, `UserMirrorService`, `MembershipService`, `GET /me`, claims unit tests) + Next.js Google sign-in (`@supabase/ssr` client/server/middleware, login page, `/auth/callback`, server home calling `/me`). Migrations 0003 (`app_user_memberships` SECURITY DEFINER resolver) + 0004 (identity-table RLS policies `TO atlas_app` — discovered Supabase auto-enables RLS on **all** public tables; global identity tables had no policy → fixed). Configured Supabase URL config (Site URL + redirect `localhost:4291/**`) via Chrome. Verified: real Google account → consent → `/me` 200 with mirrored user (`users` id=auth uid + `auth_identities` google sub + gmail.com domain), email ✓verified, empty-orgs onboarding path. Guard 401s confirmed. **Ports → API :4290 / web :4291** (avoid clashes, per user). All gates green. |
| 2026-07-01 | architect | **Google auth provider configured** (via Chrome): Google Cloud project + Google Auth Platform consent screen + OAuth client "Atlas Web" + redirect URI (Supabase callback) + test user added; **Supabase Google provider enabled** with real Client ID/secret. Prereq for F1.5 done. (Secrets in `.env`/dashboards only, never committed.) |
| 2026-06-30 | architect | **F1.4 ✅** — `@atlas/db` (plain `pg`, **Kysely removed**): 5 core tables, SQL migration runner, `atlas_app` non-bypass role + RLS, `withOrgScope`. **Verified on live Supabase** (RLS 3/3). Decisions from real testing: session-pooler/IPv4, two-role model (postgres BYPASSRLS), `NULLIF` GUC. |
| 2026-06-30 | architect | **Supabase project live** — connected, schema + RLS migrated to the real DB. `.env` set (app=atlas_app, migrations=postgres). Google provider + connection-string setup done via browser. |
| 2026-06-30 | architect | **Decision: adopt Supabase** (managed Postgres + Auth/Google + Storage). Backend/graph/AI unchanged; keep GUC-RLS isolation. Recorded in CLAUDE.md + memory; banners added to docs 02/04/12/13/17. Reordered F1 to DB-first. |
| 2026-06-30 | architect | **F1.3 ✅** — NestJS API shell (Fastify, `/health` boots OK) + Next.js 15 web shell + Turborepo build orchestration. `@atlas/config` → CJS dist consumed at runtime. All gates green. |
| 2026-06-30 | architect | **F1.2 ✅** — GitHub Actions CI (`ci.yml`) running format/lint/typecheck/test on push to main. First run green (22s). README badge added. |
| 2026-06-30 | architect | Adopted **main-only git workflow** (no branches/PRs yet; `dev` later). Cleaned history to 2 commits, no AI attribution. F1.1 on `main`. |
| 2026-06-30 | architect | **F1.1 ✅** — monorepo scaffold (pnpm workspaces, strict TS, eslint/prettier, vitest) + `@atlas/config` (Zod env). All gates green. |
| 2026-06-30 | architect | Pushed to **private** GitHub remote `anmolbhardwaj17/atlas`. |
| 2026-06-30 | architect | `git init` (repo `atlas`, branch `main`); initial commit `ec99660` with blueprint + scaffolding (28 files). SET-6 ✅. |
| 2026-06-30 | architect | Set up continuity scaffolding: CLAUDE.md, docs/README.md, this board, skills, memory (EPIC SETUP). |
| 2026-06-30 | architect | Completed full blueprint: wrote all 19 design docs `00`–`18` + `07b` (EPIC 0 ✅). |
| 2026-06-30 | architect | Interactive decisions captured: Bitbucket=Phase-2, Google-only auth + `hd` domain-join Phase-1, shadcn+MCP, adversarial QA agent. Propagated across 00/01/03/04/08/09/12/16. |
